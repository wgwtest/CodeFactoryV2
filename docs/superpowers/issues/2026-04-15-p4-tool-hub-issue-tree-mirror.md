# P4 工具中台本地 Issue 树镜像

> 当前工作树内未提供可直接提交的 GitHub Issue / Project 承载面，因此这里维护一份本地镜像，作为当前轮次的 WBS 树形结构落位记录。后续若接入 GitHub issue 树，应按相同编号与父子关系回写。

## WBS Tree

- `P4` 工具仓库 / 工具中台 `[开发中]`
  - `P4.1` 第一批最小闭环 `[开发中]`
    - `P4.1.1` 工具描述模型 `[已形成初版]`
    - `P4.1.2` 工具仓库 CRUD `[已形成初版]`
    - `P4.1.3` 工具分类与标签体系 `[已形成初版]`
    - `P4.1.4` 工具匹配规则 MVP `[已形成初版]`
    - `P4.1.5` 工具验证工作台 `[已形成初版]`
    - `P4.1.6` 统一数据层与同源快照验证 `[待开发]`

## 当前节点说明

`P4.1.6` 的目标不是继续扩页面能力，而是为当前四个一级工作区补齐统一数据层和快照同源验证机制，确保：

- 总览、输入工具链、自演进巡检、工具仓库消费同一份状态快照
- 指标、风险摘要、覆盖矩阵、运行监视等状态被明确归入派生层
- 多路读接口与前端并发消费过程能够显式验证 `snapshot_id`
