# 知识库管理模块

职责：管理知识库列表、创建知识库、绑定资料来源，并进入某个知识库工作区。

边界：
- 拥有 `/p1` 上层入口。
- 只输出 `archiveId` 给工作区。
- 不读取资料接入、策略规则、抽取运行、质量图谱、发布输出模块的内部状态。

输入合同：
- 无必需工作区上下文。

输出合同：
- `archiveId`：进入 `/p1/archives/:archiveId/...` 工作区的唯一上下文锚点。

测试入口：
- `testEntry.ts` 暴露本模块验收点。
