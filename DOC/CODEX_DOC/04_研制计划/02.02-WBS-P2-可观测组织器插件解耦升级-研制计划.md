# P2 可观测组织器插件解耦升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `feat/p2-validation-workflow` 中把 `P2` 需求分析 Lab 的组织器升级为可切换、可观测的插件宿主，使自研组织器和未来 `Dify workflow` 组织器都能通过同一输入输出合同接入。

**Architecture:** 先新增 `xg-observable-orchestrator-contract@1` 合同和插件注册层，再把当前本地强规则/启发式组织器包装成完整观测插件，最后收缩 `TurnEngine` 对固定阶段链的直接依赖。`Dify` 本轮只作为有限观测 fake 插件预留，不接真实平台。

**Tech Stack:** FastAPI, Pydantic, Python service layer, pytest, React, TypeScript, Ant Design, Vitest.

---

## 0. 分支和范围约束

### 当前 worktree

```text
/home/wgw/CodexProject/CodeFactoryV2/.worktrees/p2-validation-workflow
```

### 当前分支

```text
feat/p2-validation-workflow
```

### 设计依据

```text
DOC/CODEX_DOC/02_设计说明/P2_需求分析系统/P2-需求分析系统设计-260506-可观测组织器插件解耦补充.md
```

### 不进入本计划的内容

- 不重做 `P2` 专家正式工作台。
- 不重做模板配置台。
- 不接真实 `Dify` API。
- 不迁移 `feat/p2-requirement-analysis-system` 分支职责。
- 不合并本补充设计到主设计说明正文。

---

## 1. 文件结构

### 后端新增文件

- Create: `apps/api/app/orchestrators/plugin_contracts.py`
  - 定义 `OrchestratorPluginManifest`、`OrchestratorRunRequest`、`OrchestratorRunResult` 和嵌套 Pydantic 模型。
- Create: `apps/api/app/orchestrators/plugin_registry.py`
  - 统一列出当前可用插件，包括本地 `xg` 组织器插件和 fake `Dify` 插件。
- Create: `apps/api/app/orchestrators/plugin_result_normalizer.py`
  - 将插件输出归一化为当前 `TurnEngine` 可落库的结构。
- Create: `apps/api/app/orchestrators/adapters/__init__.py`
  - 插件适配器包入口。
- Create: `apps/api/app/orchestrators/adapters/base.py`
  - 定义插件适配器协议。
- Create: `apps/api/app/orchestrators/adapters/local_xg_plugin.py`
  - 包装当前 `intent/write/review/next_interaction` 阶段链，输出完整观测合同。
- Create: `apps/api/app/orchestrators/adapters/dify_workflow_plugin.py`
  - fake `Dify workflow` 插件适配器，返回有限观测合同。
- Create: `apps/api/tests/test_orchestrator_plugin_contracts.py`
  - 插件合同、注册、fake `Dify`、本地插件输出的单元测试。

### 后端修改文件

- Modify: `apps/api/app/orchestrators/package_loader.py`
  - 在现有 `OrchestratorPackage.to_api()` 输出中补充插件能力字段，或由插件注册层统一包裹。
- Modify: `apps/api/app/requirement_analysis/session_service.py`
  - `list_orchestrators()` 改为展示插件 manifest 和 capabilities。
  - `create_session()` 继续接受现有 `orchestrator_id`，但校验来源切到插件注册层。
- Modify: `apps/api/app/requirement_analysis/turn_engine.py`
  - 分阶段迁移为插件宿主。
- Modify: `apps/api/app/requirement_analysis/provider_call_log_service.py`
  - 接收插件输出的 provider logs / workflow trace。
- Modify: `apps/api/tests/test_orchestrator_runtime.py`
  - 保留既有包装载测试，新增本地组织器插件适配断言。
- Modify: `apps/api/tests/test_requirement_analysis_api.py`
  - 断言组织器列表出现插件能力声明。
  - 断言当前本地组织器输出不退化。
  - 断言 fake `Dify` 插件可被选择并返回有限观测结果。

### 前端修改文件

- Modify: `apps/web/src/lib/api.ts`
  - 扩展 `RequirementAnalysisOrchestrator` 类型，增加 `plugin_type`、`observability_level`、`capabilities`。
- Modify: `apps/web/src/pages/RequirementAnalysisLabPage.tsx`
  - 组织器卡片展示能力声明。
  - Current Turn 对缺失过程输出做降级提示。
- Modify: `apps/web/src/test/RequirementAnalysisLabPage.test.tsx`
  - 增加组织器能力显示和有限观测降级展示测试。

### 正式文档修改

- Modify: `DOC/CODEX_DOC/04_研制计划/02-WBS-P2-需求分析系统-研制计划.md`
  - 增加本升级计划入口。

---

## 2. Task 1：新增插件合同和注册层

**Files:**

- Create: `apps/api/app/orchestrators/plugin_contracts.py`
- Create: `apps/api/app/orchestrators/plugin_registry.py`
- Create: `apps/api/tests/test_orchestrator_plugin_contracts.py`
- Modify: `apps/api/app/orchestrators/__init__.py`

### 2.1 写失败测试

- [ ] 在 `apps/api/tests/test_orchestrator_plugin_contracts.py` 新增测试：

```python
from app.orchestrators.plugin_contracts import (
    OrchestratorPluginManifest,
    OrchestratorRunRequest,
    OrchestratorRunResult,
)
from app.orchestrators.plugin_registry import OrchestratorPluginRegistry


def test_observable_orchestrator_plugin_manifest_contract() -> None:
    manifest = OrchestratorPluginManifest(
        plugin_id="xg-local-heuristic-orchestrator",
        name="XG Local Heuristic Orchestrator",
        plugin_type="local_package",
        document_type="xg",
        contract="xg-observable-orchestrator-contract@1",
        status="active",
        priority=10,
        capabilities={
            "filled_document_text": False,
            "document_patch": True,
            "stage_results": True,
            "stage_audits": True,
            "provider_logs": True,
            "decision_trace": True,
            "review_after_apply": True,
            "spec_tree_update": True,
            "streaming_events": False,
        },
        requires={"template": True, "model_provider": "optional"},
        adapter_entry="local_xg",
    )

    assert manifest.plugin_id == "xg-local-heuristic-orchestrator"
    assert manifest.observability_level == "full"
    assert manifest.capabilities["stage_audits"] is True
    assert manifest.to_api()["contract"] == "xg-observable-orchestrator-contract@1"


def test_orchestrator_run_request_and_result_contract() -> None:
    request = OrchestratorRunRequest(
        contract_version="xg-observable-orchestrator-contract@1",
        session={
            "session_id": "ra-001",
            "topic": "空域运算软件需求规格探索",
            "template_id": "81433号",
            "knowledge_package_id": "airspace-domain-demo",
            "orchestrator_id": "xg-local-heuristic-orchestrator",
            "provider_id": "mock",
            "model": "mock-requirement-analysis-v1",
            "write_policy": "patch_suggestion_only",
        },
        turn={
            "turn_id": "turn-0001",
            "turn_index": 1,
            "user_input": "这个系统叫空域运算软件",
            "normalized_input": {"input_type": "free_text", "semantic": "这个系统叫空域运算软件"},
            "previous_interaction": {"type": "none"},
            "input_relation": {"relation": "none"},
        },
        template={
            "template_id": "81433号",
            "format": "structured",
            "content": "",
            "parsed_structure": {"source": "spec_tree"},
        },
        document_context={
            "working_document": {"document_id": "lab-working-document", "blocks": []},
            "active_spec_node": {"node_id": "SPEC-REQ-1.1", "target_section": "1 总则 / 编写目的"},
            "spec_tree": [],
            "confirmed_facts": [],
            "open_questions": [],
            "patches": [],
            "history_summary": "",
        },
        execution_options={
            "expected_output": "both",
            "observability_required": "full",
            "streaming_enabled": False,
        },
    )
    assert request.turn["turn_id"] == "turn-0001"
    assert request.template["format"] == "structured"

    result = OrchestratorRunResult(
        contract_version="xg-observable-orchestrator-contract@1",
        plugin={
            "plugin_id": "xg-local-heuristic-orchestrator",
            "plugin_type": "local_package",
            "observability_level": "full",
        },
        final_output={
            "filled_document_text": "",
            "document_patch": [{"plan_ref": "AP-001", "operation": "append_or_update", "content": "软件名称为：空域运算软件。"}],
            "changed_sections": ["REQ-1.1"],
            "completion_status": "partial",
            "confidence": "medium",
        },
        interaction_output={
            "assistant_message": "本轮已补入临时正文。",
            "next_question": "请确认软件定位。",
            "quick_options": [],
            "suggested_focus": {"target_spec_node_ids": ["SPEC-REQ-2.1"]},
        },
        process_output={
            "stage_results": [],
            "stage_audits": [],
            "decision_trace": ["插件输出通过合同校验。"],
            "provider_logs": [],
            "review_after_apply_result": {},
            "annotations": [],
            "risks": [],
        },
        state_output={
            "confirmed_facts_delta": ["软件名称初步确认：空域运算软件"],
            "open_questions_delta": ["请确认软件定位。"],
            "spec_tree_update": {},
            "working_document_update": {},
            "turn_path_update": {},
        },
        raw_output={
            "raw_plugin_response": {},
            "raw_model_response": {},
            "raw_workflow_trace": {},
        },
    )
    assert result.plugin["observability_level"] == "full"
    assert result.final_output["document_patch"][0]["plan_ref"] == "AP-001"


def test_plugin_registry_lists_local_and_dify_plugins() -> None:
    registry = OrchestratorPluginRegistry()
    plugins = registry.list_plugins()
    plugin_ids = {plugin.plugin_id for plugin in plugins}

    assert "xg-local-heuristic-orchestrator" in plugin_ids
    assert "xg-local-strong-rule-orchestrator" in plugin_ids
    assert "xg-dify-workflow-orchestrator" in plugin_ids

    dify = registry.require("xg-dify-workflow-orchestrator")
    assert dify.plugin_type == "dify_workflow"
    assert dify.observability_level == "limited"
    assert dify.capabilities["filled_document_text"] is True
    assert dify.capabilities["stage_audits"] is False
```

### 2.2 运行失败测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py -q
```

预期：

```text
FAIL: ModuleNotFoundError: No module named 'app.orchestrators.plugin_contracts'
```

### 2.3 实现合同模型

- [ ] 新增 `apps/api/app/orchestrators/plugin_contracts.py`：

```python
from __future__ import annotations

from typing import Any, Literal

from pydantic import BaseModel, ConfigDict, Field


PluginType = Literal["local_package", "dify_workflow", "remote_service"]
ObservabilityLevel = Literal["full", "limited", "none"]

OBSERVABILITY_CAPABILITY_KEYS = (
    "stage_results",
    "stage_audits",
    "provider_logs",
    "decision_trace",
    "review_after_apply",
    "spec_tree_update",
)


class OrchestratorPluginManifest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    plugin_id: str
    name: str
    plugin_type: PluginType
    document_type: str = "xg"
    contract: str = "xg-observable-orchestrator-contract@1"
    status: str = "active"
    priority: int = 100
    capabilities: dict[str, bool] = Field(default_factory=dict)
    requires: dict[str, Any] = Field(default_factory=dict)
    adapter_entry: str

    @property
    def observability_level(self) -> ObservabilityLevel:
        if all(bool(self.capabilities.get(key)) for key in OBSERVABILITY_CAPABILITY_KEYS):
            return "full"
        if any(bool(self.capabilities.get(key)) for key in OBSERVABILITY_CAPABILITY_KEYS) or bool(self.capabilities.get("filled_document_text")) or bool(self.capabilities.get("document_patch")):
            return "limited"
        return "none"

    def to_api(self) -> dict[str, Any]:
        return {
            "plugin_id": self.plugin_id,
            "orchestrator_id": self.plugin_id,
            "name": self.name,
            "plugin_type": self.plugin_type,
            "document_type": self.document_type,
            "contract": self.contract,
            "status": self.status,
            "priority": self.priority,
            "capabilities": dict(self.capabilities),
            "requires": dict(self.requires),
            "adapter_entry": self.adapter_entry,
            "observability_level": self.observability_level,
        }


class OrchestratorRunRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    contract_version: str
    session: dict[str, Any]
    turn: dict[str, Any]
    template: dict[str, Any]
    document_context: dict[str, Any]
    execution_options: dict[str, Any]


class OrchestratorRunResult(BaseModel):
    model_config = ConfigDict(extra="forbid")

    contract_version: str
    plugin: dict[str, Any]
    final_output: dict[str, Any]
    interaction_output: dict[str, Any]
    process_output: dict[str, Any]
    state_output: dict[str, Any]
    raw_output: dict[str, Any]
```

### 2.4 实现插件注册层

- [ ] 新增 `apps/api/app/orchestrators/plugin_registry.py`：

```python
from __future__ import annotations

from functools import lru_cache

from app.orchestrators.plugin_contracts import OrchestratorPluginManifest


def _full_local_capabilities() -> dict[str, bool]:
    return {
        "filled_document_text": False,
        "document_patch": True,
        "stage_results": True,
        "stage_audits": True,
        "provider_logs": True,
        "decision_trace": True,
        "review_after_apply": True,
        "spec_tree_update": True,
        "streaming_events": False,
    }


class OrchestratorPluginRegistry:
    def __init__(self) -> None:
        self._plugins = {
            "xg-local-heuristic-orchestrator": OrchestratorPluginManifest(
                plugin_id="xg-local-heuristic-orchestrator",
                name="XG Local Heuristic Orchestrator",
                plugin_type="local_package",
                status="active",
                priority=10,
                capabilities=_full_local_capabilities(),
                requires={"template": True, "knowledge_binding": True, "model_provider": "optional"},
                adapter_entry="local_xg",
            ),
            "xg-local-strong-rule-orchestrator": OrchestratorPluginManifest(
                plugin_id="xg-local-strong-rule-orchestrator",
                name="XG Local Strong Rule Orchestrator",
                plugin_type="local_package",
                status="active",
                priority=20,
                capabilities=_full_local_capabilities(),
                requires={"template": True, "knowledge_binding": False, "model_provider": "optional"},
                adapter_entry="local_xg",
            ),
            "xg-dify-workflow-orchestrator": OrchestratorPluginManifest(
                plugin_id="xg-dify-workflow-orchestrator",
                name="XG Dify Workflow Orchestrator",
                plugin_type="dify_workflow",
                status="available",
                priority=90,
                capabilities={
                    "filled_document_text": True,
                    "document_patch": False,
                    "stage_results": False,
                    "stage_audits": False,
                    "provider_logs": False,
                    "decision_trace": False,
                    "review_after_apply": False,
                    "spec_tree_update": False,
                    "streaming_events": True,
                },
                requires={"template": True, "knowledge_binding": False, "model_provider": "external_workflow"},
                adapter_entry="dify_workflow",
            ),
        }

    def list_plugins(self) -> list[OrchestratorPluginManifest]:
        return sorted(self._plugins.values(), key=lambda item: (item.priority, item.plugin_id))

    def get(self, plugin_id: str) -> OrchestratorPluginManifest | None:
        return self._plugins.get(self._normalize(plugin_id))

    def require(self, plugin_id: str) -> OrchestratorPluginManifest:
        plugin = self.get(plugin_id)
        if plugin is None:
            raise ValueError("unsupported orchestrator")
        return plugin

    @staticmethod
    def _normalize(plugin_id: str) -> str:
        mapping = {
            "xg-heuristic-orchestrator": "xg-local-heuristic-orchestrator",
            "xg-strong-rule-orchestrator": "xg-local-strong-rule-orchestrator",
        }
        return mapping.get(plugin_id, plugin_id)


@lru_cache(maxsize=1)
def get_orchestrator_plugin_registry() -> OrchestratorPluginRegistry:
    return OrchestratorPluginRegistry()
```

### 2.5 导出模块入口

- [ ] 修改 `apps/api/app/orchestrators/__init__.py`，确保可以导入新增模块。若当前文件为空，可以保持空文件；无需额外导出。

### 2.6 运行测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py -q
```

预期：

```text
3 passed
```

### 2.7 提交

- [ ] 提交：

```bash
git add apps/api/app/orchestrators/plugin_contracts.py \
  apps/api/app/orchestrators/plugin_registry.py \
  apps/api/tests/test_orchestrator_plugin_contracts.py
git commit -m "feat: add observable orchestrator plugin contracts"
```

---

## 3. Task 2：将组织器列表切到插件 manifest

**Files:**

- Modify: `apps/api/app/requirement_analysis/session_service.py`
- Modify: `apps/api/tests/test_requirement_analysis_api.py`
- Modify: `apps/web/src/lib/api.ts`
- Modify: `apps/web/src/test/RequirementAnalysisLabPage.test.tsx`

### 3.1 写后端失败测试

- [ ] 修改 `apps/api/tests/test_requirement_analysis_api.py` 中 `test_requirement_analysis_lab_session_turn_and_recovery` 的组织器列表断言，将现有 `xg-heuristic-orchestrator` 断言升级为插件断言：

```python
    orchestrators = client.get("/api/requirement-analysis/orchestrators")
    assert orchestrators.status_code == 200
    items = orchestrators.json()["items"]
    plugin_ids = {item["plugin_id"] for item in items}
    assert {
        "xg-local-heuristic-orchestrator",
        "xg-local-strong-rule-orchestrator",
        "xg-dify-workflow-orchestrator",
    }.issubset(plugin_ids)

    heuristic = next(item for item in items if item["plugin_id"] == "xg-local-heuristic-orchestrator")
    assert heuristic["orchestrator_id"] == "xg-local-heuristic-orchestrator"
    assert heuristic["plugin_type"] == "local_package"
    assert heuristic["document_type"] == "xg"
    assert heuristic["contract"] == "xg-observable-orchestrator-contract@1"
    assert heuristic["observability_level"] == "full"
    assert heuristic["capabilities"]["document_patch"] is True
    assert heuristic["capabilities"]["stage_audits"] is True

    dify = next(item for item in items if item["plugin_id"] == "xg-dify-workflow-orchestrator")
    assert dify["plugin_type"] == "dify_workflow"
    assert dify["observability_level"] == "limited"
    assert dify["capabilities"]["filled_document_text"] is True
    assert dify["capabilities"]["stage_audits"] is False
```

### 3.2 运行后端失败测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_requirement_analysis_api.py::test_requirement_analysis_lab_session_turn_and_recovery -q
```

预期：

```text
FAIL: KeyError: 'plugin_id'
```

### 3.3 实现后端列表切换

- [ ] 修改 `apps/api/app/requirement_analysis/session_service.py`：

```python
from app.orchestrators.plugin_registry import get_orchestrator_plugin_registry
```

- [ ] 将 `list_orchestrators()` 改为：

```python
    def list_orchestrators(self) -> dict:
        registry = get_orchestrator_plugin_registry()
        return {
            "items": [plugin.to_api() for plugin in registry.list_plugins()],
            "stable_contract": self._stable_contract(),
            "output_protocol": [
                "previous_interaction",
                "input_relation",
                "spec_execution",
                "post_update_review",
                "closure_decision",
                "next_interaction",
                "decision_trace",
            ],
        }
```

### 3.4 保持旧 ID 兼容到当前阶段

- [ ] 在 `session_service.py` 中 `_normalize_orchestrator_id()` 保留旧 ID 输入。目标行为：

```text
xg-heuristic-orchestrator -> xg-local-heuristic-orchestrator
xg-strong-rule-orchestrator -> xg-local-strong-rule-orchestrator
```

实现约束：

1. 会话里可以保存新插件 ID。
2. 若现有测试或页面仍传旧 ID，后端暂时接受并规范化。

### 3.5 扩展前端类型

- [ ] 在 `apps/web/src/lib/api.ts` 找到 `RequirementAnalysisOrchestrator` 类型，增加：

```ts
  plugin_id?: string;
  plugin_type?: "local_package" | "dify_workflow" | "remote_service";
  observability_level?: "full" | "limited" | "none";
  capabilities?: Record<string, boolean>;
```

### 3.6 前端 mock 数据补充能力字段

- [ ] 修改 `apps/web/src/test/RequirementAnalysisLabPage.test.tsx` 中 `buildOrchestrators()` 返回值，让每个组织器都有：

```ts
{
  plugin_id: "xg-local-heuristic-orchestrator",
  orchestrator_id: "xg-local-heuristic-orchestrator",
  plugin_type: "local_package",
  observability_level: "full",
  capabilities: {
    filled_document_text: false,
    document_patch: true,
    stage_results: true,
    stage_audits: true,
    provider_logs: true,
    decision_trace: true,
    review_after_apply: true,
    spec_tree_update: true,
    streaming_events: false,
  }
}
```

并增加 `xg-dify-workflow-orchestrator` fake 项。

### 3.7 运行测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_requirement_analysis_api.py::test_requirement_analysis_lab_session_turn_and_recovery -q
corepack pnpm --dir apps/web exec vitest run src/test/RequirementAnalysisLabPage.test.tsx
```

预期：

```text
PASS
```

### 3.8 提交

- [ ] 提交：

```bash
git add apps/api/app/requirement_analysis/session_service.py \
  apps/api/tests/test_requirement_analysis_api.py \
  apps/web/src/lib/api.ts \
  apps/web/src/test/RequirementAnalysisLabPage.test.tsx
git commit -m "feat: expose observable orchestrator plugin manifests"
```

---

## 4. Task 3：新增本地 XG 插件适配器并保持行为不变

**Files:**

- Create: `apps/api/app/orchestrators/adapters/__init__.py`
- Create: `apps/api/app/orchestrators/adapters/base.py`
- Create: `apps/api/app/orchestrators/adapters/local_xg_plugin.py`
- Create: `apps/api/app/orchestrators/plugin_result_normalizer.py`
- Modify: `apps/api/tests/test_orchestrator_plugin_contracts.py`
- Modify: `apps/api/tests/test_orchestrator_runtime.py`

### 4.1 写失败测试

- [ ] 在 `apps/api/tests/test_orchestrator_plugin_contracts.py` 增加：

```python
from app.orchestrators.adapters.local_xg_plugin import LocalXGOrchestratorPluginAdapter
from app.orchestrators.plugin_registry import OrchestratorPluginRegistry


def test_local_xg_plugin_wraps_existing_runner_output() -> None:
    registry = OrchestratorPluginRegistry()
    manifest = registry.require("xg-local-strong-rule-orchestrator")
    adapter = LocalXGOrchestratorPluginAdapter(manifest=manifest)
    request = OrchestratorRunRequest(
        contract_version="xg-observable-orchestrator-contract@1",
        session={
            "session_id": "ra-001",
            "topic": "空域运算软件需求规格探索",
            "template_id": "81433号",
            "knowledge_package_id": "airspace-domain-demo",
            "orchestrator_id": "xg-local-strong-rule-orchestrator",
            "provider_id": "mock",
            "model": "mock-requirement-analysis-v1",
            "write_policy": "patch_suggestion_only",
        },
        turn={
            "turn_id": "turn-0001",
            "turn_index": 1,
            "user_input": "这个系统叫空域运算软件，主要解决空域计算分析需求",
            "normalized_input": {
                "input_type": "free_text",
                "semantic": "这个系统叫空域运算软件，主要解决空域计算分析需求",
            },
            "previous_interaction": {"type": "none"},
            "input_relation": {"relation": "none"},
        },
        template={"template_id": "81433号", "format": "structured", "content": "", "parsed_structure": {}},
        document_context={
            "working_document": {"document_id": "lab-working-document", "blocks": []},
            "active_spec_node": {
                "node_id": "SPEC-REQ-1.1",
                "title": "REQ-1.1 编写目的",
                "target_section": "1 总则 / 编写目的",
                "question": "系统要做什么？",
            },
            "spec_tree": [],
            "confirmed_facts": [],
            "open_questions": [],
            "patches": [],
            "history_summary": "",
        },
        execution_options={"expected_output": "both", "observability_required": "full", "streaming_enabled": False},
    )

    result = adapter.run(request)

    assert result.plugin["plugin_id"] == "xg-local-strong-rule-orchestrator"
    assert result.plugin["observability_level"] == "full"
    assert result.final_output["document_patch"][0]["plan_ref"] == "AP-001"
    assert result.interaction_output["assistant_message"].startswith("强规则组织器")
    assert result.state_output["confirmed_facts_delta"]
    assert result.raw_output["raw_model_response"]["runner_invoked"] is True
```

### 4.2 运行失败测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py::test_local_xg_plugin_wraps_existing_runner_output -q
```

预期：

```text
FAIL: ModuleNotFoundError: No module named 'app.orchestrators.adapters'
```

### 4.3 实现适配器协议

- [ ] 新增 `apps/api/app/orchestrators/adapters/base.py`：

```python
from __future__ import annotations

from typing import Protocol

from app.orchestrators.plugin_contracts import OrchestratorRunRequest, OrchestratorRunResult


class OrchestratorPluginAdapter(Protocol):
    def run(self, request: OrchestratorRunRequest) -> OrchestratorRunResult:
        ...
```

- [ ] 新增空文件 `apps/api/app/orchestrators/adapters/__init__.py`。

### 4.4 实现本地 XG 插件适配器

- [ ] 新增 `apps/api/app/orchestrators/adapters/local_xg_plugin.py`：

```python
from __future__ import annotations

from app.orchestrators.plugin_contracts import OrchestratorPluginManifest, OrchestratorRunRequest, OrchestratorRunResult
from app.orchestrators.runner_host import OrchestratorRunnerHost


LOCAL_PLUGIN_TO_PACKAGE_ID = {
    "xg-local-heuristic-orchestrator": "xg-heuristic-orchestrator",
    "xg-local-strong-rule-orchestrator": "xg-strong-rule-orchestrator",
}


class LocalXGOrchestratorPluginAdapter:
    def __init__(self, *, manifest: OrchestratorPluginManifest, runner_host: OrchestratorRunnerHost | None = None) -> None:
        self.manifest = manifest
        self.runner_host = runner_host or OrchestratorRunnerHost()

    def run(self, request: OrchestratorRunRequest) -> OrchestratorRunResult:
        package_id = LOCAL_PLUGIN_TO_PACKAGE_ID.get(self.manifest.plugin_id, self.manifest.plugin_id)
        output = self.runner_host.execute_local_runner(
            package_id,
            context={
                "session": request.session,
                "user_input": request.turn["user_input"],
                "normalized": request.turn["normalized_input"],
                "active_spec_node": request.document_context.get("active_spec_node") or {},
                "state": {
                    "working_document": request.document_context.get("working_document") or {},
                    "spec_tree": request.document_context.get("spec_tree") or [],
                    "confirmed_facts": request.document_context.get("confirmed_facts") or [],
                    "open_questions": request.document_context.get("open_questions") or [],
                    "patches": request.document_context.get("patches") or [],
                },
            },
        )
        document_patch = list(output.get("document_patch") or [])
        raw_model_response = dict(output.get("raw_model_response") or {})
        return OrchestratorRunResult(
            contract_version=request.contract_version,
            plugin={
                "plugin_id": self.manifest.plugin_id,
                "plugin_type": self.manifest.plugin_type,
                "observability_level": self.manifest.observability_level,
            },
            final_output={
                "filled_document_text": "",
                "document_patch": document_patch,
                "changed_sections": [
                    str(plan.get("template_clause_id") or "")
                    for plan in list(output.get("target_anchor_plan") or [])
                    if str(plan.get("template_clause_id") or "").strip()
                ],
                "completion_status": "partial",
                "confidence": str(output.get("confidence") or "medium"),
            },
            interaction_output={
                "assistant_message": str(output.get("assistant_message") or ""),
                "next_question": str(output.get("next_question") or ""),
                "quick_options": list(output.get("quick_options") or []),
                "suggested_focus": dict(output.get("next_suggestion") or {}),
            },
            process_output={
                "stage_results": [],
                "stage_audits": [
                    {
                        "stage_id": "run",
                        "stage_kind": "local_runner",
                        "status": "completed",
                        "summary": "本地 XG 组织器 runner 已返回可观测插件结果。",
                    }
                ],
                "decision_trace": list(output.get("annotations") or []),
                "provider_logs": [],
                "review_after_apply_result": {},
                "annotations": list(output.get("annotations") or []),
                "risks": list(output.get("risks") or []),
            },
            state_output={
                "confirmed_facts_delta": list(output.get("confirmed_facts_delta") or []),
                "open_questions_delta": list(output.get("open_questions_delta") or []),
                "spec_tree_update": {},
                "working_document_update": {},
                "turn_path_update": {},
            },
            raw_output={
                "raw_plugin_response": output,
                "raw_model_response": raw_model_response,
                "raw_workflow_trace": {},
            },
        )
```

### 4.5 运行测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py -q
```

预期：

```text
4 passed
```

### 4.6 提交

- [ ] 提交：

```bash
git add apps/api/app/orchestrators/adapters \
  apps/api/tests/test_orchestrator_plugin_contracts.py
git commit -m "feat: wrap local XG orchestrator as observable plugin"
```

---

## 5. Task 4：新增 fake Dify 插件适配器

**Files:**

- Create: `apps/api/app/orchestrators/adapters/dify_workflow_plugin.py`
- Modify: `apps/api/tests/test_orchestrator_plugin_contracts.py`

### 5.1 写失败测试

- [ ] 在 `apps/api/tests/test_orchestrator_plugin_contracts.py` 增加：

```python
from app.orchestrators.adapters.dify_workflow_plugin import DifyWorkflowOrchestratorPluginAdapter


def test_dify_workflow_plugin_returns_limited_observability_result() -> None:
    registry = OrchestratorPluginRegistry()
    manifest = registry.require("xg-dify-workflow-orchestrator")
    adapter = DifyWorkflowOrchestratorPluginAdapter(manifest=manifest)
    request = OrchestratorRunRequest(
        contract_version="xg-observable-orchestrator-contract@1",
        session={
            "session_id": "ra-001",
            "topic": "空域运算软件需求规格探索",
            "template_id": "81433号",
            "knowledge_package_id": "airspace-domain-demo",
            "orchestrator_id": "xg-dify-workflow-orchestrator",
            "provider_id": "mock",
            "model": "mock-requirement-analysis-v1",
            "write_policy": "patch_suggestion_only",
        },
        turn={
            "turn_id": "turn-0001",
            "turn_index": 1,
            "user_input": "这个系统叫空域运算软件",
            "normalized_input": {"input_type": "free_text", "semantic": "这个系统叫空域运算软件"},
            "previous_interaction": {"type": "none"},
            "input_relation": {"relation": "none"},
        },
        template={"template_id": "81433号", "format": "markdown", "content": "# 需求规格说明\n", "parsed_structure": {}},
        document_context={
            "working_document": {"document_id": "lab-working-document", "blocks": []},
            "active_spec_node": {"node_id": "SPEC-REQ-1.1", "target_section": "1 总则 / 编写目的"},
            "spec_tree": [],
            "confirmed_facts": [],
            "open_questions": [],
            "patches": [],
            "history_summary": "",
        },
        execution_options={"expected_output": "full_document", "observability_required": "limited", "streaming_enabled": False},
    )

    result = adapter.run(request)

    assert result.plugin["plugin_id"] == "xg-dify-workflow-orchestrator"
    assert result.plugin["observability_level"] == "limited"
    assert "空域运算软件" in result.final_output["filled_document_text"]
    assert result.final_output["document_patch"] == []
    assert result.process_output["stage_audits"] == []
    assert result.raw_output["raw_workflow_trace"]["fake"] is True
```

### 5.2 运行失败测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py::test_dify_workflow_plugin_returns_limited_observability_result -q
```

预期：

```text
FAIL: ModuleNotFoundError
```

### 5.3 实现 fake Dify 适配器

- [ ] 新增 `apps/api/app/orchestrators/adapters/dify_workflow_plugin.py`：

```python
from __future__ import annotations

from app.orchestrators.plugin_contracts import OrchestratorPluginManifest, OrchestratorRunRequest, OrchestratorRunResult


class DifyWorkflowOrchestratorPluginAdapter:
    def __init__(self, *, manifest: OrchestratorPluginManifest) -> None:
        self.manifest = manifest

    def run(self, request: OrchestratorRunRequest) -> OrchestratorRunResult:
        semantic = str(request.turn.get("normalized_input", {}).get("semantic") or request.turn.get("user_input") or "")
        template_content = str(request.template.get("content") or "# 需求规格说明\n")
        filled_document_text = f"{template_content.rstrip()}\n\n## 本轮补充\n\n{semantic}\n"
        return OrchestratorRunResult(
            contract_version=request.contract_version,
            plugin={
                "plugin_id": self.manifest.plugin_id,
                "plugin_type": self.manifest.plugin_type,
                "observability_level": self.manifest.observability_level,
            },
            final_output={
                "filled_document_text": filled_document_text,
                "document_patch": [],
                "changed_sections": [],
                "completion_status": "partial",
                "confidence": "medium",
            },
            interaction_output={
                "assistant_message": "Dify workflow 预留插件已生成整篇正文草稿。",
                "next_question": "请继续补充下一项需求规格信息。",
                "quick_options": [],
                "suggested_focus": {},
            },
            process_output={
                "stage_results": [],
                "stage_audits": [],
                "decision_trace": [],
                "provider_logs": [],
                "review_after_apply_result": {},
                "annotations": ["该结果来自 fake Dify workflow 插件，仅验证有限观测合同。"],
                "risks": [],
            },
            state_output={
                "confirmed_facts_delta": [semantic] if semantic else [],
                "open_questions_delta": ["请继续补充下一项需求规格信息。"],
                "spec_tree_update": {},
                "working_document_update": {},
                "turn_path_update": {},
            },
            raw_output={
                "raw_plugin_response": {},
                "raw_model_response": {},
                "raw_workflow_trace": {
                    "fake": True,
                    "workflow_id": "fake-xg-dify-workflow",
                    "run_id": f"fake-{request.turn.get('turn_id', 'turn')}",
                },
            },
        )
```

### 5.4 运行测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py -q
```

预期：

```text
5 passed
```

### 5.5 提交

- [ ] 提交：

```bash
git add apps/api/app/orchestrators/adapters/dify_workflow_plugin.py \
  apps/api/tests/test_orchestrator_plugin_contracts.py
git commit -m "feat: add fake Dify workflow orchestrator plugin"
```

---

## 6. Task 5：让 TurnEngine 构造插件请求并记录插件输出

**Files:**

- Modify: `apps/api/app/requirement_analysis/turn_engine.py`
- Modify: `apps/api/app/requirement_analysis/session_service.py`
- Modify: `apps/api/tests/test_requirement_analysis_api.py`

### 6.1 写失败测试

- [ ] 在 `apps/api/tests/test_requirement_analysis_api.py` 中给 `test_requirement_analysis_lab_runs_xg_strong_rule_orchestrator_package` 增加断言：

```python
    assert payload["turn"]["orchestrator_plugin"]["plugin_id"] == "xg-local-strong-rule-orchestrator"
    assert payload["turn"]["orchestrator_plugin"]["observability_level"] == "full"
    assert payload["turn"]["raw_plugin_response"]["plugin"]["plugin_id"] == "xg-local-strong-rule-orchestrator"
```

- [ ] 新增 fake Dify API 测试：

```python
def test_requirement_analysis_lab_can_run_fake_dify_plugin_with_limited_observability() -> None:
    client = TestClient(create_app())

    created = client.post(
        "/api/requirement-analysis/sessions",
        json={
            "topic": "空域运算软件需求规格探索",
            "orchestrator_id": "xg-dify-workflow-orchestrator",
            "provider_id": "mock",
            "model": "mock-requirement-analysis-v1",
            "template_id": "81433号",
            "knowledge_package_id": "airspace-domain-demo",
            "write_policy": "patch_suggestion_only",
        },
    )
    assert created.status_code == 200
    session = created.json()
    assert session["orchestrator"]["plugin_type"] == "dify_workflow"
    assert session["orchestrator"]["observability_level"] == "limited"

    turn = client.post(
        f"/api/requirement-analysis/sessions/{session['session_id']}/turns",
        json={"user_input": "这个系统叫空域运算软件，主要解决空域计算分析需求"},
    )
    assert turn.status_code == 200
    payload = turn.json()
    assert payload["turn"]["orchestrator_plugin"]["plugin_id"] == "xg-dify-workflow-orchestrator"
    assert payload["turn"]["orchestrator_plugin"]["observability_level"] == "limited"
    assert payload["turn"]["stage_audits"] == []
    assert payload["turn"]["raw_plugin_response"]["raw_output"]["raw_workflow_trace"]["fake"] is True
    assert "空域运算软件" in payload["session"]["working_document"]["blocks"][0]["text"]
```

### 6.2 运行失败测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_requirement_analysis_api.py::test_requirement_analysis_lab_runs_xg_strong_rule_orchestrator_package apps/api/tests/test_requirement_analysis_api.py::test_requirement_analysis_lab_can_run_fake_dify_plugin_with_limited_observability -q
```

预期：

```text
FAIL
```

### 6.3 实现迁移的第一步

- [ ] 在 `turn_engine.py` 中新增私有方法：

```python
    def _build_plugin_request(self, *, session: SessionSnapshot, context: TurnContext, turn_id: str, user_input: str, working_document: dict) -> OrchestratorRunRequest:
        return OrchestratorRunRequest(
            contract_version="xg-observable-orchestrator-contract@1",
            session={
                "session_id": session.session_id,
                "topic": session.topic,
                "template_id": session.template_id,
                "knowledge_package_id": session.knowledge_package_id,
                "orchestrator_id": session.orchestrator_id,
                "provider_id": session.provider_id,
                "model": session.model,
                "write_policy": session.write_policy,
            },
            turn={
                "turn_id": turn_id,
                "turn_index": context.turn_index,
                "user_input": user_input,
                "normalized_input": context.normalized_input,
                "previous_interaction": context.previous_interaction,
                "input_relation": context.input_relation,
            },
            template={
                "template_id": session.template_id,
                "format": "structured",
                "content": "",
                "parsed_structure": {"spec_tree": context.spec_tree},
            },
            document_context={
                "working_document": working_document,
                "active_spec_node": context.active_spec_node,
                "spec_tree": context.spec_tree,
                "confirmed_facts": context.facts,
                "open_questions": context.questions,
                "patches": context.patches,
                "history_summary": "",
            },
            execution_options={
                "expected_output": "both",
                "observability_required": "full",
                "streaming_enabled": False,
            },
        )
```

- [ ] 本阶段先不删除旧阶段链。仅将本地强规则和 fake Dify 作为插件入口：

```text
if orchestrator_id == xg-dify-workflow-orchestrator:
    run DifyWorkflowOrchestratorPluginAdapter
else:
    preserve existing stage chain
    attach orchestrator_plugin and raw_plugin_response fields for local orchestrators
```

### 6.4 fake Dify 结果落入 working_document

- [ ] 对 fake Dify 插件输出，使用 `WorkingDocumentService.apply_patches()` 的最小 patch 形式落入 `working_document`：

```python
document_patch = [
    {
        "plan_ref": "AP-DIFY-001",
        "operation": "append_or_update",
        "content": result.final_output["filled_document_text"],
        "write_policy": session.write_policy,
    }
]
target_anchor_plan = [
    {
        "plan_id": "AP-DIFY-001",
        "template_clause_id": context.active_spec_node.get("node_id", "SPEC-REQ-1.1").removeprefix("SPEC-"),
        "display_heading": context.active_spec_node.get("target_section", "需求规格说明"),
        "anchor_path": context.active_spec_node.get("node_id", "SPEC-REQ-1.1").removeprefix("SPEC-"),
    }
]
```

### 6.5 本地组织器输出挂载插件元数据

- [ ] 对现有本地组织器路径，在 `turn` 中补充：

```python
"orchestrator_plugin": {
    "plugin_id": normalized_plugin_id,
    "plugin_type": "local_package",
    "observability_level": "full",
},
"raw_plugin_response": {
    "plugin": {
        "plugin_id": normalized_plugin_id,
        "plugin_type": "local_package",
        "observability_level": "full",
    },
    "raw_output": {
        "raw_model_response": model_output["raw_model_response"],
    },
},
```

### 6.6 运行测试

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_requirement_analysis_api.py apps/api/tests/test_orchestrator_plugin_contracts.py -q
```

预期：

```text
PASS
```

### 6.7 提交

- [ ] 提交：

```bash
git add apps/api/app/requirement_analysis/turn_engine.py \
  apps/api/app/requirement_analysis/session_service.py \
  apps/api/tests/test_requirement_analysis_api.py
git commit -m "feat: route requirement analysis turns through orchestrator plugin metadata"
```

---

## 7. Task 6：前端展示插件能力和有限观测降级

**Files:**

- Modify: `apps/web/src/lib/api.ts`
- Modify: `apps/web/src/pages/RequirementAnalysisLabPage.tsx`
- Modify: `apps/web/src/test/RequirementAnalysisLabPage.test.tsx`

### 7.1 写失败测试

- [ ] 在 `RequirementAnalysisLabPage.test.tsx` 中增加断言：

```tsx
expect(await screen.findByText("完整观测")).toBeInTheDocument();
expect(screen.getByText("有限观测")).toBeInTheDocument();
expect(screen.getByText("Dify Workflow")).toBeInTheDocument();
```

- [ ] 对 fake Dify turn mock 增加：

```ts
turn: {
  ...buildTurnEnvelope().turn,
  orchestrator_plugin: {
    plugin_id: "xg-dify-workflow-orchestrator",
    plugin_type: "dify_workflow",
    observability_level: "limited",
  },
  stage_audits: [],
  raw_plugin_response: {
    raw_output: {
      raw_workflow_trace: {
        fake: true,
        workflow_id: "fake-xg-dify-workflow",
      },
    },
  },
}
```

并断言：

```tsx
expect(screen.getByText("该插件未提供阶段审计输出")).toBeInTheDocument();
expect(screen.getByText("fake-xg-dify-workflow")).toBeInTheDocument();
```

### 7.2 运行失败测试

- [ ] 执行：

```bash
corepack pnpm --dir apps/web exec vitest run src/test/RequirementAnalysisLabPage.test.tsx
```

预期：

```text
FAIL: Unable to find text 完整观测
```

### 7.3 实现前端能力展示

- [ ] 在 `RequirementAnalysisLabPage.tsx` 增加 helper：

```ts
function formatObservabilityLevel(level?: string) {
  if (level === "full") return "完整观测";
  if (level === "limited") return "有限观测";
  return "无过程观测";
}

function formatPluginType(type?: string) {
  if (type === "dify_workflow") return "Dify Workflow";
  if (type === "local_package") return "本地组织器";
  if (type === "remote_service") return "远端服务";
  return "组织器";
}
```

- [ ] 在组织器卡片中展示：

```tsx
<Tag>{formatPluginType(orchestrator.plugin_type)}</Tag>
<Tag color={orchestrator.observability_level === "full" ? "green" : "orange"}>
  {formatObservabilityLevel(orchestrator.observability_level)}
</Tag>
```

### 7.4 实现有限观测降级展示

- [ ] 在 `TurnTab` 或 `TurnView` 中，当 `stage_audits.length === 0` 且 `turn.orchestrator_plugin?.observability_level === "limited"` 时显示：

```tsx
<Alert
  type="info"
  showIcon
  message="该插件未提供阶段审计输出"
/>
```

- [ ] 如果 `turn.raw_plugin_response?.raw_output?.raw_workflow_trace?.workflow_id` 存在，展示 workflow id。

### 7.5 运行测试

- [ ] 执行：

```bash
corepack pnpm --dir apps/web exec vitest run src/test/RequirementAnalysisLabPage.test.tsx
```

预期：

```text
PASS
```

### 7.6 提交

- [ ] 提交：

```bash
git add apps/web/src/lib/api.ts \
  apps/web/src/pages/RequirementAnalysisLabPage.tsx \
  apps/web/src/test/RequirementAnalysisLabPage.test.tsx
git commit -m "feat: show orchestrator plugin observability in P2 lab"
```

---

## 8. Task 7：收缩 TurnEngine 固定阶段链职责

**Files:**

- Modify: `apps/api/app/requirement_analysis/turn_engine.py`
- Modify: `apps/api/app/orchestrators/adapters/local_xg_plugin.py`
- Modify: `apps/api/app/orchestrators/plugin_result_normalizer.py`
- Modify: `apps/api/tests/test_requirement_analysis_api.py`
- Modify: `apps/api/tests/test_orchestrator_plugin_contracts.py`

### 8.1 目标

本任务将 `TurnEngine` 的结构从：

```text
直接 plan stages / run stages / reduce stages
```

收缩为：

```text
build plugin request
invoke plugin
normalize result
persist state
```

但允许为了降低风险，保留本地插件内部复用当前 stage planner / executor / reducer 的实现。

### 8.2 写结构性测试

- [ ] 在 `test_requirement_analysis_api.py` 增加断言：

```python
assert payload["turn"]["raw_plugin_response"]["contract_version"] == "xg-observable-orchestrator-contract@1"
assert payload["turn"]["orchestrator_plugin"]["plugin_type"] == "local_package"
```

- [ ] 在 `test_orchestrator_plugin_contracts.py` 增加断言本地启发式插件返回：

```python
assert result.process_output["stage_audits"]
assert result.process_output["provider_logs"] or result.plugin["observability_level"] == "full"
```

### 8.3 实现 `plugin_result_normalizer`

- [ ] 新增 `apps/api/app/orchestrators/plugin_result_normalizer.py`，职责：

1. 将 `OrchestratorRunResult.final_output.document_patch` 转成当前 `model_output["document_patch"]`。
2. 将 `interaction_output` 转成当前 `assistant_message`、`next_question`、`quick_options`。
3. 将 `process_output` 转成 `stage_audits`、`provider_logs`、`decision_trace`。

首版可以只实现当前测试需要的字段，不做泛化。

### 8.4 迁移本地启发式插件

- [ ] 将当前 `turn_engine.py` 中的阶段执行逻辑逐步移动到 `LocalXGOrchestratorPluginAdapter`。

迁移边界：

1. `TurnEngine` 仍保留 working document 权威写入和会话 state patch。
2. 插件内部负责 intent/write/review/next 的执行和过程输出。
3. 插件返回的 `process_output.stage_audits` 应与当前 `turn.stage_audits` 等价。

### 8.5 运行全量 P2 后端回归

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py \
  apps/api/tests/test_orchestrator_runtime.py \
  apps/api/tests/test_requirement_analysis_api.py \
  apps/api/tests/test_requirement_analysis_session_service.py \
  apps/api/tests/test_requirement_analysis_modules.py -q
```

预期：

```text
PASS
```

### 8.6 提交

- [ ] 提交：

```bash
git add apps/api/app/requirement_analysis/turn_engine.py \
  apps/api/app/orchestrators/adapters/local_xg_plugin.py \
  apps/api/app/orchestrators/plugin_result_normalizer.py \
  apps/api/tests/test_requirement_analysis_api.py \
  apps/api/tests/test_orchestrator_plugin_contracts.py
git commit -m "refactor: make TurnEngine consume observable orchestrator results"
```

---

## 9. Task 8：正式文档和 WBS 入口更新

**Files:**

- Modify: `DOC/CODEX_DOC/04_研制计划/02-WBS-P2-需求分析系统-研制计划.md`
- Modify: `DOC/CODEX_DOC/06_测试文档/03_机测记录/`

### 9.1 更新 P2 WBS 入口

- [ ] 在 `DOC/CODEX_DOC/04_研制计划/02-WBS-P2-需求分析系统-研制计划.md` 中增加本计划入口：

```markdown
### P2 Lab 可观测组织器插件解耦升级

- 计划文档：`DOC/CODEX_DOC/04_研制计划/02.02-WBS-P2-可观测组织器插件解耦升级-研制计划.md`
- 设计依据：`DOC/CODEX_DOC/02_设计说明/P2_需求分析系统/P2-需求分析系统设计-260506-可观测组织器插件解耦补充.md`
- 分支：`feat/p2-validation-workflow`
- 范围：只处理 P2 Lab 组织器插件宿主、合同、可观测输出和 Dify 预留适配。
```

### 9.2 写机测记录

- [ ] 新增机测记录：

```text
DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05-06-P2可观测组织器插件解耦升级自检记录.md
```

记录内容包括：

1. 当前分支。
2. 涉及文件。
3. 后端 pytest 命令和结果。
4. 前端 vitest 命令和结果。
5. `Dify` 仍为 fake 插件、未真实接入的说明。

### 9.3 提交

- [ ] 提交：

```bash
git add DOC/CODEX_DOC/04_研制计划/02-WBS-P2-需求分析系统-研制计划.md \
  DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05-06-P2可观测组织器插件解耦升级自检记录.md
git commit -m "docs: record P2 orchestrator plugin upgrade verification"
```

---

## 10. Task 9：最终验证

**Files:**

- No direct file changes.

### 10.1 后端验证

- [ ] 执行：

```bash
uv run pytest apps/api/tests/test_orchestrator_plugin_contracts.py \
  apps/api/tests/test_orchestrator_runtime.py \
  apps/api/tests/test_requirement_analysis_api.py \
  apps/api/tests/test_requirement_analysis_session_service.py \
  apps/api/tests/test_requirement_analysis_modules.py -q
```

预期：

```text
PASS
```

### 10.2 前端验证

- [ ] 执行：

```bash
corepack pnpm --dir apps/web exec vitest run src/test/RequirementAnalysisLabPage.test.tsx
```

预期：

```text
PASS
```

### 10.3 分支边界检查

- [ ] 执行：

```bash
git diff --name-only main...HEAD | rg "RequirementAuthoringPage|RequirementAuthoringAdminPage|requirement_authoring"
```

预期：

```text
无新增输出，或仅出现本分支已有历史文件；本升级不新增专家工作台职责。
```

### 10.4 提交最终状态

- [ ] 如果最终验证只更新机测记录，提交：

```bash
git add DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05-06-P2可观测组织器插件解耦升级自检记录.md
git commit -m "test: verify P2 observable orchestrator plugin upgrade"
```

---

## 11. 计划自检结果

### 11.1 设计覆盖

本计划覆盖正式补充设计中的以下要求：

1. 可观测组织器插件合同：Task 1。
2. 插件能力声明：Task 1、Task 2、Task 6。
3. 当前自研组织器插件化：Task 3、Task 7。
4. `Dify` 有限观测预留：Task 4、Task 5、Task 6。
5. `TurnEngine` 职责收缩：Task 5、Task 7。
6. 前端观测降级展示：Task 6。
7. 当前分支与并行分支边界：Task 8、Task 9。

### 11.2 明确不覆盖

以下内容不在本实施计划中：

1. 真实 `Dify` API 接入。
2. Word 模板解析。
3. 专家工作台重构。
4. 主设计说明正文合并。

### 11.3 验收结论

完成本计划后，`feat/p2-validation-workflow` 应达到：

```text
P2 Lab 可以通过统一插件合同选择本地完整观测组织器或 fake Dify 有限观测组织器；
当前自研组织器过程输出不退化；
TurnEngine 开始从固定阶段编排者转为插件宿主。
```
