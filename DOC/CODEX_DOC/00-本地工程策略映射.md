# CodeFactoryV2 本地工程策略映射

本文件用于把 `CodexProject` 的全局工程策略映射为 `CodeFactoryV2` 的本地执行规则。

## 1. 本地路径判定

### 1.1 仓库主目录

- `CodeFactoryV2`

### 1.2 用户检查目录

- 当前项目默认与仓库主目录一致
- 用户查看页面、阅读文档、做人工验收时，应以当前仓库目录为准

### 1.3 正式文档根

- `DOC/CODEX_DOC/`

这是当前项目唯一的正式本地文档根。

## 2. 三层协同映射

### 2.1 正式文档层

路径：

- `DOC/CODEX_DOC/`

职责：

- 沉淀长期有效的正式设计
- 沉淀 WBS 总纲和阶段计划
- 沉淀正式测试文档和过程文档
- 对外提供稳定引用路径
- 作为用户唯一默认阅读和确认的终稿文档根

### 2.2 superpowers 工作层

路径：

- `docs/superpowers/`
- `.superpowers/`

职责：

- 承接 brainstorm、方案草案、执行计划、issue 镜像
- 支持快速推演和阶段性迭代

约束：

- `docs/superpowers/` 不是单一权威源
- `.superpowers/brainstorm/` 一类内容属于临时工作产物，不应被引用为正式规范
- 被用户确认、被项目采纳的结论，必须回写到 `DOC/CODEX_DOC/`
- `docs/superpowers/` 默认按时间组织，不应被要求作为用户按逻辑查阅的阅读路径
- 只建立映射关系、不迁移正文，不算正式归档完成

### 2.3 远端协作层

载体：

- GitHub Project
- GitHub Issues
- Pull Requests

职责：

- 排程
- WBS 树投影
- 执行契约
- 评审反馈

约束：

- 远端路径默认引用仓库相对路径
- 正式文档优先引用 `DOC/CODEX_DOC/`
- `docs/superpowers/` 路径可作为工作过程证据引用，但不替代正式节点文档

## 3. 文档更新规则

### 3.1 正式设计更新触发条件

出现以下任一情况时，必须更新 `DOC/CODEX_DOC/`：

- 一级或二级 WBS 节点结构发生变化
- 平台模块边界、输入输出契约发生变化
- 用户明确确认某一份 `superpowers` 设计草案成为正式方案
- 新增阶段级功能说明、长期有效规范或正式验收口径
- 本轮工作已经实际采用 `superpowers` 草稿中的核心设计细节作为实现依据

### 3.2 superpowers 文档的使用方式

允许继续使用 `superpowers` 的典型场景：

- 方案探索
- 设计草案
- 局部实现计划
- issue tree mirror
- 分支或并行线的工作过程文档

但后续开发必须遵守：

- 先有工作文档，不等于已经有正式规范
- 正式规范必须回写 `DOC/CODEX_DOC/`
- 如果 `DOC/CODEX_DOC/` 与 `docs/superpowers/` 冲突，以 `DOC/CODEX_DOC/` 为准
- 不能要求用户到 `docs/superpowers/` 中按时间文件名寻找设计结论
- `superpowers` 中形成的核心设计细节，必须按逻辑结构整理进根目录正式文档，而不是只保留链接或映射

核心设计细节至少包括：

- 节点拆解
- 模块边界
- 数据/对象/状态模型
- 接口与输入输出约束
- 关键处理策略
- 页面级关键交互
- 风险、验收与迁移口径

## 4. WBS 与执行契约映射

- WBS 权威树来源：GitHub parent/sub-issue
- 本地 WBS 正式镜像：`DOC/CODEX_DOC/03_研制计划/`
- 阶段级、节点级长期说明：`DOC/CODEX_DOC/02_设计说明/`
- 过程性计划、issue tree mirror：`docs/superpowers/plans/`、`docs/superpowers/issues/`

## 5. 分支与 worktree 策略

### 5.1 默认策略

- 默认在 `main` 推进
- 默认不使用 `git worktree`
- 默认从仓库主目录启动服务和进行用户验收

### 5.2 例外策略

只有在用户明确要求并行开发时，才允许建立独立分支或受控 worktree。

即使启用了并行线，也必须遵守：

- 用户可见成果先回到主工作区
- 正式文档先回写 `DOC/CODEX_DOC/`
- 不得长期让主线和并行线各自维护一套不同的正式规范

## 6. 当前项目特别说明

- `P1` 已形成可运行的知识库最小闭环，但仍在抽取质量与展示逻辑上持续优化
- `P2` 已形成设计方向与模拟输入台文档，但正式独立执行契约仍需持续补齐
- `P3`、`P4` 已形成较完整的 `superpowers` 设计/计划/issue 镜像
- `P6` 已开始建立门户层文档，但需要纳入正式 WBS 体系

## 7. 后续执行口径

从本文件生效开始，本项目采用如下口径：

- `superpowers` 负责“快推演、快分解、快起草”
- `DOC/CODEX_DOC` 负责“正式落版、长期追踪、稳定引用、用户阅读”
- 后续方案开发以二者协同为准，不再让单一目录同时承担全部职责
