# P6 语义画布端口用户对齐 v16 交接

时间：2026-04-30 00:44:26

## 1. 本轮目标

响应用户对 v15 的批注，创建下一版原型 v16，不覆盖 v15。

## 2. 用户批注

1. 卡片输入输出不应做成夸张标签，只要小圆圈或可吸附节点。
2. 顶部用户应该整齐对齐，不应错落。
3. 必须认真参考用户点名认可的浅色卡片参考图；该图在主目录中。

## 3. 已完成产物

新增原型包：

`DOC/CODEX_DOC/08_原型与附图/2026-04-30-003923-CodeFactoryV2-P6语义画布端口用户对齐修正版-v16/`

主要文件：

- `README.md`
- `REVIEW_RESPONSE.md`
- `source/v16-semantic-canvas.html`
- `source/v16-semantic-canvas.css`
- `01-1920x1080-五阶段语义画布总览态.png`
- `02-1920x1080-P3选中抽屉态.png`
- `03-1920x1080-语义流与图例说明态.png`
- `original/用户点名浅色卡片参考图.png`
- `original/v15-总览态问题对照.png`

新增自检记录：

- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-004426-P6语义画布端口用户对齐v16自检记录.md`

## 4. 设计处理

1. 端口从大标签改为 `20 x 20` 小圆形吸附节点。
2. 端口文字保留在 DOM 中作为辅助语义，但视觉上隐藏，不再展示为标签。
3. 顶部用户积木取消错落位移，统一水平居中对齐。
4. 卡片边框、阴影和浅色分区向用户点名浅色参考图靠拢。
5. 主链语义不变：`P1 -> P2 -> P3 -> P4 -> P5 -> 交付目录`。

## 5. 验证状态

已完成：

- Playwright 生成三张 `1920 x 1080` 评审图。
- `file` 检查确认三张 PNG 均为 `1920 x 1080`。
- DOM 检查确认端口尺寸为 `20 x 20`，用户行纵向偏差为 `0`。
- 人工查看三张图，确认端口不再是大标签，用户积木已对齐。

## 6. 当前状态

`待用户确认`。

用户确认前，不进入运行页面开发，不把 v16 写成已批准实现标准。

