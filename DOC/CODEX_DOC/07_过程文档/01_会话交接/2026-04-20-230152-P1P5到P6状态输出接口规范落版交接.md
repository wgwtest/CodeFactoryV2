# P1P5 到 P6 状态输出接口规范落版交接

## 1. 本轮目标

把 `P1 ~ P5 -> P6` 的共享状态输出标准固化到正式文档中的明确位置，并让 `P1 ~ P5` 与 `P6.2` 都显式依赖这份规范，供后续各阶段补接口时直接对照执行。

## 2. 本轮已完成内容

- 新增共享规范：
  - `DOC/CODEX_DOC/03_规范与流程/01_数据规范/01-P1-P5到P6状态输出接口规范.md`
- 更新 `P6.2` 正式设计：
  - `DOC/CODEX_DOC/02_设计说明/12-P6.2-跨阶段只读集成与状态投影设计.md`
  - 新增共享规范引用
  - 明确共享规范与消费侧设计的职责分工
  - 把查询字段口径收敛为 `primary_status / entry_route / entry_available / health_level`
- 更新 `P1 ~ P5` 正式设计：
  - `DOC/CODEX_DOC/02_设计说明/02-P1-业务知识库设计.md`
  - `DOC/CODEX_DOC/02_设计说明/03-P2-需求分析系统设计.md`
  - `DOC/CODEX_DOC/02_设计说明/04-P3-软件设计系统设计.md`
  - `DOC/CODEX_DOC/02_设计说明/05-P4-工具仓库设计.md`
  - `DOC/CODEX_DOC/02_设计说明/07-P5-软件构建系统设计.md`
  - 均已在“正式文档关系”中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- 新增本轮验收与自测证据：
  - `DOC/CODEX_DOC/06_测试文档/01_自测报告/2026-04-20-230152-P1P5到P6状态输出接口规范落版自测.md`
  - `DOC/CODEX_DOC/06_测试文档/05_验收清单/2026-04-20-230152-P1P5到P6状态输出接口规范落版验收清单.md`

## 3. 本轮核心结论

- 共享规范应放在 `DOC/CODEX_DOC/03_规范与流程/01_数据规范/`，作为跨阶段共享事实源，而不是只写在 `P6.2` 单文件里。
- 阶段状态事实的来源、计算和刷新策略应继续由 `P1 ~ P5` 各阶段正式设计负责说明。
- `P6.2` 负责的是消费侧统一映射、新鲜度判断和降级聚合，不负责替各阶段定义事实来源。
- 如果后续新增共享字段或统一枚举，应先回写共享规范，再同步调整 `P1 ~ P5` 和 `P6.2`。

## 4. 已执行校验

执行过以下校验：

```bash
rg -n "01-P1-P5到P6状态输出接口规范" DOC/CODEX_DOC/02_设计说明 DOC/CODEX_DOC/04_研发文档 -g '*.md'
git diff --check
```

校验结果：

- 共享规范已被 `P1 ~ P5` 与 `P6.2` 正式引用。
- 当前补丁未发现格式错误。

## 5. 后续动作

1. 后续若 `P1 ~ P5` 各阶段开始补真实接口，应按本规范逐步把真实字段来源落实到各阶段正式设计或专题设计中。
2. 若需要提升可发现性，可再考虑把该规范补充到 `10-P6` 总体设计或 `DOC/CODEX_DOC/README.md` 的导航中。
3. 若接口实现阶段发现共享字段还不够，再回写本规范统一扩展，而不要在页面层私自加字段。

## 6. 当前状态

- 当前状态：待人工验收
