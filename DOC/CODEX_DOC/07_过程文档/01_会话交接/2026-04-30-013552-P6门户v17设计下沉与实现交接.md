# P6 门户 v17 设计下沉与实现交接

时间：2026-04-30 01:35:52

## 1. 本轮目标

在用户确认 v17 原型方向后，把已确认事实同步到正式设计文档，并按该事实源修正 `/portal` 当前实现。

## 2. 已完成

1. 正式文档同步：
   - `P6.1-首屏观察门户设计.md`：补充 `P1` 输入例外、`P3 -> P5` 设计基线流、流动对象表达、验收口径。
   - `P6.2-跨阶段只读集成与状态投影设计.md`：补充 P6 投影合同中的 `P3 -> P5` 辅助流，明确 `P1` 外部知识入库由队列表达。
   - `P6.3-设计语言与前端展示基线设计.md`：补充小圆点端口、顶部用户对齐、P1 队列输入、P3-P5 设计基线流。
   - `01-P1-P5到P6状态输出接口规范.md`：同步展示合同和端口规则。
2. API / 模拟投影：
   - `P1` mock 合同去掉左侧外部输入端口。
   - 增加 `P3 -> P5` 设计基线流。
   - 增加 `P5 -> 交付目录` 终端输出流。
   - `artifact_list` 不再驱动门户画布产物卡片。
3. 前端 `/portal`：
   - 支持交付目录终端输出端点。
   - 端口改成小圆点 / pin，不显示大块端口文字标签。
   - 去掉可见固定世界边界面板。
   - 去掉门户内 `卡片配置` 实验柜入口。
   - 关系聚合忽略终端端点，避免把交付目录当成系统卡片。
4. 证据：
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-013552-P6门户v17设计下沉与实现自检记录.md`
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-013552-P6门户v17实现截图.png`

## 3. 验证结论

已通过：

- `uv run pytest apps/api/tests/test_p6_api.py -q`：`12 passed`
- `corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx`：`11 passed`
- P6 TypeScript 过滤检查：无 P6 相关错误
- Playwright 运行页 DOM 检查：`cards=5`、`artifacts=0`、`hasP3P5=1`、`hasTerminal=1`、`hasBoundary=0`、`hasConfig=0`、`externalText=0`

未通过 / 未关闭：

- 完整 `apps/web build` 仍被非 P6 既有 TypeScript 错误阻断，主要在 P3/P4/P5/toolHub 相关文件和既有测试类型处。本轮未改这些范围。

## 4. 运行地址

本轮验证使用：

- API：`http://127.0.0.1:8060`
- Web：`http://127.0.0.1:5183/portal`

注意：`8050` 上存在旧 API 实例，本轮未复用，避免截图读到旧投影。

## 5. 下一步建议

下一轮若继续推进，应优先做运行页人工验收批注；若通过，再考虑把右侧图例 / 场景控制进一步做成更低干扰的折叠态，而不是继续修改卡片和主链语义。
