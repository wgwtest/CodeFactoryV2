# P1 远端运行工作台归并交接

## 1. 本轮目标

检查远端是否存在新的 `P1` 大改动，并在严谨检查后把远端分支 `origin/codex/archive-runtime-policy-workbench` 归并到当前主工作区。

## 2. 本轮已完成

- 已执行远端抓取：
  - `git fetch --all --prune --tags`
- 已确认新增远端分支：
  - `origin/codex/archive-runtime-policy-workbench`
- 已确认 `origin/main` 未更新：
  - `origin/main` 仍为 `ad34ae7`
- 已完成归并前检查：
  - 分支基线：`9877aea`
  - 远端提交：`3db51ae archive runtime workbench and policy pipeline`
  - 远端变更规模：67 个文件，约 30187 行新增、1109 行删除
  - 预演冲突：3 个前端文件
- 已保存归并前本地已跟踪改动：
  - `stash@{0}: On main: pre-p1-remote-merge tracked local changes`
  - `/tmp/codefactory-merge-safety/pre-p1-merge-tracked.diff`
- 已执行非提交归并：
  - `git merge --no-commit --no-ff origin/codex/archive-runtime-policy-workbench`
- 已解决冲突：
  - `apps/web/src/App.tsx`
  - `apps/web/src/test/AppRoutes.test.tsx`
  - `apps/web/vite.config.ts`
- 已恢复本地 `P6` 已跟踪改动：
  - `git stash apply stash@{0}`
- 已补正式设计摘要：
  - `DOC/CODEX_DOC/02_设计说明/P1_业务知识库/P1-业务知识库设计.md`
- 已补自测记录：
  - `DOC/CODEX_DOC/06_测试文档/01_自测报告/2026-04-25-092619-P1远端运行工作台归并自测.md`

## 3. 当前工作区状态

- 当前处于 merge 已解冲突、未提交状态。
- `MERGE_HEAD` 指向：
  - `3db51ae46bbed63f1c45ea0fca1e89ada30b36ca`
- 归并改动已暂存。
- 归并前已有 `P6` 本地改动已恢复为未暂存状态。
- 未执行 `git commit`，因为本轮尚未收到明确提交指令。

## 4. 自测结果

- `git diff --check --cached`
  - 通过
- `git diff --check`
  - 通过
- `uv run pytest apps/api/tests/test_archive_policy_config_api.py apps/api/tests/test_archive_document_runtime_api.py apps/api/tests/test_archive_registry_api.py apps/api/tests/test_archive_incremental_rebuild.py apps/api/tests/test_formal_archive_extraction_policy.py -q`
  - 通过：`50 passed`
- `corepack pnpm --dir apps/web exec vitest run src/test/AppRoutes.test.tsx src/test/DocumentIntakePage.test.tsx src/test/ArchiveManagementPolicy.test.tsx src/test/ArchiveManagementPage.test.tsx src/test/DocumentsPage.test.tsx src/test/archiveKnowledge.test.ts`
  - 通过：`6 passed`，`26 passed | 4 skipped`

## 5. 风险与后续动作

1. 当前远端大改动已归并到本地工作区，但尚未提交。
2. `stash@{0}` 仍保留为安全备份；确认无需回退后可删除。
3. 全量 Vitest 曾因命令形式误触发，并暴露 `P6PortalPage.test.tsx` 中一个既有 `P6 -> P5` 导航断言失败；该问题不属于本轮 `P1` 归并范围。
4. 远端分支新增了大量 `docs/superpowers/specs/` 过程稿，本轮仅把核心运行态与策略管线语义摘要同步到 `P1` 正式设计；若后续要长期承接该链路，应继续拆出更细正式专题设计或验收大纲。

## 6. 当前状态

- 当前状态：已自测，待人工确认是否提交归并。
