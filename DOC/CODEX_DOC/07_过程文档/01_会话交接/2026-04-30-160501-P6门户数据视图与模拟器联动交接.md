# P6 门户数据视图与模拟器联动交接

时间：2026-04-30 16:05:01

## 1. 本轮目标

在已确认的 P6 语义画布基础上，补充同源精确数据视图和统一模拟发生器联动能力，不改写 `/portal` 主画布定位。

## 2. 已完成

1. 后端新增 `/api/p6/portal-data`：
   - 返回五阶段总表、场景摘要、跨阶段流量序列、阶段下钻区。
   - 基线场景无历史样本时返回空趋势，不生成假趋势。
   - 模拟器每次提交五阶段合同后写入内存历史样本。
2. 前端新增 `/portal-data`：
   - 默认展示 `P1 ~ P5` 五阶段合同总表。
   - 展示六条设计约束内的流：`P1->P2`、`P2->P3`、`P3->P4`、`P3->P5`、`P4->P5`、`P5->交付目录`。
   - 点击阶段行切换下钻区。
3. `/xx-p6-sim` 补正：
   - P1 不再生成外部知识输入端口，外部知识入库继续由队列表达。
   - P3 输出补充 `设计基线 -> P5`。
   - 提交成功后提供 `打开 P6 门户` 与 `打开图表视图`。
4. `/portal` 增加低干扰入口：
   - `图表视图`
   - `模拟发生器`

## 3. 当前运行地址

- Web：`http://127.0.0.1:5184/portal-data?scenario=baseline`
- API：`http://127.0.0.1:8062`

## 4. 验证结果

已通过：

- `uv run pytest apps/api/tests/test_p6_api.py -q`：`14 passed`
- `corepack pnpm --dir apps/web test src/test/P6PortalDataPage.test.tsx src/test/P6SimulatorPage.test.tsx src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts`：`18 passed`
- `corepack pnpm --dir apps/web exec tsc --noEmit --pretty false`：退出码 `0`
- `corepack pnpm --dir apps/web build`：退出码 `0`
- `git diff --check`：无输出

截图证据：

- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-160501-P6门户数据视图运行截图-1920x1080.png`

## 5. 未关闭事项

当前状态为已自测，待人工验收。
