# P6 门户数据驱动连线交接

时间：2026-04-30 13:18:39

## 1. 本轮目标

修正 `/portal` 语义流表达：连线不再只显示静态通道标签或装饰性流动点，而是由端口运行数据生成真实 payload 流动对象。

## 2. 已完成

1. 前端实现：
   - `apps/web/src/components/p6/P6BlueprintCanvas.tsx`
     - 新增 flow payload 解析和数量派生逻辑。
     - 从起点输出端口 `current_rate` 推导 payload 数量。
     - 以 SVG `animateMotion` 渲染沿路径运动的 payload。
     - 移除主画面静态 `p6-flow-label-*` 线名渲染。
   - `apps/web/src/pages/P6PortalPage.css`
     - 新增 `p6-portal-flow-payload*` 样式。
     - 为 `知识 / 规格 / 工单 / 基线 / 工具 / 目录` payload 提供语义色。
2. 测试：
   - `apps/web/src/test/P6PortalPage.test.tsx`
     - 补充数据驱动 payload 流断言。
     - 确认 `P1 -> P2` 为 12 个“知识”payload。
     - 确认语义视图切换到投影聚合后 payload 层消失。
3. 文档同步：
   - `P6.1-首屏观察门户设计.md`
   - `P6.2-跨阶段只读集成与状态投影设计.md`
   - `P6.3-设计语言与前端展示基线设计.md`
   - 明确 `flow.label` 不作为主画面默认静态线名，主画面显示 `payload_label + current_rate` 派生的运动对象。
4. 运行截图与自检记录：
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-131839-P6门户数据驱动连线运行截图-1920x1080.png`
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-131839-P6门户数据驱动连线运行截图-1365x768.png`
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-131839-P6门户数据驱动连线自检记录.md`

## 3. 当前运行地址

- API：`http://127.0.0.1:8060`
- Web：`http://127.0.0.1:5183/portal?scenario=baseline&v=flow-payload`

## 4. 验证结果

已通过：

- `uv run pytest apps/api/tests/test_p6_api.py -q`：`12 passed`
- `corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/P6SimulatorPage.test.tsx`：`16 passed`
- `git diff --check`：无输出
- P6 TypeScript 过滤检查：无 P6 / portal 相关错误输出
- Playwright 运行时位置检查：`p6-flow-payload-p1-p2-0` 两次 bounding box 坐标不同，`moved=true`

未通过但属于既有范围：

- `corepack pnpm --dir apps/web build` 仍失败，错误集中在 P3 / P4 / P5 / toolHub / 既有测试类型，不是本轮 P6 门户连线改动引入。

## 5. 未关闭事项

1. 当前状态为已自测，待人工验收。
2. 若后续真实数据源出现单条线超高频率，需要继续补充密度降级的可见提示；本轮 baseline 数据均未触发上限。
