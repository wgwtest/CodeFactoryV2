# P5 构建工作台输入绑定与模拟链路交接

## 1. 本轮目标

把 `P5` 工作台补到“可显式绑定输入、可确认反馈、可接收 `XX-P3 / XX-P4` 模拟链路”的状态，并收敛为可单独提交的代码包。

## 2. 本轮已完成内容

- 后端 `software_build` 路由补入：
  - 设计输入列表
  - 供给输入列表
  - 设计输入模拟创建
  - 供给输入模拟创建
  - 输入绑定确认
  - 模块级绑定更新
  - 反馈任务评审
  - 测试态清理接口
- `software_build` 服务与模型补入：
  - 设计输入源 / 供给输入源
  - 输入绑定对象
  - 节点级工具绑定决策
  - 反馈任务确认 / 忽略
  - 模拟输入创建与 `P5` 主单联动
- 前端补入：
  - `P5` 工作台输入绑定区
  - 缺口模块工具绑定按钮
  - 反馈任务确认 / 忽略按钮
  - `XXP3DocSimPage`
  - `XXP4SupplySimPage`
  - 对应路由与测试

## 3. 验证

- 验收入口：
  - `DOC/CODEX_DOC/06_测试文档/02_验收入口/99_历史归档/SETTING/2026-04-21-005612-P5构建工作台输入绑定与模拟链路验收入口.md`
- 机测记录：
  - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-21-005612-P5构建工作台输入绑定与模拟链路机测记录.md`

## 4. 当前状态

- 当前状态：已自测，待人工验收

## 5. 后续建议

1. 本提交可以作为 `P5` 代码主提交单独推送。
2. 推完后再单独处理 `DOC/CODEX_DOC/02_设计说明` 目录重构与 `JB_DOC` 同步，不要和 `P5` 代码混成一笔。
