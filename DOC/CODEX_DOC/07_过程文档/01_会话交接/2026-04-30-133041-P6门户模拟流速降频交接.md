# P6 门户模拟流速降频交接

时间：2026-04-30 13:30:41

## 1. 本轮目标

在数据驱动 payload 连线已成立的基础上，降低模拟源流速，解决画布上 `知识 / 规格 / 工单 / 基线 / 工具 / 目录` payload 过密的问题。

## 2. 已完成

1. 降低 mock 展示合同流速：
   - `P1 -> P2`：`5 条/小时`
   - `P2 -> P3`：`4 份/小时`
   - `P3 -> P4`：`5 包/小时`
   - `P3 -> P5`：`3 份/小时`
   - `P4 -> P5`：`4 项/小时`
   - `P5 -> 交付目录`：`2 个/日`
2. 同步前端测试 fixture：
   - `apps/web/src/test/p6TestData.ts`
3. 同步前端断言：
   - `apps/web/src/test/P6PortalPage.test.tsx`
   - 断言低频 payload 数量分别为 `5 / 4 / 5 / 3 / 4 / 2`
4. 同步正式设计说明示例：
   - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
5. 重新截图并归档：
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-133041-P6门户模拟流速降频运行截图-1920x1080.png`
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-133041-P6门户模拟流速降频运行截图-1365x768.png`
6. 新增自检记录：
   - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-133041-P6门户模拟流速降频自检记录.md`

## 3. 当前运行地址

- Web：`http://127.0.0.1:5183/portal?scenario=baseline&v=low-flow-rate`
- API：`http://127.0.0.1:8060`

## 4. 当前运行进程

- Web：既有 Vite 进程，端口 `5183`
- API：已重启到最新代码，端口 `8060`

## 5. 验证结果

已通过：

- `uv run pytest apps/api/tests/test_p6_api.py -q`：`12 passed`
- `corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/P6SimulatorPage.test.tsx`：`16 passed`
- `git diff --check`：无输出
- P6 TypeScript 过滤检查：无 P6 / portal 相关错误输出
- 接口抽查：`/api/p6/portal-projection?source=mock&scenario=baseline` 返回低频 `current_rate`

未通过但属于既有范围：

- `corepack pnpm --dir apps/web build` 仍失败，错误集中在 P3 / P4 / P5 / toolHub / 既有测试类型，不是本轮 P6 门户流速降频改动引入。

## 6. 未关闭事项

当前状态为已自测，待人工验收。
