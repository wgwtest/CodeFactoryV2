# P4 后端服务化与运行时演进交接

## 1. 本轮完成内容

- 补齐 `P4.4` 当前规划范围内的后台落地
- 将 `tool_hub` 路由按端口拆为 `P3 Input / P5 Query / Operator / Internal Runtime`
- 新增独立 `ToolHubRuntimeWorker / ToolHubRuntimeCoordinator`
- 新增核心查询投影仓储，并让 `overview / tool_list / evolution_workspace` 改为显式持久化读取
- 为内部 runtime 新增：
  - `POST /api/tool-hub/internal-runtime/projections/refresh`
  - `POST /api/tool-hub/internal-runtime/cycles/run-once`
- 同步修正制造任务到期后的补领与完成逻辑
- 更新 P4 WBS、issue mirror、设计实现映射到最新状态

## 2. 已验证证据

- 机测记录：
  - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-19-021205-P4后端服务化与运行时演进机测记录.md`
- 验收入口：
  - `DOC/CODEX_DOC/06_测试文档/02_验收入口/99_历史归档/WBS/2026-04-19-021205-P4后端服务化与运行时演进验收入口.md`
- 自动化命令：
  - `uv run pytest apps/api/tests/test_tool_hub*.py -q`
  - 结果：`28 passed`

## 3. 当前状态判断

- `P4.1`：已完成
- `P4.2`：已自测，待人工验收
- `P4.3`：已自测，待人工验收
- `P4.4`：已自测，待人工验收

## 4. 后续建议

- 由人工执行一次 `XX-P4 / P3-sim / P5-sim` 联调验收
- 若验收通过，再同步远端 issue / project 的状态为对应验收态
- 后续若继续推进服务化，可优先补：
  - `dead_letter`
  - 专属 `projection queue`
  - 更细粒度的 `demand / delivery` 投影
  - 独立部署的 `p4 backend service`
