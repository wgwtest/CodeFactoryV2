# P6 合同模拟器与门户 v2 卡片接入交接

- 交接时间：2026-04-29 21:10
- 分支：`feat/p6-portal-platform-entry`
- 工作目录：`.worktrees/p6-portal-platform-entry`
- 状态：已自测，待人工验收
- 复核时间：2026-04-29 21:28 PST

## 1. 本轮完成内容

1. 后端补充 `P6DisplayExportContract.v2` 相关模型和模拟提交接口：
   - `POST /api/p6/simulator/contracts`
   - 模拟合同提交后生成 `simulator-latest` 场景。
2. 后端门户投影改为五个系统节点主链：
   - `P1 -> P2 -> P3 -> P4 -> P5`
   - 移除独立 `行业用户` 节点。
   - P5 输出端口固定为 `交付目录`，且为终端输出。
3. 前端新增 `/xx-p6-sim` 合同模拟器页面：
   - 发送五阶段 v2 展示合同。
   - 成功后提供 `/portal?scenario=simulator-latest` 入口。
4. P6 门户节点渲染更新：
   - 顶部方形用户积木展示当前接入用户。
   - 左右接口展示输入/输出连接对象。
   - 中段展示总体累计指标。
   - 下段展示实时输入/输出计数。
   - 底部挂载队列展示顺序前移的知识/对象队列。
   - P1 总体指标补正为知识库、已发布知识、领域、贡献者四项。
   - 模拟器合同左侧输入端口补正为外部知识、发布态知识、需求规格、模块工单包、工具供给等真实展示语义。
5. 默认门户布局更新为 1920x1080 世界内五节点同屏：
   - 上排：P2、P3、P5
   - 下排：P1、P4
6. 正式文档同步更新：
   - `DOC/CODEX_DOC/03_规范与流程/01_数据规范/01-P1-P5到P6状态输出接口规范.md`
   - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
   - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.2-跨阶段只读集成与状态投影设计.md`

## 2. 主要改动文件

- `apps/api/app/p6/models.py`
- `apps/api/app/p6/service.py`
- `apps/api/app/p6/mock_scenarios.py`
- `apps/api/app/api/routes/p6.py`
- `apps/api/tests/test_p6_api.py`
- `apps/web/src/lib/p6.ts`
- `apps/web/src/App.tsx`
- `apps/web/src/pages/P6SimulatorPage.tsx`
- `apps/web/src/pages/P6SimulatorPage.css`
- `apps/web/src/pages/P6PortalPage.tsx`
- `apps/web/src/pages/P6PortalPage.css`
- `apps/web/src/components/p6/P6BlueprintNode.tsx`
- `apps/web/src/components/p6/p6PortalData.ts`
- `apps/web/src/test/P6SimulatorPage.test.tsx`
- `apps/web/src/test/P6PortalPage.test.tsx`
- `apps/web/src/test/P6ObservationPage.test.tsx`
- `apps/web/src/test/p6TestData.ts`

## 3. 已验证命令

```bash
.venv/bin/pytest apps/api/tests/test_p6_api.py -q
```

通过：`12 passed`

```bash
corepack pnpm --dir apps/web exec vitest run src/test/P6SimulatorPage.test.tsx src/test/P6PortalPage.test.tsx src/test/P6ObservationPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/AppRoutes.test.tsx
```

通过：`25 tests passed`

```bash
git diff --check
```

通过：无输出。

运行时复核：

- worktree `.env.local` 指定 `VITE_API_PROXY_TARGET=http://127.0.0.1:8050`，因此 API 需启动在 `8050` 才能让浏览器 `/api` 代理成功。
- 已启动：`http://127.0.0.1:8050`
- 前端已启动：`http://127.0.0.1:5173`
- 截图证据：
  - `/tmp/p6-runtime/p6-simulator-submitted-1920.png`
  - `/tmp/p6-runtime/p6-portal-simulator-latest-1920.png`
- 1920×1080 复核结果：五节点同屏，无独立行业用户节点，`P1` 外部知识输入和贡献者指标可见，`P5` 输出为交付目录，`P2 -> P3` 与 `P4 -> P5` 端口未重叠。

## 4. 遗留风险

`corepack pnpm --dir apps/web build` 当前仍失败，失败集中在既有 P3/P4/P5 类型导出和 readonly 样例数组问题，不属于本轮 P6 接入改动。后续如要以完整 build 作为验收门，需要单独处理这些既有 TypeScript 问题。

## 5. 下轮建议

1. 人工打开 `/xx-p6-sim`，点击发送模拟合同。
2. 进入 `/portal?scenario=simulator-latest`，检查五节点同屏、顶部用户积木、底部队列挂载和接口方向是否符合原型意图。
3. 如人工确认当前视觉方向，再继续扩展点击节点后的浮动抽屉详情，而不是回到传统页面堆叠布局。
