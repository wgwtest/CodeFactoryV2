# 2026-05-14 P2 需规管理收口交接

## 1. 本轮目标

按“`P2` 先收口、平台交换层转入 `P-BasePlatform`”的边界，整理当前 `P2` 分支中的需规管理收尾修正，补齐自测与交接记录，并准备提交。

## 2. 本轮范围

本轮只处理以下内容：

- 需规管理列表排版修正。
- 需规列表删除动作与确认模态窗。
- Lab 模板实例 ID 到编写工作台模板 ID 的映射兼容。
- 发布失败 `409` 的业务化错误提示。
- 自测记录与交接文档补充。

本轮不处理以下内容：

- `ArtifactEnvelope` / `ArtifactConsumption` 等平台交换层模型。
- `P3` 读取来源从扫描冻结包切换到平台交换层。
- 跨阶段共享 API、消费审计、幂等发布等 `P-BasePlatform` 范围事项。

## 3. 当前改动文件

- `apps/api/app/requirement_spec_work_items/service.py`
- `apps/api/tests/test_requirement_spec_work_items_api.py`
- `apps/web/src/lib/requirementAnalysis.ts`
- `apps/web/src/pages/RequirementAnalysisLabPage.tsx`
- `apps/web/src/pages/RequirementAnalysisLabPage.css`
- `apps/web/src/test/RequirementAnalysisLabPage.test.tsx`
- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/260514-1733-P2-需规管理收口自测记录.md`

## 4. 关键结论

- 当前 `P2` 分支的业务闭环仍然是：`需规对象管理 -> 进入配置 -> 组织器会话 -> 发布 -> P3 可接收标识`。
- 当前 `P3` 可接收仍基于 `RequirementAuthoringDocument.frozen_package` 的发布态标记，不代表已经实现统一平台交换层。
- 平台交换层的分支开设与后续实现应留在 `feat/p-base-platform`，不随本次 `P2` 收口提交进入。

## 5. 验证要求

提交前已重新执行以下命令，结果见：

- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/260514-1733-P2-需规管理收口自测记录.md`

- `uv run pytest apps/api/tests/test_requirement_spec_work_items_api.py apps/api/tests/test_requirement_authoring_api.py apps/api/tests/test_requirement_analysis_api.py apps/api/tests/test_requirement_specs_api.py -q`
- `npm --prefix apps/web test -- RequirementAnalysisLabPage.test.tsx requirementAnalysisLabViewModel.test.ts`
- `npm --prefix apps/web run build`
- `git diff --check`

## 6. 当前状态

本轮代码与文档已自测通过，待提交。状态保持为“已自测，待人工验收”，不能直接记为“已验收”。
