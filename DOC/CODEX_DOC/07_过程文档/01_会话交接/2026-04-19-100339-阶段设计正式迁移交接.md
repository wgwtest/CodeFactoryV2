# 阶段设计正式迁移交接

## 1. 本轮目标

把 `P1 / P2 / P3 / P4 / P6` 相关设计从 `docs/superpowers/` 过程稿继续迁入 `DOC/CODEX_DOC` 正式文档根，并把入口、映射、阶段计划、自测与人测记录同步补齐。

## 2. 本轮完成内容

- 完成 `README` 阅读顺序补全，纳入 `P4` 专题设计入口
- 在平台总体设计中补充阶段正式设计入口
- 更新 `P1 / P2 / P4 / P6` 阶段计划，明确“正式设计归档同步完成”
- 更新 `P1` 计划参考来源，纳入文档钻取、治理工作台、OpenAI 兼容适配层
- 更新 `P4` 计划参考来源，纳入模拟研制执行器、工具仓测试治理、`P3-sim` 联调输入台、自演进巡检闭环
- 更新 `P4` 总体设计，显式写入工具仓测试治理、联调输入台、模拟研制执行器观察面
- 更新 `superpowers` 映射表，使 `P1 / P4` 的剩余专题不再只挂在计划层
- 把 `P1` 临时缺口盘点文档标记为“已完成迁移，仅保留审计用途”
- 新增本轮自测与验收入口

## 3. 当前状态

- 文档迁移层面：已完成本轮目标
- 验收状态：`待人工验收`
- 代码实现：本轮未改动

## 4. 后续注意事项

- 后续新产生的 `superpowers` 设计稿，如被采纳为正式基线，必须继续回写 `DOC/CODEX_DOC`
- 若后续继续推进 `P5`，应沿用本轮形成的入口、映射、计划、自测、交接一体化方式
- 临时盘点文档只做审计，不再作为正式设计依据
