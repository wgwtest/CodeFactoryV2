# 远端文档路径同步交接记录

## 1. 本轮目标

把本地新建立的正式文档根 `DOC/CODEX_DOC/` 同步到远端 GitHub Project 与一级 WBS Issue 协作面。

## 2. 本轮完成项

- 更新 GitHub Project `#7` readme
- 更新一级 WBS issue：
  - `#1` 总纲
  - `#2` P1
  - `#6` P2
  - `#9` P3
  - `#12` P4
  - `#25` P5
  - `#59` P6
- 为上述 issue 增加“正式文档路径”段落
- 为 `P3` 补充“远端旧子树仅为历史结构，后续以正式节点计划为准”的说明

## 3. 未完成项

- 尚未逐个下钻更新所有 L2/L3 issue 的正式路径
- 尚未执行 `P3` 远端子树结构重排
- 尚未把新的正式文档路径同步到所有历史评论、PR 或二级工单中

## 4. 后续建议

- 如果下一轮继续同步远端，优先处理：
  - `P2` 的 L2 子节点
  - `P3` 子树重排后的 L2/L3 路径
  - `P4/P6` 已存在子节点的正式路径归一化
- 远端后续新增节点时，直接以 `DOC/CODEX_DOC/...` 路径为正式引用，避免再次回补
