# P6 远端 WBS 树与 Project 投影同步交接

## 1. 本轮目标

把已经完成的 `P6` 远端 GitHub WBS 树重构和 `Project 7` 投影修正，同步回本地仓库中的历史快照、自测、验收和交接文档，形成闭环证据。

## 2. 本轮已完成内容

- 远端现状复核：
  - 复核 `#59` 的两层 `sub-issue` 树
  - 复核 `Project 7` 字段选项
  - 复核 `#59 ~ #88` 的项目项投影
- 新增本轮历史快照：
  - `docs/superpowers/issues/2026-04-21-p6-remote-wbs-project-sync.md`
- 更新旧快照说明：
  - `docs/superpowers/issues/2026-04-17-p6-platform-entry-issue-tree-mirror.md`
- 新增本轮自测文档：
  - `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-21-003714-P6远端WBS树与Project投影同步机测记录.md`
- 新增本轮验收入口：
  - `DOC/CODEX_DOC/06_测试文档/02_验收入口/99_历史归档/SETTING/2026-04-21-003714-P6远端WBS树与Project投影同步验收入口.md`

## 3. 本轮核心结论

- `P6` 当前远端 WBS 树已经固定为 `L1 -> L2 -> L3` 的严格树形结构。
- 本轮不应再另建第二棵 `P6` 树，后续应沿用 `#59` 主根继续拆分。
- `Project 7` 对 `P6` 的支撑已经补齐：`Layer` 支持 `L3`，`Phase` 支持 `6`，`Contributes To` 支持 `M6`。
- 本地 `docs/superpowers/issues/` 只能保留历史快照和过程证据，不能再次充当当前事实源。

## 4. 后续动作

1. 若后续继续细化 `P6`，先在 GitHub `#59` 树下继续挂接，不要绕开既有树结构单独开平行节点。
2. 若 `Project 7` 再增加新的 `P6` 子节点，必须同步补全 `Layer / Phase / Contributes To / Work Type / WBS Node / Parent Node`。
3. 若远端 `P6` 再次调整，应新增下一份历史快照，不覆盖本文件和 `2026-04-21-p6-remote-wbs-project-sync.md` 的时间点事实。

## 5. 当前状态

- 当前状态：待人工验收
