# P4 对象视图工作台编码落地交接

交接时间：2026-05-14 01:14

## 1. 本轮目标

按用户要求分三步推进 P4 编码落地：

1. 前端对象视图重构。
2. 前端 DTO / 投影适配。
3. 后端对象视图查询接口与测试。

## 2. 已完成内容

- `XX-P4` 页面已切为 v8 对象工作台结构，左侧对象导航包含九个对象页。
- 新增前端对象视图投影适配器 `buildP4ObjectViewsProjection`，页面关键派生数据已收敛到该 ViewModel。
- 新增后端 `P4ObjectWorkbenchProjection`，刷新投影时写入 `object_workbench`。
- 新增 `GET /api/tool-hub/operator/workbench/object-view`，支持按 `sheet_id`、`item_id`、`tool_id` 查询对象工作台投影。
- 更新测试覆盖对象页结构、前端投影派生、后端对象视图端口和 runtime 投影刷新集合。

## 3. 机测证据

- 机测记录：`DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/2026-05-14-011426-P4对象视图工作台编码落地机测记录.md`

已执行：

```bash
corepack pnpm --dir apps/web exec vitest run src/test/p4ObjectViewsProjection.test.ts src/test/XXP4Page.test.tsx
uv run pytest apps/api/tests/test_tool_hub_api.py -q
corepack pnpm --dir apps/web build
```

## 4. 后续建议

- 后续可把 `/xx-p4` 的页面加载从多接口组合逐步切到 `operator/workbench/object-view`，减少前端首屏组装负担。
- 覆盖知识图谱当前仍兼容 `coverage_matrix` 事实字段，中期可继续扩展为节点边 DTO。

## 5. 当前状态

已自测，待人工验收。
