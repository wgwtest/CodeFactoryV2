# P6 模拟状态源正式文档与 WBS 同步交接

## 1. 本轮目标

修正“把用户审阅入口错误指向 `superpowers spec`”的问题，把 `P6` 公共模拟状态源的设计结论直接回写到正式文档根，并同步远端 WBS。

## 2. 本轮已完成内容

- 回读全局工程策略与本地策略映射，确认：
  - 用户默认审阅入口是 `DOC/CODEX_DOC/`
  - `docs/superpowers/` 不是默认用户审阅源
- 更新正式研制计划：
  - `DOC/CODEX_DOC/03_研制计划/06-WBS-P6-门户与平台入口-研制计划.md`
- 更新正式设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6-门户与平台入口设计.md`
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.2-跨阶段只读集成与状态投影设计.md`
- 新增远端 WBS 节点：
  - `#89 WBS L3: P6.2.5 模拟状态源`
- 将 `#89` 挂接到：
  - `#61 WBS L2: P6.2 跨阶段状态集成`
- 将 `#89` 加入 `Project 7` 并补齐字段投影

## 3. 本轮核心结论

- 这轮设计的正式审阅入口必须是 `DOC/CODEX_DOC/`，不是 `docs/superpowers/specs/`。
- `P6` 首版不应直接绑定 `P1 ~ P5` 真实源，而应先通过 `P6.2.5 模拟状态源` 建立公共读侧闭环。
- 门户页只允许提供小型场景切换控件，不新增独立模拟页面。
- `P6.2.5` 必须服务 `P6.1`、未来串行观察页和 `P6.4`，不能做成只给 `/portal` 使用的页面私有 mock。

## 4. 后续动作

1. 以正式文档根中的本轮结论为依据，继续编写 implementation plan。
2. 代码实现顺序继续遵守：
   - 先模型与契约
   - 再后端公共读源
   - 最后前端 `/portal` 切换到公共投影
3. 进入代码实现前，应以 `DOC/CODEX_DOC/` 为用户确认入口，不再要求用户阅读 `superpowers spec`。

## 5. 当前状态

- 当前状态：待用户确认
