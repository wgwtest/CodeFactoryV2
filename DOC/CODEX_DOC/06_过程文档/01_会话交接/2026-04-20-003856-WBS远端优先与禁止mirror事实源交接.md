# WBS 远端优先与禁止 mirror 事实源交接

## 1. 本轮目标

响应新的协作约束：不再默认维护本地 `issue tree mirror` 作为当前树状态入口；WBS 读取应直接查询远端 GitHub，远端不可访问时才回退到主工作树中的正式文档根。

## 2. 本轮已完成内容

- 更新全局策略源文档：
  - `/home/wgw/CodexProject/PROJECT-TRA/ENGINEERING_WORK_STRATEGY.md`
- 更新技能引用文档：
  - `/home/wgw/CodexProject/PROJECT-TRA/skills/project-engineering-strategy/references/ENGINEERING_WORK_STRATEGY.md`
- 更新当前项目本地策略映射：
  - `DOC/CODEX_DOC/00-本地工程策略映射.md`
- 更新当前项目文档治理说明：
  - `DOC/CODEX_DOC/02_设计说明/01-文档治理与superpowers协同设计.md`
- 更新当前项目工作层 README：
  - `docs/superpowers/README.md`

## 3. 当前固定口径

- GitHub `Issues + sub-issues` 是 WBS 树唯一事实源。
- GitHub Project 只作为排程 / 展示投影，不替代 issue 树成为结构事实源。
- 默认不再创建或维护本地 `issue tree mirror` 作为当前树状态入口。
- 若远端暂时不可访问，只能回退到仓库主工作树中的正式文档根；不能从当前隔离目录或历史 mirror 反推当前远端树状态。
- 已存在的 `docs/superpowers/issues/` 文件只保留为历史快照、执行工单镜像和过程证据。

## 4. 后续动作

1. 后续若需要确认 WBS 结构、节点标题、节点正文、父子关系，先查 GitHub。
2. 远端不可访问时，再读取主工作树中的 `DOC/CODEX_DOC/`，并明确标记为本地快照或待远端复核。
3. 不再新增新的本地 `issue tree mirror` 文件作为当前事实入口。

## 5. 当前状态

- 当前状态：待人工验收
