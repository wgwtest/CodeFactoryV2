# superpowers 终稿归档规则交接

## 1. 本轮目标

把“`superpowers` 是过程稿，`CODEX_DOC` 才是终稿；用户不看时间化过程稿；核心细节必须归档回根目录”这套口径写成本地硬规则。

## 2. 已完成内容

- 更新 `DOC/CODEX_DOC/02_设计说明/01-文档治理与superpowers协同设计.md`
- 更新 `DOC/CODEX_DOC/00-本地工程策略映射.md`
- 更新 `docs/development-policy.md`
- 更新 `DOC/CODEX_DOC/04_研发文档/00-superpowers文档映射表.md`
- 新增验收清单与自测报告

## 3. 核心结论

- 后续不再把 `docs/superpowers/` 当成用户阅读路径。
- `superpowers` 只承担过程稿、草稿、计划、镜像职责。
- `CODEX_DOC` 必须承担用户阅读、正式确认、长期引用和逻辑归档职责。
- 映射表只表示来源关系，不表示正文已经完成迁移。

## 4. 后续执行要求

- 新的 `superpowers/specs` 若产出核心设计细节，必须回写到 `DOC/CODEX_DOC/`。
- 以后回答“设计在哪里”时，应优先给出 `DOC/CODEX_DOC/` 下的终稿路径，而不是 `superpowers` 路径。
- 若某阶段尚未把核心细节迁成终稿，应直接说明“正式迁移未完成”，不能再让用户去看时间稿。

## 5. 当前状态

- 当前状态：待用户确认
