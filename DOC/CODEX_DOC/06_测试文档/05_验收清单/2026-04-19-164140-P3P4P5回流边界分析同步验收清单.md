# P3P4P5 回流边界分析同步验收清单

## 1. 验收目标

确认本轮关于 `P3 / P4 / P5` 回流边界的分析，已经进入相应正式文档，而不是继续停留在临时讨论层。

## 2. 验收项

- [ ] `P3` 正式设计已明确 `P5` 可行性反馈的唯一仲裁规则
- [ ] `P3` 正式设计已明确 `design_gap / supply_gap / assembly_or_build_gap`
- [ ] `P4` 总体设计已明确“非直接修订语义”
- [ ] `P4` 核心业务循环已明确 `P5` 问题先进入 `P3` 仲裁，而不是直接进入 `P4`
- [ ] `P3` 阶段计划已补入唯一仲裁与新供给目标下发约束
- [ ] `P4` 阶段计划已补入“内部质量修订不等于接受 `P5` 直修回流”的约束
- [ ] `P5` 前置分析与过程稿已补入对“`P4` 完全无修订”这一表述的不准确性校正
- [ ] `superpowers` 映射表已登记本次 `P5` 过程稿向 `P3 / P4` 正式设计的回写关系

## 3. 验收说明

- 本清单验收的是“分析是否已回写到正式文档层”。
- 本清单不代表 `P5` 正式详细设计已经全部完成。
- 后续仍需继续把 `P5` 对象字段、状态机和导出契约正式迁入 `02_设计说明/`。

## 4. 当前状态

- 当前状态：待用户确认
