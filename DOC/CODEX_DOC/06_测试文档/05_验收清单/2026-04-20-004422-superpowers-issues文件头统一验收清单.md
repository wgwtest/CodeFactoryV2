# `docs/superpowers/issues` 文件头统一验收清单

## 1. 验收目标

确认 `docs/superpowers/issues/` 下现有文件已经统一补充防误读文件头。

## 2. 验收项

- [ ] 三个 `issue tree mirror` 文件已声明其为历史快照 / 过程证据，不是当前 WBS 树事实源
- [ ] 三个 `execution` 文件已声明其为执行工单镜像，不替代正式设计 / 正式计划 / 远端 issue 正文
- [ ] 六个文件都已写明 GitHub `Issues + sub-issues` 为当前 WBS 事实源
- [ ] 六个文件都已写明远端不可访问时回退到仓库主工作树中的 `DOC/CODEX_DOC/`

## 3. 当前状态

- 当前状态：待用户确认
