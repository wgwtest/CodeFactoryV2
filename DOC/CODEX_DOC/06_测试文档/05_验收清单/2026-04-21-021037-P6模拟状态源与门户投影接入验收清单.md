# P6 模拟状态源与门户投影接入验收清单

## 1. 验收目标

确认 `P6.2.5 模拟状态源` 已形成公共读侧接口闭环，并且 `/portal` 已切换为消费 `P6` 门户投影。

## 2. 验收项

- [ ] `/api/p6/mock-scenarios` 返回 `baseline / review-pressure / delivery-gap`
- [ ] `/api/p6/stage-snapshots?source=mock&scenario=baseline` 返回五阶段统一快照
- [ ] `/api/p6/portal-projection?source=mock&scenario=baseline` 返回 6 个节点、6 条关系、3 个产物
- [ ] `/api/p6/observation-projection?source=mock&scenario=delivery-gap` 返回 `focus_stage_id=P5`
- [ ] `/api/p6/portal-projection?source=live&scenario=baseline` 明确返回未实现
- [ ] `/portal` 页面右上角存在 `模拟源` 与三套场景切换按钮
- [ ] `/portal` 切换到 `交付缺口` 后，`P5` 节点显示交付缺口态与降级提示
- [ ] `/portal` 仍支持推荐布局 / 个人布局、拖拽记忆、语义线 / 投影聚合切换
- [ ] 双击 `P5` 节点仍可进入 `P5` 工作台
- [ ] 自测文档、交接文档已落盘到 `DOC/CODEX_DOC/`

## 3. 验收说明

- 本轮验收聚焦 `P6` 模拟状态源与门户读侧接入。
- 本轮不要求 `P1 ~ P5` 接入真实数据。
- 当前仓库存在与本轮无关的前端 TypeScript 旧问题，不应误判为本轮功能未完成。

## 4. 当前状态

- 当前状态：待用户确认
