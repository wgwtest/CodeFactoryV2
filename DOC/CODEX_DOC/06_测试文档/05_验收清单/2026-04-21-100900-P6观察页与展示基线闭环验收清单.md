# P6 观察页与展示基线闭环验收清单

- 日期：2026-04-21
- 对应范围：`P6.1`、`P6.2`、`P6.3`、`P6.4`

## 1. 页面入口

- [ ] `/portal` 继续作为 `P6` 首屏门户主入口。
- [ ] `/observation` 已形成独立的串行观察入口。
- [ ] 两个入口都不回落到主壳层菜单页。

## 2. P6.2 读侧投影消费

- [ ] 门户页继续消费 `PortalProjection`。
- [ ] 串行观察页消费 `ObservationProjection`。
- [ ] 场景切换继续固定为 `baseline / review-pressure / delivery-gap`。
- [ ] 当前首版默认仍走 `mock` 源，不伪装 `live` 已接通。

## 3. P6.3 平台展示基线

- [ ] 页面已通过正式接口读取 `display-baseline`。
- [ ] 页面已通过正式接口读取 `routes`。
- [ ] 门户页已通过正式接口读取 `legend`。
- [ ] 门户和观察页至少在壳层样式、文案或路由层显式继承平台基线，而不是只依赖本地硬编码。

## 4. P6.4 实验台与证据

- [ ] `/portal` 中仍采用小型 `卡片配置` 按钮承载实验台。
- [ ] 实验台 6 个区块完整存在。
- [ ] 模板、绑定、布局、预设、实验记录和晋升候选来自正式接口，而不是纯前端常量。
- [ ] 点击 `登记实验` 会调用正式实验登记接口。
- [ ] 实验结果只形成记录和候选，不直接改写阶段事实。

## 5. 串行观察页区块

- [ ] 串行观察卡片区存在。
- [ ] 平台总览条区存在。
- [ ] 阶段告警区存在。
- [ ] 降级说明区存在。
- [ ] 跨阶段对比区存在。
- [ ] 路由动作区存在。

## 6. 文档闭环

- [ ] `DOC/CODEX_DOC/02_设计说明/10-P6-门户与平台入口设计.md` 已回写新接口与 `/observation` 路由。
- [ ] `DOC/CODEX_DOC/02_设计说明/12-P6.2-跨阶段只读集成与状态投影设计.md` 已回写页面消费落地。
- [ ] `DOC/CODEX_DOC/02_设计说明/13-P6.3-设计语言与前端展示基线设计.md` 已回写基线查询接口。
- [ ] `DOC/CODEX_DOC/02_设计说明/14-P6.4-前端展示工具化实验场设计.md` 已回写 workbench 与实验登记接口。
