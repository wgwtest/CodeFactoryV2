# P3P4P5 回流边界分析同步自测

## 1. 自测目标

验证本轮关于 `P3 / P4 / P5` 回流边界的关键分析，是否已经从聊天结论下沉到正式文档层，尤其是以下三点：

- `P3` 是 `P5` 可行性反馈的唯一仲裁者
- `P4` 只接受 `P3` 重新签发的新供给目标，不接受 `P5` 直接修订命令
- “`P4` 不直接承接 `P5` 修订”不应被误写成“`P4` 完全不存在任何修订”

## 2. 本轮变更

本轮更新文档如下：

- `DOC/CODEX_DOC/02_设计说明/P3_软件设计系统/P3-软件设计系统设计.md`
- `DOC/CODEX_DOC/02_设计说明/P4_工具仓库/P4-工具仓库设计.md`
- `DOC/CODEX_DOC/02_设计说明/P4_工具仓库/P4-10-核心业务循环设计.md`
- `DOC/CODEX_DOC/04_研制计划/03-WBS-P3-软件设计系统-研制计划.md`
- `DOC/CODEX_DOC/04_研制计划/04-WBS-P4-工具仓库-研制计划.md`
- `DOC/CODEX_DOC/07_过程文档/04_历史分析/03-P5详细设计前置分析-临时.md`
- `DOC/CODEX_DOC/03_规范与流程/03_设计实现映射/00-superpowers文档映射表.md`
- `docs/superpowers/specs/2026-04-19-xx-p5-software-construction-system-design.md`

## 3. 自测项

1. 检查 `P3` 正式设计中是否明确 `P5` 可行性反馈的唯一仲裁规则。
2. 检查 `P3` 正式设计中是否已区分 `design_gap / supply_gap / assembly_or_build_gap`。
3. 检查 `P4` 总体设计中是否明确“非直接修订语义”。
4. 检查 `P4` 核心业务循环中是否明确 `P5` 问题报告进入 `P3` 仲裁队列，而不是直接进入 `P4`。
5. 检查 `P3 / P4` 阶段计划中是否补入相同边界约束。
6. 检查 `P5` 前置分析与过程稿中是否补入“`P4` 完全无修订”这一说法的不准确性校正。
7. 检查 `superpowers` 映射表中是否记录本次过程稿已向 `P3 / P4` 正式设计回写。

## 4. 自测结果

### 4.1 `P3` 正式设计

检查结果：

- 已确认 `DOC/CODEX_DOC/02_设计说明/P3_软件设计系统/P3-软件设计系统设计.md` 新增 `3.3 P5 可行性反馈的唯一仲裁规则`。
- 已明确三类判定：
  - `design_gap`
  - `supply_gap`
  - `assembly_or_build_gap`
- 已明确只有经 `P3` 仲裁后的供给缺口，才转成 `P4` 的新供给任务 / 派生任务 / 组件拆分任务。

### 4.2 `P4` 正式设计

检查结果：

- 已确认 `DOC/CODEX_DOC/02_设计说明/P4_工具仓库/P4-工具仓库设计.md` 新增 `3.4 外部回流边界与“非直接修订语义”`。
- 已明确：
  - `P4` 不直接接受 `P5` 的修订回流
  - `P4` 仍保留内部质量修订与自演进能力
  - `P5` 供给不匹配必须经 `P3` 仲裁后重签任务

### 4.3 `P4` 核心业务循环

检查结果：

- 已确认 `DOC/CODEX_DOC/02_设计说明/P4_工具仓库/P4-10-核心业务循环设计.md` 新增 `9.4 P5 可行性反馈的进入路径`。
- 已明确标准路径为：
  - `P5 问题报告 -> P3 仲裁 -> 设计重开 / P5 规则整改 / 新供给任务下发到 P4`

### 4.4 计划与映射

检查结果：

- `DOC/CODEX_DOC/04_研制计划/03-WBS-P3-软件设计系统-研制计划.md` 已补入唯一仲裁与供给缺口转发约束。
- `DOC/CODEX_DOC/04_研制计划/04-WBS-P4-工具仓库-研制计划.md` 已补入“内部修订不等于接受 `P5` 直修回流”的约束。
- `DOC/CODEX_DOC/03_规范与流程/03_设计实现映射/00-superpowers文档映射表.md` 已补入 `P5` 过程稿向 `P3 / P4` 正式设计的回写关系。

### 4.5 `P5` 过程与前置分析

检查结果：

- `DOC/CODEX_DOC/07_过程文档/04_历史分析/03-P5详细设计前置分析-临时.md` 已新增 `6.4 对“P4 完全无修订”的校正`。
- `docs/superpowers/specs/2026-04-19-xx-p5-software-construction-system-design.md` 已新增 `3.4 对“P4 完全无修订”的校正`。

## 5. 结论

本轮自测通过。

当前状态：

- 文档同步状态：已完成
- 正式设计完整度：边界分析已回写，`P5` 正式详细设计仍待继续展开
- 人工验收状态：待用户确认
