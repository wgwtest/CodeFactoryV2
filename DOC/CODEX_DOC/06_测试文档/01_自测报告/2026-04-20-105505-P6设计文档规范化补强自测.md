# P6 设计文档规范化补强自测

## 1. 自测目标

验证本轮是否已经把 `P6` 当前相关正式设计说明文档补强到接近 `07A-P5.1-最小构建闭环设计` 的规范度，并保证 `10-P6` 与 `11 ~ 14` 五份文档之间的结构和术语口径一致。

## 2. 本轮变更范围

- 更新 `P6` 总设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6-门户与平台入口设计.md`
- 更新 `P6.1` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
- 更新 `P6.2` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.2-跨阶段只读集成与状态投影设计.md`
- 更新 `P6.3` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.3-设计语言与前端展示基线设计.md`
- 更新 `P6.4` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.4-前端展示工具化实验场设计.md`

## 3. 自测项

1. 检查五份文档的章节骨架是否已整体向 `07A` 对齐。
2. 检查五份文档是否都补齐“契约、流程、对象、模块、作用域、页面映射、支撑机制、验收、文档关系”等关键章节。
3. 检查 `P6.1 ~ P6.4` 是否各自形成独立且真实的专项设计，而不是机械套用 `07A` 词汇。
4. 检查 `P6` 总体设计与四份专项设计的对象引用、模块边界和职责分工是否一致。
5. 检查登录 / 权限是否仍被保留为 `P6.R1 / P6.R2` 后置能力。

## 4. 自测结果

### 4.1 章节骨架对齐结果

检查结果：

- `10-P6` 与 `11 ~ 14` 五份文档均已采用接近 `07A` 的章节骨架。
- 五份文档均已具备以下大节：
  - 设计定位
  - 目标与边界
  - 输入输出契约
  - 总流程 / 最小闭环
  - 核心对象模型
  - 核心功能模块设计
  - 对象模型与模块设计关系
  - 数据来源、作用域与切换规则
  - 页面级交互设计与模块映射
  - 最小支撑服务与机制设计
  - 验收口径
  - 与后续节点的关系
  - 风格继承与节点例外
  - 正式文档关系

### 4.2 专项设计真实性检查

检查结果：

- `P6.1` 已补齐门户投影、布局档案、交互状态、模块依赖和页面区块映射。
- `P6.2` 已补齐查询 / 订阅输入、快照对象、投影对象、新鲜度策略和降级状态。
- `P6.3` 已补齐 token、命名基线、状态文案基线、共享展示基元和改造规则。
- `P6.4` 已补齐模板、绑定、布局、预设、实验记录和晋升候选机制。
- 对 `07A` 中不适合 `P6` 语义的章节，已采用“最小支撑服务与机制设计”等等位替换，而不是硬套伪后端内容。

### 4.3 总体设计与专项设计一致性

检查结果：

- `10-P6` 已明确产出 `PortalProjection`、`ObservationProjection`、`PlatformDisplayBaselinePackage`、`DisplayExperimentRecord` 等总层对象。
- `11 ~ 14` 四份专项设计均已与总层对象和模块职责形成引用关系。
- 登录 / 权限仍保留为 `P6.R1 / P6.R2`，没有回流重写当前 `P6` 阶段定位。

### 4.4 最小校验方式

本轮通过以下方式完成最小一致性校验：

- 使用 `rg` 检查五份文档的标题层级和章节编号
- 使用 `rg` 检查关键术语在五份文档中的引用一致性
- 使用 `git diff --stat` 检查本轮文档改动范围

## 5. 结论

本轮 `P6` 设计文档规范化补强自测通过。

当前状态：

- 文档补强状态：已完成
- 代码实现状态：本轮未改动
- 人工验收状态：待用户确认
