# CODEX_DOC 到 JB_DOC 完整投影缺口矩阵自测

## 1. 自测目标

验证本轮新增的“`CODEX_DOC -> JB_DOC` 完整投影缺口矩阵”是否已经把当前问题准确拆分为两类：

- `已有源内容，仅需投影`
- `源内容缺失，需先补 CODEX_DOC`

并明确 `JB_DOC` 的正确定位是 `CODEX_DOC` 的严格投影层，而不是替代根。

## 2. 本轮变更

本轮新增正式文档：

- `DOC/CODEX_DOC/01_需求分析/02-CODEX_DOC到JB_DOC完整投影缺口矩阵.md`

## 3. 自测项

1. 检查缺口矩阵文档是否存在。
2. 检查文档是否明确写出“`CODEX_DOC` 是根，`JB_DOC` 是严格投影层”。
3. 检查文档是否区分 `A 类` 与 `B 类` 两类问题。
4. 检查 `P1 / P2 / P3 / P4 / P6.1` 是否被归入“已有源内容，仅需投影”。
5. 检查 `P5`、契约级文档、测试/发布/移交/实例链是否被归入“源内容缺失，需先补 `CODEX_DOC`”。
6. 检查文档是否给出后续补齐顺序，而不只是停留在批评性描述。

## 4. 自测结果

### 4.1 文件存在性

检查结果：

- 已确认 `DOC/CODEX_DOC/01_需求分析/02-CODEX_DOC到JB_DOC完整投影缺口矩阵.md` 存在。

### 4.2 关键结构检查

检查结果：

- 文档已明确写出：
  - `DOC/CODEX_DOC/` 是研发正式文档根
  - `DOC/JB_DOC/` 是 `CODEX_DOC` 的标准化投影层
- 文档已区分：
  - `A 类矩阵：已有源内容，仅需投影`
  - `B 类矩阵：源内容缺失，需先补 CODEX_DOC`

### 4.3 核心分类检查

检查结果：

- 已将以下对象归入 `A 类`：
  - 平台总体层
  - 文档治理与双根规则
  - `P1`
  - `P2`
  - `P3`
  - `P4` 总体与四个专题
  - `P6.1`
- 已将以下对象归入 `B 类`：
  - `P5` 独立详细设计
  - `P6.2 / P6.3 / P6.4`
  - `P1 / P2 / P3 / P4` 契约级文档
  - 平台级测试链
  - 平台级发布与移交链
  - 项目实例样例链

### 4.4 可执行性检查

检查结果：

- 文档给出了后续补齐顺序：
  1. 先补 `A 类` 投影
  2. 再补 `B 类` 源内容
  3. 最后补测试、发布、移交与项目实例链

## 5. 结论

本轮自测通过。

当前真实状态：

- 矩阵文档：已形成
- 文档用途：可作为后续 `JB_DOC` 严格投影工作的分类基线
- 人工验收状态：待用户确认

