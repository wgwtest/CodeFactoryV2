# P1P5 到 P6 状态输出接口规范落版自测

## 1. 自测目标

验证本轮是否已经把 `P1 ~ P5 -> P6` 的共享状态输出规范落到正式文档中的明确位置，并让 `P1 ~ P5` 与 `P6.2` 都建立起可追溯的正式引用关系。

## 2. 本轮变更范围

- 新增共享规范：
  - `DOC/CODEX_DOC/03_规范与流程/01_数据规范/01-P1-P5到P6状态输出接口规范.md`
- 更新 `P6.2` 正式设计：
  - `DOC/CODEX_DOC/02_设计说明/12-P6.2-跨阶段只读集成与状态投影设计.md`
- 更新 `P1 ~ P5` 正式设计：
  - `DOC/CODEX_DOC/02_设计说明/02-P1-业务知识库设计.md`
  - `DOC/CODEX_DOC/02_设计说明/03-P2-需求分析系统设计.md`
  - `DOC/CODEX_DOC/02_设计说明/04-P3-软件设计系统设计.md`
  - `DOC/CODEX_DOC/02_设计说明/05-P4-工具仓库设计.md`
  - `DOC/CODEX_DOC/02_设计说明/07-P5-软件构建系统设计.md`

## 3. 自测项

1. 共享规范是否已落在 `DOC/CODEX_DOC/03_规范与流程/01_数据规范/` 的正式位置。
2. 共享规范是否已覆盖统一查询契约、统一骨架字段、五阶段差异字段、节点状态负载、订阅接口、新鲜度与降级、验收要求。
3. `P6.2` 是否已显式引用共享规范，并明确“共享规范负责跨阶段约束，`P6.2` 负责消费侧映射”。
4. `P1 ~ P5` 是否都已在“正式文档关系”中反向引用该共享规范。
5. `P6.2` 查询接口字段名是否已与共享规范收敛为同一口径。
6. 文档补丁是否通过 `git diff --check` 的格式检查。

## 4. 自测结果

### 4.1 共享规范位置检查

检查结果：

- 已新增 `DOC/CODEX_DOC/03_规范与流程/01_数据规范/01-P1-P5到P6状态输出接口规范.md`。
- 该文件位于正式研发文档下的“数据规范”目录，满足“放在文档中明确位置”的要求。

### 4.2 规范内容覆盖检查

检查结果：

- 共享规范已包含以下章节：
  - 规范定位
  - 适用范围
  - 职责分工
  - 查询接口统一契约
  - 平台统一骨架字段
  - 五阶段差异字段规范
  - 节点状态负载映射规范
  - 订阅接口规范
  - 新鲜度与降级规范
  - 响应结构建议
  - 示例
  - 验收要求
  - 关联文档

### 4.3 反向引用检查

执行检查命令：

```bash
rg -n "01-P1-P5到P6状态输出接口规范" DOC/CODEX_DOC/02_设计说明 DOC/CODEX_DOC/04_研发文档 -g '*.md'
```

检查结果：

- `P6.2` 已在以下位置引用共享规范：
  - 顶部“关联正式文档”
  - `3.2 五阶段状态输出契约`
  - `14. 正式文档关系`
- `P1 ~ P5` 已全部在“正式文档关系”中补入对共享规范的引用。

### 4.4 字段名一致性检查

检查结果：

- 已把 `P6.2` 中旧口径字段名收敛为共享规范口径：
  - `status -> primary_status`
  - `route -> entry_route`
  - `available -> entry_available`
  - `level -> health_level`
- `StageSnapshot` 也已改用 `primary_status`，避免消费侧对象与共享规范自相矛盾。

### 4.5 格式检查

执行检查命令：

```bash
git diff --check
```

检查结果：

- 命令返回空结果，未发现补丁格式问题。

## 5. 结论

本轮 `P1 ~ P5 -> P6` 状态输出接口规范落版自测通过。

当前状态：

- 正式规范位置：已固定
- `P1 ~ P5 / P6.2` 引用关系：已补齐
- 字段名一致性：已收敛
- 代码实现状态：本轮未改动
- 人工验收状态：待用户确认
