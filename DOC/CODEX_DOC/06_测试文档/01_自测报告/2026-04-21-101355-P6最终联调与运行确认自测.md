# P6 最终联调与运行确认自测

- 日期：2026-04-21
- 对应范围：`P6.1` 首屏观察门户、`P6.2` 跨阶段只读集成与状态投影、`P6.3` 设计语言与前端展示基线、`P6.4` 前端展示工具化实验场

## 1. 本轮补充动作

- 修正 `apps/web/src/test/p6TestData.ts` 中 `ObservationProjection` 测试夹具的类型收窄问题，清除本轮新增的前端构建噪音。
- 复核当前开发服务进程，确认前后端均来自当前工作树：
  - 前端：`http://127.0.0.1:5182`
  - 后端：`http://127.0.0.1:8050`
- 复核 `/portal`、`/observation` 与新增 `platform-config / p6` 接口均可达。

## 2. 自测命令与结果

### 2.1 后端 P6 接口测试

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：通过，`10` 个测试全部通过。

### 2.2 前端 P6 定向测试

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/P6ObservationPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/AppRoutes.test.tsx
```

结果：通过，`23` 个测试全部通过。

### 2.3 前端 P6 关键页面回归

```bash
corepack pnpm --dir apps/web test src/test/P6ObservationPage.test.tsx src/test/P6PortalPage.test.tsx src/test/AppRoutes.test.tsx
```

结果：通过，`21` 个测试全部通过。

### 2.4 运行时可达性检查

```bash
curl -I http://127.0.0.1:5182/portal
curl -I http://127.0.0.1:5182/observation
curl -s http://127.0.0.1:8050/api/platform-config/display-baseline | head -c 300
curl -s "http://127.0.0.1:8050/api/p6/observation-projection?source=mock&scenario=delivery-gap" | head -c 400
```

结果：

- `/portal` 返回 `HTTP/1.1 200 OK`
- `/observation` 返回 `HTTP/1.1 200 OK`
- `display-baseline` 返回 `p6.3-v1` 配置内容
- `observation-projection` 返回 `delivery-gap` 场景投影内容

### 2.5 差异检查

```bash
git diff --check
```

结果：通过。

## 3. 关键确认点

- `/portal` 继续作为 `P6` 主入口，且由正式接口提供场景目录、门户投影、展示基线、平台路由和图例配置。
- `/observation` 已独立可达，且直接消费 `ObservationProjection`。
- `P6.4` 实验台继续嵌入 `/portal`，模板、绑定、布局、预设、实验记录和晋升候选由正式接口提供。
- 当前本地服务已对应到本工作树，不是外部旧实例。

## 4. 非 P6 范围遗留项

执行以下命令时：

```bash
corepack pnpm --dir apps/web build
```

结果：失败，但失败点均位于 `P3 / P4 / P5` 和通用前端历史类型问题，不属于本轮 `P6` 改动引入的问题，当前剩余报错包括：

- `src/components/p3/P3AtomicToolRequestGenerator.tsx`
- `src/components/p4/P4RealToolDeliveryWorkspace.tsx`
- `src/components/p5/P5DeliveryManifestPanel.tsx`
- `src/lib/toolHub.ts`
- `src/test/XXP4Page.test.tsx`
- `src/test/viteConfig.test.ts`

## 5. 当前结论

- `P6` 范围代码、文档、接口、页面与本地运行链路已闭环。
- 当前状态：`待人工验收`
