# P2 组织器插件与 Codex Brainstorming 对照测试指南

**文档性质：** 长期测试指南  
**适用范围：** P2 需求分析系统、组织器插件、Codex `brainstorming` skill 对照验证  
**首次整理日期：** 2026-05-08

## 1. 指南定位

本文用于统一沉淀 P2 组织器插件验证、真实 Codex `brainstorming` skill 对照测试、长轮次决策模式分析和整改建议归纳的方法。

本文不是某一次测试报告，也不是某个组织器的设计说明。单次执行数据、运行截图、日志摘要和统计结论仍应归档到 `DOC/CODEX_DOC/06_测试文档/03_机测记录/`；本文只规定后续反复执行同类测试时应遵守的测试口径、隔离方法、数据格式和分析方法。

本文应作为以下工作的共同入口：

1. 新增一种 P2 组织器插件后的效果验证。
2. 对比 `xg-heuristic-orchestrator`、`brainstorm-v1`、Dify workflow 型组织器或远端服务型组织器的决策行为。
3. 将 P2 组织器插件与真实 Codex `brainstorming` skill 做对照测试。
4. 分析“为什么 P2 输出不如 brainstorming 直观、深入或稳定”。
5. 把测试结论反推到组织器设计、Prompt 资产、插件合同和前端观测页面。

当前重点对照对象为三组：

| 对象 | 性质 | 是否 P2 插件 | 测试定位 |
| --- | --- | --- | --- |
| `brainstorm-v1` | 本地 Prompt/schema 驱动组织器 | 是 | P2 本地组织器主要整改对象 |
| `brainstorm-v1-dify-workflow` | Dify Workflow 驱动组织器 | 是 | P2 外部 workflow 型组织器主要整改对象 |
| Codex `brainstorming` skill | Codex 本身的 skill | 否 | 强参考标杆，用于对比交互深度、决策推进和成稿质量 |

真实 Codex `brainstorming` skill 不是插件，不能要求它输出 P2 插件合同、Provider 调用日志、document patch 或 decision state JSON。它的价值是提供高质量需求探索行为样本，用于反推 P2 组织器应如何改进。

## 2. 归档位置规则

### 2.1 长期指南

长期指南统一放在：

```text
DOC/CODEX_DOC/06_测试文档/00_测试指南/
```

本目录存放可复用测试方法，不记录某次测试的完整原始数据。

### 2.2 单次机测记录

单次测试报告统一放在：

```text
DOC/CODEX_DOC/06_测试文档/03_机测记录/
```

命名建议：

```text
YYYY-MM-DD-P2-<测试对象>-<测试主题>-测试报告.md
YYYY-MM-DD-P2与Codex-Brainstorming-<轮次或主题>-对比测试报告.md
```

### 2.3 设计反推文档

如果测试结论会改变系统设计，应先形成设计补充稿，再按评审结论归并到主设计文档：

```text
DOC/CODEX_DOC/02_设计说明/P2_需求分析系统/
```

命名建议：

```text
P2-需求分析系统设计-YYMMDD-<主题>补充.md
```

### 2.4 插件合同与协作规范

涉及组织器插件输入输出合同、Dify workflow 字段、远端服务接入等公共约束，应同步维护：

```text
DOC/CODEX_DOC/03_规范与流程/
```

## 3. 已有依据

后续测试应优先阅读以下文档，避免重复摸索：

| 类型 | 文档 |
| --- | --- |
| P2 多轮效果验证 | `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/260506-1928-P2需求分析Lab-态势分析系统多轮效果验证报告.md` |
| 真实 Codex Brainstorming 深化测试 | `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/260506-2040-Codex真实BrainstormingSkill-态势分析系统深化测试报告.md` |
| P2 与 Codex 20 回合对比 | `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-05测试/260506-2334-P2与Codex-Brainstorming-20回合决策模式对比测试报告.md` |
| 决策模式整改策略 | `DOC/CODEX_DOC/02_设计说明/P2_需求分析系统/P2-需求分析系统设计-260506-决策模式与组织器整改策略补充.md` |
| 组织器插件输入输出合同 | `DOC/CODEX_DOC/03_规范与流程/01_数据规范/02-P2-组织器插件输入输出合同规范.md` |
| Brainstorm v1 Dify 工作流规范 | `DOC/CODEX_DOC/03_规范与流程/02_组织器协作流程/02-P2-Brainstorm-v1-Dify工作流搭建规范.md` |
| 态势分析系统样例目录 | `DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/README.md` |
| 需求规格说明样例-态势分析系统 | `DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/01-需求规格说明样例-态势分析系统.md` |
| 受控动态考官手册-态势分析系统 | `DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/02-动态考官手册-态势分析系统.md` |
| 组织器评估清单-态势分析系统 | `DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/03-组织器评估清单-态势分析系统.md` |

真实 Codex `brainstorming` skill 源文件位置为：

```text
/home/wgw/.codex/skills/brainstorming/SKILL.md
```

## 4. 核心测试问题

这类测试不应只比较“能不能跑通”。必须回答以下问题：

1. 组织器是否先识别关键产品决策，而不是直接填需求规格章节。
2. 组织器是否能在长轮次中维护稳定的决策链。
3. 每轮是否只提出一个最关键的下一步问题，且用户能理解为什么要问。
4. 对用户输入的组合意图、偏离选项、补充事实、反驳和改题是否能正确处理。
5. 是否存在模板章节、完成度树或硬编码规则过度牵引的问题。
6. 是否能在轮次上限或信息足够时主动收束并输出成稿。
7. 调用日志是否能复现每一阶段的模型输入、输出、输出格式要求和后处理。
8. 测试结论能否明确映射到插件 Prompt、schema、adoption、runtime 或前端观测设计。
9. 最终正文有效字数、平均正文块长度、实质正文块比例和草案丰富度是否随整改向好。
10. 已确认事实和已确认决策是否随整改稳定沉淀，而不是只形成空泛正文。
11. P2 组织器与真实 Codex `brainstorming` skill 的差距是否缩小。

## 5. 测试对象分类

### 5.1 P2 本地策略解释型组织器

典型对象：

```text
orchestrators/xg/xg-heuristic-orchestrator/
orchestrators/xg/brainstorm-v1/
```

测试重点：

1. 插件 manifest 是否可发现。
2. adapter 是否只通过统一运行时端口与宿主交互。
3. Prompt、schema、adoption、spec_strategy 是否承担主要业务判断。
4. Python 代码是否只做合同校验、字段映射、状态归并和错误处理，不做章节匹配、业务拒绝、下一问选择等智能判断。

### 5.2 Dify workflow 型组织器

典型对象：

```text
orchestrators/xg/brainstorm-v1-dify-workflow/
```

测试重点：

1. Dify 输入字段是否与 P2 插件输入合同一致。
2. Dify 输出字段是否能归一化为 P2 `OrchestratorRunResult`。
3. Dify 内部节点能否表达 intent、decision state、write projection、next planning 等阶段。
4. P2 调用日志是否能看到 workflow 请求、原始输出和归一化结果。

### 5.3 远端服务型组织器

远端服务型组织器后续接入时，应按同一合同测试。区别只在于 transport 和认证方式，不改变 P2 侧对组织器的判断口径。

### 5.4 Codex Brainstorming Skill

真实 Codex `brainstorming` 不是 P2 插件，也不是需求规格正文 patch 系统。它是 Codex 本地 skill，通过 `SKILL.md` 约束模型行为。

因此，对照测试不能把 P2 的正文写入量作为唯一指标；应重点比较它的决策推进方式：

1. 是否先判断项目范围和关键风险。
2. 是否一轮一个主问题。
3. 是否给出选项、推荐和理由。
4. 是否在信息足够后进入方案比选。
5. 是否在用户要求或轮次上限时主动成稿。

对照测试时，应把 Codex `brainstorming` skill 当作参考标杆，而不是当作要被 P2 宿主调度的组件。

Codex `brainstorming` skill 的测试要求：

1. 使用隔离 Codex 会话，明确加载真实 `brainstorming` skill。
2. 使用与 P2 组织器相同的基准起始输入和受控动态考官事实池。
3. 不强制要求它每轮输出结构化 `decision_state`，但测试者需要在记录中归纳已确认事实、已确认决策、未闭合问题和最终成稿。
4. 当交互已充分或达到轮次上限时，应要求它形成需求规格说明或等价成稿。
5. 记录其交互过程、最终成稿、问题组织方式、方案收束方式和可借鉴策略。

Codex `brainstorming` skill 的对照指标可以采用以下折算方式：

| P2 指标 | Codex skill 折算口径 |
| --- | --- |
| `completed_turns` | 实际交互轮数 |
| `confirmed_facts_count` | 从交互记录和最终成稿中归纳的明确事实数 |
| `confirmed_decisions_count` | 从交互记录和最终成稿中归纳的稳定产品决策数 |
| `open_questions_remaining` | 最终仍需用户确认的问题数 |
| `final_working_document_text_chars` | 最终需求规格说明或等价成稿的有效字数 |
| `final_working_document_block_count` | 最终成稿的章节数、条目数或自然段数，需说明切分口径 |
| `avg_block_text_chars` | 最终有效字数 / 有效正文块数 |
| `substantive_block_count` | 包含具体事实、角色、场景、功能、数据、约束、异常或验收要点的正文块数 |
| `draft_richness_score` | 根据最终成稿的详实程度、章节覆盖、具体性和可执行性评分 |
| `next_question_quality_score` | 根据交互过程中提问是否聚焦、承接上下文、有推荐理由评分 |

这些折算指标用于对照分析，不表示 Codex skill 已变成 P2 插件。

## 6. 标准测试场景

### 6.1 基准起始输入

后续默认使用以下起始输入作为基准场景：

```text
我希望创建一个态势分析系统。这个态势分析系统里面要有态势的展示，要有一系列态势分析工具，比如地理信息分析工具、通视量算、坡度分析，还有部署分析系统。但是当前用户、使用模式我都没有想清楚，希望你来一块帮我想清楚。
```

### 6.2 受控动态多轮测试策略

后续态势分析系统多轮测试默认采用“受控动态考官”策略，而不是固定投喂脚本。受控动态考官的默认动作是回答组织器问题；只有在明显偏离、连续空泛、遗漏已确认事实或过早收束时，才少量引导或纠偏。

测试者在测试前应先阅读：

```text
DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/01-需求规格说明样例-态势分析系统.md
DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/02-动态考官手册-态势分析系统.md
DOC/JB_DOC/03-项目实例与样例/P2-需求分析Lab样例-态势分析系统/03-组织器评估清单-态势分析系统.md
```

其中：

1. `需求规格说明样例-态势分析系统` 是隐藏基准和人工复核依据，不得整体输入给被测组织器。
2. `受控动态考官手册-态势分析系统` 规定多轮测试中的观察、分类、决策、响应方式。
3. `组织器评估清单-态势分析系统` 用于评估最终产物是否覆盖关键需求域。

受控动态多轮测试执行规则：

1. 首轮只输入 `6.1` 的基准起始输入。
2. 后续每轮先观察组织器当前输出、结构化状态、临时正文、下一问和调用日志。
3. 如果组织器提出聚焦问题，测试者应按题回答；如果是选择题，优先选项化回答，并最多补充一句理由。
4. 每轮输入最多覆盖一个主题，通常不超过 3 条事实；不得一次性释放完整角色表、功能表、流程表、数据表或验收表。
5. 如果组织器方向合理，即使还不完整，也应先顺着它的问题链继续回答，不主动抢跑成稿。
6. 如果组织器遗漏已确认事实、混淆模板与样例、过早成稿或过度扩展系统边界，测试者应按受控动态考官手册进行轻量纠偏。
7. 如果组织器连续两轮空泛追问或重复追问，测试者可以从样例事实池中选择一个具体场景、角色或功能例子补充，观察其归纳能力。
8. 如果组织器已覆盖当前阶段重点，测试者不应继续强塞信息，应要求它总结已闭合决策、未闭合问题和下一步计划。
9. 终止时用评估清单检查最终需求规格草案，而不是检查它是否命中某个固定输入顺序。

受控动态测试至少应覆盖以下需求域：

| 需求域 | 测试关注点 |
| --- | --- |
| 软件定位 | 是否识别为软件级态势编辑、展示与分析工具 |
| 用户角色 | 是否识别科研分析人员、业务专家、数据管理员、系统管理员等角色 |
| 核心流程 | 是否形成态势创建编辑、分析工具使用、成果导出复核等流程 |
| 功能行为 | 是否把核心功能写成角色、触发、输入、处理、输出、异常和验收要点 |
| 数据接口 | 是否覆盖底图、地形、矢量、栅格、业务对象、参数和导出结果 |
| 范围边界 | 是否排除实时多源情报、自动决策、工程级测绘和多单位协同指挥 |
| 非功能 | 是否覆盖性能、可靠性、安全权限、部署环境、精度质量 |
| 待确认事项 | 是否保留未闭合问题，而不是虚构确定结论 |

### 6.3 历史 20 回合回归链路

以下链路来自早期长轮次对比测试，可用于历史回归、回放和与旧报告复现比对。它不再作为默认多轮测试策略，也不得替代 `6.2` 的受控动态考官策略。

| 回合 | 输入摘要 |
| ---: | --- |
| 1 | 起始模糊需求：建设态势分析系统，含态势展示、GIS 分析、通视量算、坡度分析、部署分析，用户与使用模式未明确。 |
| 2 | 主用户：参谋分析员；下游查看者：指挥员。 |
| 3 | 主场景：实时态势，兼顾事前研判；给出典型流程。 |
| 4 | 数据模式：混合模式；实时接入态势与告警，导入底图、任务区、DEM、部署点位和标注。 |
| 5 | 部署影响分析边界：覆盖与冲突分析，不做自动最优推荐。 |
| 6 | 协同模式：中协同，支持结果共享与接力编辑，不做强实时共编。 |
| 7 | 指挥员消费方式：系统内指挥视图 + 导出报告和图件。 |
| 8 | 刷新方式：准实时刷新，普通态势 10-30 秒，关键告警尽快推送。 |
| 9 | 角色权限：管理员、参谋分析员、指挥员、情报分析员、值班操作员。 |
| 10 | 非功能与部署：2 秒地图查询、10 秒分析、30 并发、权限分级、审计、离线底图、内网/专网。 |
| 11 | 精度口径：辅助研判级，不承诺测绘工程级精度。 |
| 12 | 导出形式：专题图件 + 简化研判报告。 |
| 13 | 明确不做范围。 |
| 14 | 异常与补偿机制。 |
| 15 | 验收任务链。 |
| 16 | 主要界面列表。 |
| 17 | 安全要求。 |
| 18 | 回看：哪些关键决策已闭合、哪些未闭合。 |
| 19 | 补充剩余未闭合项：GIS 数据接入优先方式、模板先固定。 |
| 20 | 强制停止追问，输出完整需求规格说明草案。 |

使用本链路时，报告必须明确标注“历史 20 回合回归链路”，不能把固定链路表现误判为组织器真实动态交互能力。

### 6.4 变体测试场景

后续至少应补充以下变体：

1. 用户只回答选项，不补充事实。
2. 用户选择选项并补充关键事实。
3. 用户完全忽略选项，提出新方向。
4. 用户反驳组织器建议。
5. 用户要求回到前面章节或前面决策点。
6. 用户给出互相冲突的信息。
7. 用户要求提前成稿。
8. 用户要求停止追问但保留未闭合问题。

## 7. P2 组织器测试方法

### 7.1 测试入口

优先使用真实后端 API，而不是直接调用内部函数：

```text
POST /api/requirement-analysis/sessions
POST /api/requirement-analysis/sessions/{session_id}/turns
```

可使用 `FastAPI TestClient(create_app())` 在本地进程内执行，避免浏览器操作不稳定影响测试。

### 7.2 必备配置

每次报告必须记录：

| 字段 | 说明 |
| --- | --- |
| 组织器 ID | 例如 `xg-heuristic-orchestrator`、`brainstorm-v1` |
| Provider | 例如 DeepSeek、Mock Provider、Dify workflow |
| 模型 | 真实模型名称或 mock 标识 |
| 模板实例 | 本次使用的需求规格说明模板实例 |
| 启动参数 | 会话 topic、写入策略、知识包、模板等 |
| 代码提交 | 当前 git commit hash |
| 测试方式 | API、页面、人测、脚本或混合 |

### 7.3 必采数据

每轮至少保存：

1. 用户输入。
2. 助手可见回复。
3. 临时正文或结构化承载页快照。
4. 决策链状态快照。
5. 下一步交互规划。
6. Provider 调用日志数量。
7. 每次 Provider 调用的阶段 ID、Prompt ID、输入摘要、输出摘要和后处理结果。
8. 错误、重试、降级或空输出情况。

如果页面已经提供调用详情，应优先导出页面展示字段；如果页面无法导出，应从后端响应和数据库快照中补齐。

## 8. Codex Brainstorming 对照测试方法

### 8.1 必须隔离运行

真实 Codex `brainstorming` 测试必须在独立临时 git 仓库或临时目录中运行，不能直接在 CodeFactoryV2 工作树中运行。

原因是 Codex 会读取当前项目上下文。如果在 CodeFactoryV2 仓库中测试，它可能读到 P2 设计文档、代码和已有测试报告，导致测试污染。

推荐目录形态：

```text
/tmp/codex-brainstorm-compare.<random>/
```

目录内至少执行：

```bash
git init
```

### 8.2 执行方式

首次启动可使用：

```bash
codex exec --json --dangerously-bypass-approvals-and-sandbox \
  --output-last-message "$OUT" \
  "请使用真实 brainstorming skill 和我进行需求探索，不要写代码。不需要浏览器。我的初始想法是：..." \
  > "$JSONL" </dev/null
```

续接可使用：

```bash
codex exec resume --json --dangerously-bypass-approvals-and-sandbox \
  --output-last-message "$OUT" \
  "$THREAD_ID" \
  "用户下一轮输入..." \
  > "$JSONL" </dev/null
```

### 8.3 必采数据

每轮至少保存：

1. thread ID。
2. 用户输入。
3. Codex 最后一条回复。
4. jsonl 原始事件。
5. 输出长度。
6. 是否提问。
7. 是否给选项。
8. 是否给推荐。
9. 是否进入方案比选。
10. 是否主动成稿。

### 8.4 污染样本处理

如果发现某轮 Codex 回复引用了 CodeFactoryV2 当前项目、P2 文件、既有测试结论或本不应知道的上下文，该样本必须标记为污染样本，不得纳入统计结论。

污染样本可以在报告中说明，但不能用于支持能力判断。

## 9. 对比指标

### 9.1 量化指标

| 指标 | 含义 |
| --- | --- |
| 总轮次 | 每组会话执行轮次 |
| 平均助手回复长度 | 判断用户可见层是否过短 |
| 中位数助手回复长度 | 排除极端长回复干扰 |
| 长回复轮次 | 例如超过 300 字的轮次 |
| 最终正文长度 | 判断成稿规模，但不能单独代表质量 |
| 正文长度回落次数 | 判断补丁应用是否覆盖或丢失 |
| Provider 调用次数 | 判断阶段链路是否按设计执行 |
| 决策项增长数 | 判断决策链是否推进 |
| 未闭合问题数量变化 | 判断是否收束 |
| 成稿轮次 | 判断系统何时进入落稿 |

### 9.2 行为指标

| 指标 | 观察点 |
| --- | --- |
| 首轮行为 | 是直接写章节，还是先识别关键决策 |
| 下一问质量 | 是否一轮一个主问题，是否有理由 |
| 组合意图处理 | 是否同时吸收选项选择和补充事实 |
| 偏离输入处理 | 是否能识别用户正在换题或新增方向 |
| 反驳处理 | 是否能修正前一轮判断 |
| 决策链连续性 | 是否重复问已经闭合的问题 |
| 方案比选能力 | 是否能提出 2-3 个路径并推荐 |
| 收束能力 | 是否能在信息足够或上限到达时主动成稿 |
| 可观测性 | 是否能从日志解释为什么这么问、为什么这么写 |

## 10. 报告结构模板

单次对比测试报告建议使用以下结构：

```text
# <测试标题>

## 1. 测试背景
## 2. 测试对象
## 3. 测试环境
## 4. 测试输入链路
## 5. P2 测试过程与数据
## 6. Codex Brainstorming 测试过程与数据
## 7. 量化统计
## 8. 代表轮次摘录
## 9. 决策模式对比
## 10. 根因分析
## 11. 对设计和实现的整改建议
## 12. 附录：原始数据位置
```

报告正文必须包含足够的测试数据，不能只给结论。至少应包括每组每轮的输入摘要、输出摘要、输出长度、阶段判断和关键异常。

## 11. 判断规则

### 11.1 不能接受的测试结论

以下结论不充分：

1. “输出少，所以效果差。”
2. “调用了多次模型，所以链路合理。”
3. “正文有内容，所以本轮成功。”
4. “Brainstorming 更像人，所以应该照抄。”
5. “Mock Provider 通过，所以真实模型也会通过。”

### 11.2 可接受的测试结论

可接受结论应满足：

1. 有原始输入输出证据。
2. 有至少一组完整长轮次链路。
3. 有 P2 与对照对象的同题比较。
4. 能指出差异发生在哪个阶段或哪个合同字段。
5. 能区分模型能力问题、Prompt 资产问题、状态设计问题、后处理问题和前端展示问题。
6. 能提出可验证的整改方向。

## 12. 测试到设计的回写规则

测试报告形成后，应按以下顺序处理：

1. 先判断问题是否只是实现缺陷。
2. 如果是实现缺陷，进入研发计划或 issue。
3. 如果涉及概念、边界、状态机、Prompt 阶段、插件合同或页面观测模型，应形成设计补充稿。
4. 设计补充稿经审阅后，归并到 `P2-需求分析系统设计.md`。
5. 主设计文档归并后，再进行代码实现或测试脚本固化。
6. 实现完成后，再用本指南规定的同类测试验证设计是否兑现。

## 13. 经验结论

截至 2026-05-08，已有测试形成以下稳定经验：

1. 真实 Codex `brainstorming` 的优势主要来自连续决策过程，而不是单轮输出更长。
2. P2 不能直接照搬 `brainstorming`，因为 P2 还要承担模板适配、结构化状态、正文投影、调用日志和组织器插件合同。
3. P2 要接近 `brainstorming` 的体验，关键不在于增加固定规则，而在于让组织器插件用模型阶段维护决策链，并把下一步问题生成交给模型判断。
4. `write` 在探索收束阶段不应被理解为“补齐需求规格正文”，而应被理解为把当前结构化决策状态可视化、可审阅地投影出来。
5. 单次短链路测试不足以评价 `brainstorming` 类能力；长轮次测试应至少覆盖 10 轮，完整对比建议使用最多 20 轮。
6. Codex skill 对照测试必须隔离工作树，否则会被项目上下文污染。
7. 组织器插件测试必须同时看用户可见回复和 Provider 调用日志；只看正文长度会误判问题。

## 14. 后续维护要求

后续每次完成新的组织器插件、Dify workflow、远端组织器接入或 Prompt 策略升级后，应至少执行一次本指南下的对照测试。

如果测试过程中出现新的稳定经验，应更新本文；如果只是某次样本现象，应只写入对应机测记录，不直接改本文。
