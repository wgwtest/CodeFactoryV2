# P6 语义画布流动与 P3-P5 v17 自检记录

时间：2026-04-30 01:00:45

## 1. 自检对象

- 原型包：`DOC/CODEX_DOC/08_原型与附图/2026-04-30-005207-CodeFactoryV2-P6语义画布流动与P3P5补充原型-v17/`
- 源文件：
  - `source/v17-semantic-canvas.html`
  - `source/v17-semantic-canvas.css`
- 评审图：
  - `01-1920x1080-五阶段语义画布总览态.png`
  - `02-1920x1080-P3选中抽屉态.png`
  - `03-1920x1080-语义流与图例说明态.png`

## 2. 执行命令与结果

### 2.1 截图生成

```bash
base="$PWD/DOC/CODEX_DOC/08_原型与附图/2026-04-30-005207-CodeFactoryV2-P6语义画布流动与P3P5补充原型-v17"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v17-semantic-canvas.html#overview" "$base/01-1920x1080-五阶段语义画布总览态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v17-semantic-canvas.html#selected" "$base/02-1920x1080-P3选中抽屉态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v17-semantic-canvas.html#legend" "$base/03-1920x1080-语义流与图例说明态.png"
```

结果：三张截图均生成成功。

### 2.2 图片尺寸检查

```bash
file 'DOC/CODEX_DOC/08_原型与附图/2026-04-30-005207-CodeFactoryV2-P6语义画布流动与P3P5补充原型-v17/'*.png
```

结果：三张评审图均为 `1920 x 1080`。

### 2.3 DOM 检查

使用 `@playwright/test` 在 `1920 x 1080` 视口分别打开 `#overview`、`#selected`、`#legend`。

检查结果：

- 三种状态均有五张系统卡。
- 三种状态端口数为 `9`，P1 左侧外部输入端口已删除。
- 三种状态均有 `6` 个流动小标签。
- `external-knowledge` 外部箭头不存在。
- `design-baseline-direct` 即 `P3 -> P5` 设计基线流存在。
- 五张卡、终端交付目录、P3 抽屉和图例面板均在 `1920 x 1080` 视口内。

## 3. 人工查看结论

已查看三张渲染图：

1. 总览态：P1 左侧外部知识入库箭头和标签已经删除，P1 底部挂载队列保留。
2. 总览态：P3 到 P5 的粉色设计基线流已经出现。
3. 总览态与图例态：阶段间连线出现小对象标签，用于表达数据从输出端向输入端流动。
4. 图例态：P1 的输入解释为底部挂载队列，不再解释为外部箭头。

## 4. 当前结论

自检通过，状态为 `待用户确认`。

