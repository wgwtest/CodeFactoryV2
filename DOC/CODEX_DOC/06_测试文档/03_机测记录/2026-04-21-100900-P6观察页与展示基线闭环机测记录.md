# P6 观察页与展示基线闭环机测记录

- 日期：2026-04-21
- 对应范围：`P6.1` 首屏观察门户、`P6.2` 跨阶段只读集成与状态投影、`P6.3` 设计语言与前端展示基线、`P6.4` 前端展示工具化实验场

## 1. 本轮改动

- 补齐 `platform-config` 路由族，正式提供 `display-baseline`、`routes`、`legend` 三类配置查询。
- 补齐 `platform-display` 路由族，正式提供 `workbench`、`experiments`、`promotion-candidates` 与实验登记接口。
- 新增 `/observation` 页面，正式消费 `ObservationProjection`，落地区块包括串行观察卡片区、平台总览条区、阶段告警区、降级说明区、跨阶段对比区与路由动作区。
- `/portal` 显式接入 `P6.3` 基线包、路由映射和图例配置，不再只依赖前端本地常量。
- `P6.4` 实验台改为后端驱动，模板、绑定、布局、预设、实验记录和晋升候选均从正式接口读取，并支持实验记录登记。

## 2. 机测记录命令

### 2.1 前端

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/P6ObservationPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/AppRoutes.test.tsx
```

结果：通过，`23` 个测试全部通过。

### 2.2 后端

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：通过，`10` 个测试全部通过。

### 2.3 差异检查

```bash
git diff --check
```

结果：通过。

## 3. 关键确认点

- `/portal` 可同时加载 `mock` 场景目录、门户投影、平台展示基线、平台路由和图例配置。
- `/observation` 已形成独立入口，不再回落到主壳层菜单页。
- 串行观察页可切换 `baseline / review-pressure / delivery-gap` 三套场景，并根据 `focus_stage_id` 与手动聚焦操作切换当前观察主位。
- `P6.4` 实验台中的模板选择区、绑定配置区、布局组合区、实时预览区、实验记录区、晋升评估区均继续存在，且实验记录与候选输出来自后端正式对象。
- 点击 `登记实验` 后会调用正式实验登记接口，而不是停留在纯前端本地状态。

## 4. 未做项

- 未执行 `apps/web` 全量构建；仓内仍存在与 `P6` 无关的既有前端问题，本轮不扩大处理范围。
