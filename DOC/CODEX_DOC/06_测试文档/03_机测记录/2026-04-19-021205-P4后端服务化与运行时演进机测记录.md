# P4 后端服务化与运行时演进机测记录

## 1. 机测记录目标

验证 `P4.4` 当前规划范围内的后台落地是否成立，并同步确认 `P4.2 / P4.3` 依赖的运行链路没有被回归破坏。

本轮重点覆盖：

- `P4.4.1` 统一业务循环首版落地
- `P4.4.2` `Runtime Coordinator / Worker / Queue` 标准化首版落地
- `P4.4.3` `P3 Input / P5 Query / Operator / Internal Runtime` 四类端口物理拆分
- `P4.4.4` 核心查询投影持久化与刷新链路落地

## 2. 本轮实现摘要

- 新增核心投影仓储：`apps/api/app/tool_hub/projection_repository.py`
- 将 `Overview / ToolList / EvolutionWorkspace` 查询投影改为显式持久化读取，并提供统一刷新：`apps/api/app/tool_hub/query_service.py`
- 新增独立 runtime worker / coordinator 骨架：`apps/api/app/tool_hub/runtime_worker.py`
- 为 runtime 暴露首版内部端口：`apps/api/app/api/routes/tool_hub_runtime.py`
- 将原 `tool_hub.py` 单文件路由拆成：
  - `tool_hub_operator.py`
  - `tool_hub_p3_input.py`
  - `tool_hub_p5_query.py`
  - `tool_hub_runtime.py`
  - `tool_hub_deps.py`
- 修正制造任务推进逻辑，使“计划已到期”的项可以被 runtime 立即补领并完成

## 3. 执行命令

```bash
uv run pytest apps/api/tests/test_tool_hub*.py -q
```

## 4. 机测记录结果

- 结果：通过
- 汇总：`28 passed`

覆盖含义：

- 现有 `P4` 工具仓库 API 基础回归通过
- 输入工序链闭环回归通过
- 自演进巡检回归通过
- 投影持久化与显式刷新测试通过
- 独立 worker 处理共享根目录任务测试通过
- 内部 runtime 端口调用测试通过

## 5. 结论

当前 `P4` 已完成本阶段规划范围内的代码落地与后端机测记录，可进入人工验收。

当前仍保留为后续增强项，不影响本轮闭环：

- `dead_letter`
- 专属 `projection queue`
- 真正独立进程/独立服务部署
- 更细粒度的 `Demand / Delivery` 读模型拆分
