# P1 远端运行工作台归并自测报告

## 1. 自测目标

验证远端分支 `origin/codex/archive-runtime-policy-workbench` 的 `P1` 大改动是否已经可以安全归并到当前主工作区，并确认冲突处理、静态检查和定向回归结果。

## 2. 自测范围

- 远端提交：`3db51ae archive runtime workbench and policy pipeline`
- 后端范围：
  - `apps/api/app/api/routes/archives.py`
  - `apps/api/app/api/routes/knowledge.py`
  - `apps/api/app/archive_knowledge/`
  - `apps/api/tests/test_archive_*`
- 前端范围：
  - `apps/web/src/App.tsx`
  - `apps/web/src/pages/ArchiveManagementPage.tsx`
  - `apps/web/src/pages/DocumentIntakePage.tsx`
  - `apps/web/src/test/*Archive*`
  - `apps/web/src/test/AppRoutes.test.tsx`
- 正式设计补充：
  - `DOC/CODEX_DOC/02_设计说明/P1_业务知识库/P1-业务知识库设计.md`

## 3. 归并前检查

1. `git fetch --all --prune --tags`
   - 发现新增远端分支：`origin/codex/archive-runtime-policy-workbench`
   - `origin/main` 未变化，仍为 `ad34ae7`
2. `git diff --stat main...origin/codex/archive-runtime-policy-workbench`
   - 67 个文件
   - 约 30187 行新增、1109 行删除
   - 主要集中在 `P1` 后端运行态、前端知识库管理页、接入验证页和 `docs/superpowers/specs/`
3. `git merge-tree main origin/codex/archive-runtime-policy-workbench`
   - 预演冲突文件：
     - `apps/web/src/App.tsx`
     - `apps/web/src/test/AppRoutes.test.tsx`
     - `apps/web/vite.config.ts`
4. 本地未提交已跟踪改动检查：
   - 与远端 `P1` 分支无路径重叠
   - 归并前已保存到 `stash@{0}`，并导出补丁到 `/tmp/codefactory-merge-safety/pre-p1-merge-tracked.diff`

## 4. 冲突处理

- `apps/web/src/App.tsx`
  - 保留主线已有 `P2 / P3 / P4 / P5 / P6` 独立路由
  - 合入远端新增 `/documents/intake` 接入解析验证入口
  - 保留主线 `VITE_DEFAULT_ROUTE` 默认路由兜底规则
- `apps/web/src/test/AppRoutes.test.tsx`
  - 以主线扩展后的大用例为基础
  - 合入 `/documents/intake` 路由测试
  - 更新文档页断言为“当前知识库文档”
- `apps/web/vite.config.ts`
  - 保留主线仓库根 `envDir`、端口和主机配置
  - 合入远端 `VITE_DEV_API_PROXY_TARGET` 兼容读取

## 5. 自测命令与结果

1. `git diff --check --cached`
   - 通过
2. `git diff --check`
   - 通过
3. `uv run pytest apps/api/tests/test_archive_policy_config_api.py apps/api/tests/test_archive_document_runtime_api.py apps/api/tests/test_archive_registry_api.py apps/api/tests/test_archive_incremental_rebuild.py apps/api/tests/test_formal_archive_extraction_policy.py -q`
   - 通过：`50 passed`
4. `corepack pnpm --dir apps/web exec vitest run src/test/AppRoutes.test.tsx src/test/DocumentIntakePage.test.tsx src/test/ArchiveManagementPolicy.test.tsx src/test/ArchiveManagementPage.test.tsx src/test/DocumentsPage.test.tsx src/test/archiveKnowledge.test.ts`
   - 通过：`6 passed`，`26 passed | 4 skipped`

## 6. 额外观察

- 使用 `corepack pnpm --dir apps/web test -- ...` 时实际触发了全量 Vitest，不适合作为本轮定向测试命令。
- 全量 Vitest 暴露 `src/test/P6PortalPage.test.tsx` 中一项既有 `P6 -> P5` 导航断言失败；该失败不在本轮 `P1` 远端归并改动范围内，本轮未修复。

## 7. 结论

- 远端 `P1` 运行工作台与策略管线改动已完成本地主工作区非提交归并。
- 冲突已处理，当前无未解决冲突。
- 定向 `P1` 后端与前端测试通过。
- 当前状态：已自测，待人工确认是否提交归并。
