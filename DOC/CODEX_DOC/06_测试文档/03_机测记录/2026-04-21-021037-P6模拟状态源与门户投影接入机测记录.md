# P6 模拟状态源与门户投影接入机测记录

## 1. 机测记录目标

验证 `P6.2.5 模拟状态源` 的后端公共读接口已经落地，并确认 `/portal` 已改为消费 `P6` 公共门户投影，而不再依赖页面内静态数据。

## 2. 本轮变更范围

- 后端新增 `P6` 公共读接口：
  - `apps/api/app/api/routes/p6.py`
  - `apps/api/app/p6/models.py`
  - `apps/api/app/p6/mock_scenarios.py`
  - `apps/api/app/p6/service.py`
  - `apps/api/tests/test_p6_api.py`
- 前端新增 `P6` 读侧客户端：
  - `apps/web/src/lib/p6.ts`
- 前端 `/portal` 改造为 API 驱动：
  - `apps/web/src/pages/P6PortalPage.tsx`
  - `apps/web/src/components/p6/P6BlueprintCanvas.tsx`
  - `apps/web/src/components/p6/P6BlueprintLegend.tsx`
  - `apps/web/src/components/p6/P6BlueprintNode.tsx`
  - `apps/web/src/components/p6/P6BlueprintArtifact.tsx`
  - `apps/web/src/components/p6/p6PortalData.ts`
  - `apps/web/src/components/p6/p6PortalGeometry.ts`
  - `apps/web/src/components/p6/p6PortalProjection.ts`
  - `apps/web/src/pages/P6PortalPage.css`
- 前端测试更新：
  - `apps/web/src/test/P6PortalPage.test.tsx`
  - `apps/web/src/test/p6PortalGeometry.test.ts`

## 3. 机测记录项

1. `/api/p6/mock-scenarios` 是否返回三套模拟场景。
2. `/api/p6/stage-snapshots` 是否返回五阶段统一快照与节点状态负载。
3. `/api/p6/portal-projection` 是否返回门户节点、关系、产物与摘要对象。
4. `/api/p6/observation-projection` 是否返回聚焦阶段与告警摘要。
5. `source=live` 是否明确返回“未实现”，而不是伪成功。
6. `/portal` 是否显示“模拟源 + 场景切换”小控件，并能切换场景。
7. `/portal` 既有交互是否仍然保留：
   - 推荐布局 / 个人布局
   - 拖拽与本地记忆
   - 语义线 / 投影聚合
   - 双击进入模块
8. 代码变更是否存在空白符或 patch 格式问题。

## 4. 机测记录执行

### 4.1 后端接口测试

执行命令：

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：

- `6 passed`
- 已覆盖：
  - 模拟场景列表
  - 基线阶段快照
  - 门户投影
  - 观察投影
  - `live` 未实现
  - 非法场景 `404`

### 4.2 前端门户测试

执行命令：

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts
```

结果：

- `12 passed`
- 已覆盖：
  - `/portal` 独立路由壳层
  - `模拟源` 控件与场景切换
  - 节点类型渲染
  - 点击高亮
  - 推荐布局 / 个人布局
  - 拖拽持久化
  - 语义线 / 投影聚合切换
  - 双击进入 `P5`
  - 节点几何边界约束

### 4.3 Patch 检查

执行命令：

```bash
git diff --check
```

结果：

- 通过

### 4.4 前端构建检查

执行命令：

```bash
corepack pnpm --dir apps/web build
```

结果：

- 未通过
- 暴露的是仓库内既有 TypeScript 问题，不属于本轮 `P6` 变更新引入问题，主要位于：
  - `apps/web/src/components/p3/P3AtomicToolRequestGenerator.tsx`
  - `apps/web/src/components/p4/P4RealToolDeliveryWorkspace.tsx`
  - `apps/web/src/components/p5/P5DeliveryManifestPanel.tsx`
  - `apps/web/src/lib/toolHub.ts`
  - `apps/web/src/pages/XXP3DocSimPage.tsx`
  - `apps/web/src/pages/XXP4SupplySimPage.tsx`
  - `apps/web/src/test/XXP4Page.test.tsx`
  - `apps/web/src/test/viteConfig.test.ts`

## 5. 机测记录结论

本轮 `P6` 模拟状态源与门户投影接入的目标功能已完成，针对性接口测试和门户测试通过。

当前状态：

- `P6` 后端公共读接口：已实现
- `/portal` API 驱动切换：已实现
- 针对性机测记录：通过
- 全量前端构建：受仓库既有 TS 错误阻塞
- 人工验收状态：待用户确认
