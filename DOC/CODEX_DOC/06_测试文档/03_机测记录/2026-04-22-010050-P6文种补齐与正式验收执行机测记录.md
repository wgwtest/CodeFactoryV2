# P6 文种补齐与正式验收执行机测记录

- 日期：2026-04-22
- 对应范围：`P6` 文种补齐、正式验收执行与运行态复核

## 1. 本轮补充动作

- 补齐 `P6.1.2 ~ P6.1.4`、`P6.2`、`P6.2.1 ~ P6.2.5`、`P6.3`、`P6.4` 的节点合同与验收大纲
- 新增 `DOC/CODEX_DOC/06_测试文档/04_人测记录/` 与 `05_验收结论/` 目录
- 按 `P6` 全树可测节点补齐 `14` 份节点级独立机测记录，并回链到各自节点合同
- 拉起当前工作树的前后端实例，执行页面与接口运行态复核

## 2. 机测记录命令与结果

### 2.1 差异检查

```bash
git diff --check
```

结果：通过。

### 2.2 后端 P6 接口测试

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：通过，`10` 个测试全部通过。

### 2.3 前端 P6 定向测试

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/P6ObservationPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/AppRoutes.test.tsx
```

结果：通过，`23` 个测试全部通过。

### 2.4 运行态检查

```bash
uv run uvicorn app.main:app --app-dir apps/api --host 127.0.0.1 --port 8050
corepack pnpm --dir apps/web dev --host 127.0.0.1 --port 5182
curl -I http://127.0.0.1:5182/portal
curl -I http://127.0.0.1:5182/observation
curl http://127.0.0.1:8050/api/platform-config/display-baseline
curl "http://127.0.0.1:8050/api/p6/observation-projection?source=mock&scenario=delivery-gap"
curl http://127.0.0.1:8050/api/p6/mock-scenarios
curl "http://127.0.0.1:8050/api/p6/portal-projection?source=mock&scenario=baseline"
curl http://127.0.0.1:8050/api/platform-config/routes
curl http://127.0.0.1:8050/api/platform-config/legend
curl http://127.0.0.1:8050/api/platform-display/workbench
curl http://127.0.0.1:8050/api/platform-display/promotion-candidates
curl -X POST http://127.0.0.1:8050/api/platform-display/experiments
```

结果：

- `/portal` 返回 `200 OK`
- `/observation` 返回 `200 OK`
- `display-baseline` 返回 `p6.3-v1`
- `observation-projection` 返回 `delivery-gap` 场景内容
- `mock-scenarios`、`portal-projection`、`routes`、`legend`、`workbench`、`promotion-candidates` 均返回正式内容
- `POST /api/platform-display/experiments` 成功返回实验记录 `exp-0002`

## 3. 当前结论

- `P6` 当前缺失的合同 / 大纲文种已补齐
- `P6` 全树可测节点当前均具备独立机测记录件，节点合同可回链到对应机测记录
- `P6` 主干代码与运行链路在当前工作树下可复核
- 当前状态：`待人工验收`
