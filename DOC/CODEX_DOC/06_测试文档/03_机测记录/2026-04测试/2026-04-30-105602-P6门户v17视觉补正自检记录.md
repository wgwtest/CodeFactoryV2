# P6 门户 v17 视觉补正自检记录

时间：2026-04-30 10:56:02

## 1. 本轮问题

用户指出 `/portal` 当前运行页没有按已确认 v17 原型落地：运行页仍表现为浅色网格、旧卡片、右侧大图例和非自适应世界视图。经重新截图确认，该问题成立。

对照事实源：

- `DOC/CODEX_DOC/08_原型与附图/2026-04-30-005207-CodeFactoryV2-P6语义画布流动与P3P5补充原型-v17/01-1920x1080-五阶段语义画布总览态.png`

## 2. 补正内容

1. 将 `/portal` 主画布改为 v17 深色星空语义画布背景。
2. 将五张系统卡片改为 v17 推荐布局和 `292 x 224` 卡片体量。
3. 将默认相机改为按浏览器视口自适应完整 `1920 x 1080` 推荐世界。
4. 将卡片内部改为：顶部用户积木、阶段图标、总体状态、实时流动、左右小圆点端口、底部挂载队列。
5. 将 P1 外部知识输入继续保持为底部挂载队列表达，不恢复左侧外部箭头。
6. 将语义流修正为 `P1 -> P2 -> P3 -> P4 -> P5 -> 交付目录`，并保留 `P3 -> P5` 设计基线流。
7. 将右侧大图例改为底部低干扰状态信号，避免遮挡卡片与连线。
8. 补足 mock 顶部用户积木和挂载队列密度，使运行态接近 v17 评审图。

## 3. 截图证据

- 1920 x 1080 运行截图：`DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-105602-P6门户v17视觉补正运行截图-1920x1080.png`
- 1365 x 768 运行截图：`DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-105602-P6门户v17视觉补正运行截图-1365x768.png`

截图自检结论：

- `1920 x 1080` 截图中五张系统卡片、交付目录、P3-P5 设计基线流、P1 底部知识队列均可见。
- `1365 x 768` 截图中画布按视口等比缩放，五张系统卡片同屏可见，未出现旧版浅色背景或右侧大图例遮挡。

## 4. 验证命令

已通过：

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：`12 passed`

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/P6SimulatorPage.test.tsx
```

结果：`15 passed`

```bash
corepack pnpm --dir apps/web exec tsc --noEmit --pretty false 2>&1 | rg "src/components/p6|src/pages/P6|src/lib/p6|src/test/P6" || true
```

结果：无 P6 相关 TypeScript 错误输出。

```bash
git diff --check
```

结果：无输出。

完整构建补充检查：

```bash
corepack pnpm --dir apps/web build
```

结果：失败，阻断项为既有非 P6 TypeScript 错误，集中在 `src/components/p3`、`src/components/p4`、`src/components/p5`、`src/lib/toolHub.ts`、`src/pages/XXP3DocSimPage.tsx`、`src/pages/XXP4SupplySimPage.tsx`、`src/test/XXP4Page.test.tsx`、`src/test/viteConfig.test.ts`。本轮 P6 过滤检查无 P6 相关错误。

## 5. 当前状态

当前状态：已自测，待人工验收。

运行地址：

- API：`http://127.0.0.1:8060`
- Web：`http://127.0.0.1:5183/portal?scenario=baseline&v=2`
