# P6 门户目录设计口径同步自检记录

- 记录时间：2026-04-30 00:04
- 工作分支：`feat/p6-portal-platform-entry`
- 工作目录：`.worktrees/p6-portal-platform-entry`
- 当前状态：目录级设计口径已同步，待人工审阅

## 1. 本轮目标

本轮延续 `P6.1` 首屏语义画布设计口径重写结果，对 `P6_门户与平台入口` 目录下其他专项设计文档做同步。

同步目标：

- `P6.3` 不再把独立参与用户节点、独立产物节点作为平台展示基线对象。
- `P6.4` 不再把实验柜、卡片配置入口或嵌入式实验台放在 `/portal` 主画布内。
- `P6.2` 保留既有技术对象名称，但明确其展示语义是系统运行态卡片和数据流连线。
- `P6` 总设计与 `P6.1` 中残留的旧对象说法完成扫尾。

## 2. 已更新设计事实源

- `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.3-设计语言与前端展示基线设计.md`
  - 将 `NodeVisualBaseline` 调整为 `PortalDisplayVisualBaseline`。
  - 基线对象调整为系统运行态卡片、顶部用户积木、底部挂载队列、语义流连线、端口、终端输出和图例解释。
- `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.4-前端展示工具化实验场设计.md`
  - 明确实验配置界面不得承载在 `P6.1` 门户主画布内。
  - 首版承载面调整为独立展示实验页、独立实验区或串行观察预览区。
- `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.2-跨阶段只读集成与状态投影设计.md`
  - 明确 `StageNodeStatusPayload` 的展示语义是系统卡片状态负载。
  - 明确 `node_list` 和 `artifact_list` 在 `P6.1` 中的映射边界。
- `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6-门户与平台入口设计.md`
  - 扫尾修正 `node_list`、`artifact_list` 和门户例外表述。
- `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
  - 扫尾修正旧“节点”词汇，避免继续触发节点化门户口径。

## 3. 自检命令

```bash
rg -n "蓝图画布|门户蓝图|节点、关系、产物|产物节点|参与用户节点|ParticipantUserNode|PortalArtifact|ArtifactNode|图例固定|右下角图例|内嵌实验台|卡片配置|节点层输出|关系层输出|摘要层输出|门户节点|阶段节点|行业用户.*独立|P6\\.4.*门户页|实验台.*门户|relationship_view_mode|关系视图|NodeVisualBaseline|节点视觉|节点状态负载|门户节点摘要|节点例外|节点规范|用户节点|主业务节点|独立节点" "DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口" -g '*.md'
```

结果：无输出。

```bash
rg -n "TODO|TBD|待定|？？|\?\?" "DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口" -g '*.md'
```

结果：无输出。

```bash
git diff --check -- "DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口"
```

结果：无空白错误。

## 4. 结论

`P6_门户与平台入口` 目录下正式设计文档已同步到当前 P6 首屏语义画布口径。

当前目录级基线为：

- `P6.1`：负责五阶段语义画布、系统卡片、语义流连线、图例解释和门户交互。
- `P6.2`：负责只读投影、系统卡片状态负载、数据流语义和降级新鲜度。
- `P6.3`：负责卡片、用户积木、挂载队列、端口、连线、终端输出和图例的展示基线。
- `P6.4`：负责独立展示实验和晋升候选，不承载在门户主画布内。
