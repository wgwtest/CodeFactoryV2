# P6 门户数据驱动连线自检记录

时间：2026-04-30 13:18:39

## 1. 目标

把 `/portal` 连线从“静态线名 + 装饰性流动点”修正为数据驱动的 payload 流：

- `知识供给`、`需求规格`、`模块工单包` 等通道名称不再作为主画面静态线名。
- `知识`、`规格`、`工单`、`基线`、`工具`、`目录` 作为 payload 实例沿路径移动。
- payload 数量从起点阶段输出端口 `current_rate` 或输出类实时计数派生。

## 2. 事实源

- 原型包：`DOC/CODEX_DOC/08_原型与附图/2026-04-30-005207-CodeFactoryV2-P6语义画布流动与P3P5补充原型-v17/`
- 正式设计：
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.1-首屏观察门户设计.md`
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.2-跨阶段只读集成与状态投影设计.md`
  - `DOC/CODEX_DOC/02_设计说明/P6_门户与平台入口/P6.3-设计语言与前端展示基线设计.md`

## 3. TDD 记录

先补充失败测试：

- 要求 `p6-flow-label-p1-p2` / `p6-flow-label-p3-p5` 不再存在。
- 要求 `P1 -> P2` 根据 `12 条/小时` 生成 12 个“知识”payload。
- 要求各主链流生成对应数量：`8` 个规格、`11` 个工单、`6` 个基线、`10` 个工具、`4` 个目录。
- 要求每个 payload 含 `animateMotion`，并带有 `data-payload-rate` / `data-payload-source-count`。

红灯结果：

```text
src/test/P6PortalPage.test.tsx (13 tests | 3 failed)
expected document not to contain element, found p6-flow-label-p1-p2
Unable to find an element by: [data-testid="p6-flow-payload-p2-p3-0"]
```

绿灯结果：

```text
src/test/P6PortalPage.test.tsx (13 tests) passed
```

## 4. 运行截图

- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-131839-P6门户数据驱动连线运行截图-1920x1080.png`
- `DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04测试/2026-04-30-131839-P6门户数据驱动连线运行截图-1365x768.png`

截图结论：

- 五张系统卡片仍在 v17 推荐布局内。
- 主画面不再显示静态 `知识供给` 等线名。
- 线内可见对象变为多枚 `知识 / 规格 / 工单 / 基线 / 工具 / 目录` payload。
- 1365x768 视口下画布仍自适应缩放，五阶段主链和终端交付目录可见。

## 5. 运动校验

用 Playwright 在运行页读取同一个 `P1 -> P2` payload 的两次位置：

```json
{"first":{"x":541.954833984375,"y":726.1023559570312,"width":47,"height":25},"second":{"x":520.1824340820312,"y":651.1033935546875,"width":47,"height":25},"moved":true}
```

结论：payload 不是静态摆放，运行时位置随时间变化。

## 6. 自动化验证

已通过：

```text
uv run pytest apps/api/tests/test_p6_api.py -q
12 passed in 1.09s
```

```text
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts src/test/P6SimulatorPage.test.tsx
Test Files  3 passed (3)
Tests  16 passed (16)
```

```text
git diff --check
无输出
```

P6 TypeScript 过滤检查：

```text
corepack pnpm --dir apps/web exec tsc --noEmit --pretty false 2>&1 | rg -i "p6|portal"
无输出；rg 返回 1 表示未匹配到 P6 / portal 相关错误。
```

完整前端构建：

```text
corepack pnpm --dir apps/web build
失败，失败点仍为既有非 P6 类型问题，集中在 P3 / P4 / P5 / toolHub / 既有测试类型。
```

## 7. 结论

本轮已把连线修正为数据驱动 payload 流，状态为：已自测，待人工验收。
