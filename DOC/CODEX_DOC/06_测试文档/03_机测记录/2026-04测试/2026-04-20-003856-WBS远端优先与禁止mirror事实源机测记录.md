# WBS 远端优先与禁止 mirror 事实源机测记录

## 1. 机测记录目标

验证本轮是否已经把以下规则同步进全局工程策略与当前项目本地文档：

- GitHub `Issues + sub-issues` 是 WBS 唯一事实源
- 默认不再创建或维护本地 `issue tree mirror` 作为当前树状态入口
- 若远端不可访问，只能回退到仓库主工作树中的正式文档根
- 不允许从当前隔离目录或历史 mirror 反推当前远端树状态

## 2. 本轮变更

本轮新增 / 更新文档如下：

- 更新 `/home/wgw/CodexProject/PROJECT-TRA/ENGINEERING_WORK_STRATEGY.md`
- 更新 `/home/wgw/CodexProject/PROJECT-TRA/skills/project-engineering-strategy/references/ENGINEERING_WORK_STRATEGY.md`
- 更新 `DOC/CODEX_DOC/00-本地工程策略映射.md`
- 更新 `DOC/CODEX_DOC/02_设计说明/00_总纲/01-文档治理与superpowers协同设计.md`
- 更新 `docs/superpowers/README.md`

## 3. 机测记录项

1. 检查全局策略是否新增 WBS 远端优先读取规则。
2. 检查全局策略是否明确禁止把本地 `issue tree mirror` 当作当前树状态入口。
3. 检查全局策略是否明确“远端不可访问时，只能回退到主工作树正式文档根”。
4. 检查当前项目本地映射与文档治理文档是否已经同步相同口径。
5. 检查 `docs/superpowers/README.md` 是否不再鼓励维护当前 WBS mirror。

## 4. 机测记录结果

### 4.1 全局策略检查

检查结果：

- `/home/wgw/CodexProject/PROJECT-TRA/ENGINEERING_WORK_STRATEGY.md` 已新增 `3.5.2A WBS 远端优先读取规则`。
- 技能引用文档 `/home/wgw/CodexProject/PROJECT-TRA/skills/project-engineering-strategy/references/ENGINEERING_WORK_STRATEGY.md` 已同步相同规则。

### 4.2 当前项目文档检查

检查结果：

- `DOC/CODEX_DOC/00-本地工程策略映射.md` 已明确：
  - GitHub parent/sub-issue 是 WBS 树唯一事实源
  - 默认不再新增或维护本地 `issue tree mirror`
  - 远端不可访问时回退到主工作树中的 `DOC/CODEX_DOC/`
- `DOC/CODEX_DOC/02_设计说明/00_总纲/01-文档治理与superpowers协同设计.md` 已将 `docs/superpowers/issues/` 降级为历史快照 / 过程证据。
- `docs/superpowers/README.md` 已不再把 `issues/` 目录描述为当前 WBS mirror 入口。

## 5. 结论

本轮机测记录通过。

当前状态：

- 全局策略：已更新
- 当前项目本地映射：已更新
- 人工验收状态：待用户确认
