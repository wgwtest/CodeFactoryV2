# P6 语义画布端口用户对齐 v16 自检记录

时间：2026-04-30 00:44:26

## 1. 自检对象

- 原型包：`DOC/CODEX_DOC/08_原型与附图/2026-04-30-003923-CodeFactoryV2-P6语义画布端口用户对齐修正版-v16/`
- 源文件：
  - `source/v16-semantic-canvas.html`
  - `source/v16-semantic-canvas.css`
- 评审图：
  - `01-1920x1080-五阶段语义画布总览态.png`
  - `02-1920x1080-P3选中抽屉态.png`
  - `03-1920x1080-语义流与图例说明态.png`

## 2. 执行命令与结果

### 2.1 截图生成

```bash
base="$PWD/DOC/CODEX_DOC/08_原型与附图/2026-04-30-003923-CodeFactoryV2-P6语义画布端口用户对齐修正版-v16"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v16-semantic-canvas.html#overview" "$base/01-1920x1080-五阶段语义画布总览态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v16-semantic-canvas.html#selected" "$base/02-1920x1080-P3选中抽屉态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v16-semantic-canvas.html#legend" "$base/03-1920x1080-语义流与图例说明态.png"
```

结果：三张截图均生成成功。

### 2.2 图片尺寸检查

```bash
file 'DOC/CODEX_DOC/08_原型与附图/2026-04-30-003923-CodeFactoryV2-P6语义画布端口用户对齐修正版-v16/'*.png
```

结果：三张评审图均为 `1920 x 1080`。

### 2.3 DOM 检查

使用 `@playwright/test` 在 `1920 x 1080` 视口分别打开 `#overview`、`#selected`、`#legend`。

检查结果：

- 三种状态均有五张系统卡。
- 三种状态均有 10 个端口。
- 端口视觉尺寸为 `20 x 20`。
- 端口文本视觉宽度为 `1px`，不作为大标签显示。
- 所有用户行纵向偏差最大值为 `0`，用户积木已对齐。
- 五张卡、终端交付目录、P3 抽屉和图例面板均在 `1920 x 1080` 视口内。

## 3. 人工查看结论

已查看三张渲染图：

1. 总览态：端口已退回小圆形吸附节点，顶部用户整齐对齐。
2. P3 选中态：P3 高亮与右侧抽屉保持，端口不再抢占卡片信息层级。
3. 图例态：图例解释小端口与连线语义，不常驻总览态。

## 4. 当前结论

自检通过，状态为 `待用户确认`。

