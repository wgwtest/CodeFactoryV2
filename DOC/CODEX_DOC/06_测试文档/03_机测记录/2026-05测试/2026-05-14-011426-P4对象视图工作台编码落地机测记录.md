# P4 对象视图工作台编码落地机测记录

测试时间：2026-05-14 01:14

## 1. 测试对象

- 前端页面：`apps/web/src/pages/XXP4Page.tsx`
- 前端投影适配：`apps/web/src/lib/toolHub.ts`
- 后端对象视图投影：`apps/api/app/tool_hub/query_models.py`、`apps/api/app/tool_hub/query_service.py`、`apps/api/app/tool_hub/projection_repository.py`
- 后端对象视图端口：`GET /api/tool-hub/operator/workbench/object-view`

## 2. 执行命令

```bash
corepack pnpm --dir apps/web exec vitest run src/test/p4ObjectViewsProjection.test.ts src/test/XXP4Page.test.tsx
```

```bash
uv run pytest apps/api/tests/test_tool_hub_api.py -q
```

```bash
corepack pnpm --dir apps/web build
```

## 3. 结果

- 前端 Vitest：2 个测试文件、9 条测试通过。
- 后端 P4 API 测试：7 条测试通过。
- 前端生产构建：`tsc && vite build` 通过。

## 4. 覆盖结论

- `/xx-p4` 已按 v8 对象页结构渲染左侧对象导航。
- 前端对象页使用统一 `buildP4ObjectViewsProjection` 派生 ViewModel。
- 后端 refresh 已包含 `object_workbench` 投影。
- `GET /api/tool-hub/operator/workbench/object-view` 可返回工单池、工单处理、工具构建、取用驾驶舱、工具资源列表、覆盖知识图谱、成品工具属性、演进配置和演进轨迹九类对象视图。

## 5. 当前状态

已自测，待人工验收。
