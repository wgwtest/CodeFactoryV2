# 2026-05-14 P2 需规管理收口自测记录

## 1. 自测目标

本轮自测用于确认 `P2` 分支在收口前只保留需求规格说明管理与发布链路范围内的修正，不混入平台交换层实现。

本轮重点覆盖：

- 需规管理列表条目排版修正，不再污染组织器会话中的规格树行样式。
- 需规管理列表补充 `删除` 动作，并通过确认模态窗二次确认。
- 需规新建时接受 Lab 模板实例 ID，并正确映射到编写工作台模板。
- 发布失败 `409` 时展示业务化提示，而不是直接透出 Axios 原始报错。
- 既有 `进入配置 / 发布 / P3 可接收` 链路保持可用。

## 2. 自测命令

| 命令 | 目的 |
| --- | --- |
| `uv run pytest apps/api/tests/test_requirement_spec_work_items_api.py apps/api/tests/test_requirement_authoring_api.py apps/api/tests/test_requirement_analysis_api.py apps/api/tests/test_requirement_specs_api.py -q` | 验证 P2 后端需规工作项、编写工作台、需求分析与结构化需规接口回归 |
| `npm --prefix apps/web test -- RequirementAnalysisLabPage.test.tsx requirementAnalysisLabViewModel.test.ts` | 验证前端需规管理页签、删除确认、发布提示与现有 Lab 交互回归 |
| `npm --prefix apps/web run build` | 验证前端构建通过 |
| `git diff --check` | 验证无空白和 patch 格式问题 |

## 3. 结果记录

| 命令 | 结果 |
| --- | --- |
| `uv run pytest apps/api/tests/test_requirement_spec_work_items_api.py apps/api/tests/test_requirement_authoring_api.py apps/api/tests/test_requirement_analysis_api.py apps/api/tests/test_requirement_specs_api.py -q` | `31 passed in 4.62s` |
| `npm --prefix apps/web test -- RequirementAnalysisLabPage.test.tsx requirementAnalysisLabViewModel.test.ts` | `23 passed` |
| `npm --prefix apps/web run build` | 通过，Vite 仍提示既有大 chunk 警告 |
| `git diff --check` | 通过，无空白错误 |

## 4. 结论

本轮 P2 收口修正已通过后端接口、前端 Lab 页面、TypeScript 构建与空白检查的聚焦自测。当前状态为“已自测，待人工验收”。
