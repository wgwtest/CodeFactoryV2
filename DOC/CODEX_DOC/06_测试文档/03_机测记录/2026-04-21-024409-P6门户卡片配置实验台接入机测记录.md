# P6 门户卡片配置实验台接入机测记录

- 日期：2026-04-21
- 对应范围：`P6.1` 首屏观察门户、`P6.4` 前端展示工具化实验场

## 1. 本轮改动

- 对齐当前工作树缺失的 `P6` 数据驱动代码，包括 `P6.2` 模拟状态源 API 与门户投影接入。
- 在 `/portal` 主页面新增“小型卡片配置”入口按钮。
- 新增 `P6.4` 嵌入式实验台，固定提供模板选择区、绑定配置区、布局组合区、实时预览区、实验记录区、晋升评估区。
- 支持对单个系统节点或参与用户节点进行选择性样式覆盖，并同步作用到门户画布节点。

## 2. 机测记录命令

### 2.1 前端

```bash
corepack pnpm --dir apps/web test src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts
```

结果：通过，`14` 个测试全部通过。

### 2.2 后端

```bash
uv run pytest apps/api/tests/test_p6_api.py -q
```

结果：通过，`6` 个测试全部通过。

### 2.3 差异检查

```bash
git diff --check
```

结果：通过。

## 3. 关键确认点

- `/portal` 可正常加载 `mock` 场景目录与门户投影。
- 门户页右上角存在 `卡片配置` 按钮，点击后可展开嵌入式实验台。
- 实验台 6 个区块完整可见，不是只有模板切换。
- 选择 `P5` 并切换为 `压缩卡片` 后，只影响 `P5` 节点，不会连带修改 `P2` 等其他系统节点。
- 选择 `行业用户` 并切换为 `信息卡片` 后，只影响参与用户节点。

## 4. 未做项

- 未执行 `apps/web` 全量构建；仓内仍存在与 `P6` 无关的既有 TypeScript 构建问题，本轮不扩大处理范围。
