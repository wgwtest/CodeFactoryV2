# P6 门户数据视图与模拟器联动自检记录

时间：2026-04-30 16:05:01

## 1. 本轮范围

- 新增 `/api/p6/portal-data`，从 `P6DisplayExportContract.v2`、阶段快照和模拟器历史样本派生五阶段精确数据视图。
- 新增前端 `/portal-data` 页面，展示五阶段总表、跨阶段流量图、场景摘要和阶段下钻区。
- 扩展 `/xx-p6-sim`，提交后同时给出语义画布与图表视图入口，并补正 P1 无外部输入端口、P3 设计基线输出到 P5。
- 在 `/portal` 上增加低干扰入口：`图表视图`、`模拟发生器`。

## 2. 运行证据

- Web：`http://127.0.0.1:5184/portal-data?scenario=baseline`
- API：`http://127.0.0.1:8062`
- 运行截图：`DOC/CODEX_DOC/06_测试文档/03_机测记录/2026-04-30-160501-P6门户数据视图运行截图-1920x1080.png`

## 3. 验证命令

已通过：

- `uv run pytest apps/api/tests/test_p6_api.py -q`：`14 passed`
- `corepack pnpm --dir apps/web test src/test/P6PortalDataPage.test.tsx src/test/P6SimulatorPage.test.tsx src/test/P6PortalPage.test.tsx src/test/p6PortalGeometry.test.ts`：`18 passed`
- `corepack pnpm --dir apps/web exec tsc --noEmit --pretty false`：退出码 `0`
- `corepack pnpm --dir apps/web build`：退出码 `0`
- `git diff --check`：无输出
- `curl http://127.0.0.1:8062/api/p6/portal-data?source=mock&scenario=baseline`：返回 `stage_count=5`、`flow_count=6`、`history_sample_count=0`

## 4. 自检结论

- `/portal-data` 首屏展示五阶段总表，不默认进入单阶段详情页。
- 无模拟历史样本时，跨阶段流量图显示空态，不由前端生成趋势。
- 模拟器提交响应包含 `/portal` 与 `/portal-data` 两个入口。
- 当前状态：已自测，待人工验收。
