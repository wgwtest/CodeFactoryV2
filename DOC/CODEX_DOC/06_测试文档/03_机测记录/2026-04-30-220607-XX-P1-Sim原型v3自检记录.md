# XX-P1-Sim 原型 v3 自检记录

**时间：** 2026-04-30 22:06:07

**对象：**

- `DOC/CODEX_DOC/08_原型与附图/2026-04-30-214734-CodeFactoryV2-XX-P1-Sim原型-v3/`

## 1. 自检范围

本次自检覆盖：

- v3 原型源文件可截图
- 三张评审图尺寸为 `1920 x 1080`
- 图面无明显裁切、重叠、文字溢出
- 概念边界符合 `XX-P1-Sim`：只模拟 P1 上游领域知识服务
- P2 普通编辑器不暴露 `XX-P1-Sim`、`mock`、`模拟` 或 `发生器` 来源概念

## 2. 执行命令

```bash
base="$PWD/DOC/CODEX_DOC/08_原型与附图/2026-04-30-214734-CodeFactoryV2-XX-P1-Sim原型-v3"
mkdir -p /tmp/cf-xx-p1-sim-v3
ln -sfn "$base/source/xx-p1-sim-prototype.html" /tmp/cf-xx-p1-sim-v3/prototype.html
for item in \
  "sim|01-1920x1080-XX-P1-Sim模拟输入台.png" \
  "bind|02-1920x1080-P2选择P1领域知识.png" \
  "editor|03-1920x1080-P2普通编辑态.png"; do
  state="${item%%|*}"
  name="${item#*|}"
  tmp="/tmp/cf-xx-p1-sim-v3/${state}-1920x1167.png"
  google-chrome --headless=new --disable-gpu --no-sandbox --hide-scrollbars --window-size=1920,1167 --screenshot="$tmp" "file:///tmp/cf-xx-p1-sim-v3/prototype.html#$state"
  python3 - "$tmp" "$base/$name" <<'PY'
from PIL import Image
import sys
src, dst = sys.argv[1], sys.argv[2]
Image.open(src).convert("RGB").crop((0, 0, 1920, 1080)).save(dst)
PY
done
file "$base"/*.png
```

## 3. 输出结果

`file` 检查结果：

- `01-1920x1080-XX-P1-Sim模拟输入台.png`：`1920 x 1080`
- `02-1920x1080-P2选择P1领域知识.png`：`1920 x 1080`
- `03-1920x1080-P2普通编辑态.png`：`1920 x 1080`

截图过程中的 Chrome `shared_memory_switch` 日志不影响 PNG 生成。

## 4. 视觉检查

已人工查看三张截图：

1. `XX-P1-Sim 模拟输入台`
   - 独立页面可见。
   - 显示服务注册、领域目录、知识版本、输出契约、调用日志。
   - 目录对象为 `空域规划领域知识`、`政务服务领域知识`、`供应链协同领域知识`。
   - 未出现 `空域协同规划软件`。
   - 长接口路径已换行，无明显横向溢出。

2. `P2 选择 P1 领域知识`
   - 表达 P2 发现 `XX-P1-Sim` 与真实 P1 未接入。
   - 用户选择的是领域知识，不是目标软件。
   - 知识预览包含业务对象、业务规则、流程知识和证据片段。

3. `P2 普通编辑态`
   - 左侧只有 `问答模式 / 表单模式`。
   - 右侧保持 Word 式标准需求规格说明。
   - 显示 `领域知识已绑定`。
   - 未显示 `XX-P1-Sim`、`mock`、`模拟` 或 `发生器` 来源概念。

## 5. 结论

自检通过。

当前状态：待用户确认。
