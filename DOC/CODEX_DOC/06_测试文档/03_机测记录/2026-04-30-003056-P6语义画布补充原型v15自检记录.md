# P6 语义画布补充原型 v15 自检记录

时间：2026-04-30 00:30:56

## 1. 自检对象

- 原型包：`DOC/CODEX_DOC/08_原型与附图/2026-04-30-001317-CodeFactoryV2-P6语义画布补充原型-v15/`
- 源文件：
  - `source/v15-semantic-canvas.html`
  - `source/v15-semantic-canvas.css`
- 评审图：
  - `01-1920x1080-五阶段语义画布总览态.png`
  - `02-1920x1080-P3选中抽屉态.png`
  - `03-1920x1080-语义流与图例说明态.png`

## 2. 执行命令

### 2.1 截图生成

```bash
base="$PWD/DOC/CODEX_DOC/08_原型与附图/2026-04-30-001317-CodeFactoryV2-P6语义画布补充原型-v15"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v15-semantic-canvas.html#overview" "$base/01-1920x1080-五阶段语义画布总览态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v15-semantic-canvas.html#selected" "$base/02-1920x1080-P3选中抽屉态.png"
corepack pnpm --dir apps/web exec playwright screenshot --viewport-size=1920,1080 "file://$base/source/v15-semantic-canvas.html#legend" "$base/03-1920x1080-语义流与图例说明态.png"
```

结果：三张截图均生成成功。

### 2.2 图片尺寸检查

```bash
file 'DOC/CODEX_DOC/08_原型与附图/2026-04-30-001317-CodeFactoryV2-P6语义画布补充原型-v15/'*.png
```

结果：

- `01-1920x1080-五阶段语义画布总览态.png`：`1920 x 1080`
- `02-1920x1080-P3选中抽屉态.png`：`1920 x 1080`
- `03-1920x1080-语义流与图例说明态.png`：`1920 x 1080`

### 2.3 DOM 边界检查

命令摘要：使用 `@playwright/test` 在 `1920 x 1080` 视口分别打开 `#overview`、`#selected`、`#legend`，检查五张卡片、终端输出、选中抽屉和图例面板的 bounding box。

结果：

- 三种状态均有五张系统卡。
- 三种状态均有 10 个端口。
- 三种状态均有 23 个顶部接入用户积木。
- 五张卡片、终端交付目录、选中抽屉和图例面板均位于 `1920 x 1080` 视口内。

### 2.4 占位词与空白检查

```bash
rg -n "$placeholder_pattern" 'DOC/CODEX_DOC/08_原型与附图/2026-04-30-001317-CodeFactoryV2-P6语义画布补充原型-v15'
git diff --check -- 'DOC/CODEX_DOC/08_原型与附图'
```

结果：

- 占位词扫描无命中。
- `git diff --check` 无输出。

## 3. 人工截图检查结论

已查看三张渲染图：

1. 总览态：五张系统卡同屏，独立行业用户卡已移除，P1 保留外部知识输入和底部知识挂载队列，P5 输出为交付目录。
2. P3 选中态：P3 及关联流高亮，右侧抽屉浮出，抽屉未遮挡 P5 或交付目录。
3. 图例态：图例单独解释卡片、端口、流线和终端输出，不作为总览态常驻面板。

## 4. 继承关系检查

v15 已在 `original/` 保存 v4、v12、v14 的关键截图副本，并在 README 写明完整历史包路径与继承依据。旧历史原型包未移动、未覆盖。

## 5. 当前结论

自检通过，状态为 `待用户确认`。

本记录只证明原型包结构、截图尺寸、可再生成性和基本布局边界通过机测；不代表用户已确认 v15 作为实现标准。
