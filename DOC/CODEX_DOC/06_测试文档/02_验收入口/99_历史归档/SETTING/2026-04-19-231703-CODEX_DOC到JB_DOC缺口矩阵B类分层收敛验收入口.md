# CODEX_DOC 到 JB_DOC 缺口矩阵 B 类分层收敛验收入口

## 1. 验收目标

确认当前缺口矩阵已经不再把所有源侧问题笼统归为一个 `B` 类，而是进一步区分为：

- `B1`：已具备补源条件，补完即可投影
- `B2`：仍属硬缺口，暂不投影

## 2. 验收项

- [ ] `DOC/CODEX_DOC/01_需求分析/02-CODEX_DOC到JB_DOC完整投影缺口矩阵.md` 已完成 `B1 / B2` 分层
- [ ] 文档已明确给出 `B1` 与 `B2` 的定义，而不是只改表头
- [ ] `P1 / P2 / P3 / P4` 契约级缺口已归入 `B1`
- [ ] `P5`、`P6.R1 / P6.R2`、平台级测试/发布/移交/实例链已归入 `B2`
- [ ] 文档已把 `JB_DOC` 的后续处置升级为三种：
  - `A` 直接投影
  - `B1` 先补源再投影
  - `B2` 保持缺口标记
- [ ] 文档已给出新的补齐顺序，而不是沿用旧的单层 `B` 类顺序

## 3. 验收说明

- 本入口验收的是“缺口矩阵的分层是否已形成并可执行”。
- 本入口不代表 `B1` 源文已经补齐。
- 本入口不代表 `B2` 已经被解决。

## 4. 当前状态

- 当前状态：待用户确认
