# 远端文档路径同步验收入口

## 1. 验收目标

确认 GitHub Project 与一级 WBS Issue 已同步到新的正式文档根口径。

## 2. 验收项

- 检查 GitHub Project `CodeFactoryV2 Delivery Roadmap` 的 readme 是否出现 `DOC/CODEX_DOC/README.md`
- 检查 `#1` 是否出现“正式文档路径”段落
- 检查 `#2`、`#6`、`#9`、`#12`、`#25`、`#59` 是否都出现对应 `DOC/CODEX_DOC/04_研制计划/...` 路径
- 检查 `#9` 是否明确说明 `P3` 远端子树仍为旧结构、后续以正式节点计划为准
- 检查 Project readme 是否明确 `DOC/CODEX_DOC/` 为正式文档层、`docs/superpowers/` 为工作文档层

## 3. 当前状态

- 待用户确认
