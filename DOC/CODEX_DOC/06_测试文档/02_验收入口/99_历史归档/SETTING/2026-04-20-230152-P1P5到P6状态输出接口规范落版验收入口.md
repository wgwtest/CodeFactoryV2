# P1P5 到 P6 状态输出接口规范落版验收入口

## 1. 验收目标

确认 `P1 ~ P5 -> P6` 的共享状态输出规范已经以正式文档形式固定，并且 `P1 ~ P5` 与 `P6.2` 均建立了明确、可追溯的规范依赖关系。

## 2. 验收项

- [ ] 已在 `DOC/CODEX_DOC/03_规范与流程/01_数据规范/` 下新增共享规范 `01-P1-P5到P6状态输出接口规范.md`
- [ ] 共享规范已明确统一查询契约、统一骨架字段、五阶段差异字段、节点状态负载、订阅接口、新鲜度与降级规则
- [ ] 共享规范已明确“阶段事实来源在各阶段正式设计中说明，平台消费映射在 `P6.2` 中说明”的职责边界
- [ ] `P6.2` 已在顶部关联文档、`3.2` 小节和正式文档关系中引用共享规范
- [ ] `P1` 已在正式文档关系中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- [ ] `P2` 已在正式文档关系中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- [ ] `P3` 已在正式文档关系中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- [ ] `P4` 已在正式文档关系中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- [ ] `P5` 已在正式文档关系中声明：若向 `P6` 暴露平台状态输出，必须遵循共享规范
- [ ] `P6.2` 中的查询接口字段名已与共享规范一致，不再保留旧口径冲突

## 3. 验收说明

- 本轮验收的是正式设计与规范位置，不代表 `P1 ~ P5` 的真实接口代码已经全部落地。
- 本轮重点是把后续各阶段补接口时必须遵守的最小契约固定下来。

## 4. 当前状态

- 当前状态：待用户确认
