# P6 门户卡片配置实验台接入验收入口

- 日期：2026-04-21
- 对应范围：`P6.1`、`P6.2`、`P6.4`

## 1. 门户承载

- [ ] `/portal` 继续作为 `P6` 主入口，不额外跳转新业务页面。
- [ ] 门户页存在小型 `卡片配置` 入口按钮。
- [ ] 点击按钮后出现嵌入式实验台，收起后不影响门户主画布使用。

## 2. P6.4 六区块

- [ ] 实验台包含模板选择区。
- [ ] 实验台包含绑定配置区。
- [ ] 实验台包含布局组合区。
- [ ] 实验台包含实时预览区。
- [ ] 实验台包含实验记录区。
- [ ] 实验台包含晋升评估区。

## 3. 选择性配置

- [ ] 可以把配置对象切换到某个具体系统节点，例如 `P5`。
- [ ] 切换 `P5` 模板后，只影响 `P5`，不会全局替换所有系统节点。
- [ ] 可以把配置对象切换到参与用户节点。
- [ ] 切换参与用户模板后，只影响参与用户节点。

## 4. 数据与边界

- [ ] 门户与实验台仍只消费 `P6.2` 的只读投影，不直接读取 `P1 ~ P5` 私有状态。
- [ ] 当前默认仍走 `mock` 源，不伪装 `live` 已接通。
- [ ] 正式设计说明已回写到 `DOC/CODEX_DOC`，明确首版落地形态为门户内嵌实验台。
