# P3 文档口径收敛验收入口

## 1. 验收目标

确认 `P3` 本地工作文档已按最新方案完成口径统一。

## 2. 验收项

- 检查 `P3` issue mirror 中 `P3.2` 是否为“需求规格说明接入与分析”
- 检查 `P3` issue mirror 中 `P3.3` 是否为“软件设计说明生成与校核”
- 检查 `XX-P2-Sim` 是否被写为 `P3.2` 下的子节点
- 检查 `P3` plan 的 Goal/Architecture 是否已包含“接入与分析 -> 生成与校核”
- 检查 `P3` design spec 是否已包含“先接入分析、后生成校核”的约束

## 3. 当前状态

- 待用户确认
