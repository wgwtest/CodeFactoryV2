# P6 节点模型与状态输出契约补强自测

## 1. 自测目标

验证本轮是否已经补齐 `P6.1` 中缺失的节点显示模型，并把对应的数据来源契约和样式基线分别回写到 `P6.2` 与 `P6.3`。

## 2. 本轮变更范围

- 更新 `P6.1` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/11-P6.1-首屏观察门户设计.md`
- 更新 `P6.2` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/12-P6.2-跨阶段只读集成与状态投影设计.md`
- 更新 `P6.3` 专项设计：
  - `DOC/CODEX_DOC/02_设计说明/13-P6.3-设计语言与前端展示基线设计.md`

## 3. 自测项

1. 检查 `P6.2` 是否已新增五阶段状态输出契约与统一节点状态负载。
2. 检查 `P6.1` 是否已新增系统状态卡节点、五阶段差异字段和参与用户节点模型。
3. 检查 `P6.3` 是否已新增节点样式基线与节点相关页面区块。
4. 检查三份文档中的关键术语是否一致。

## 4. 自测结果

### 4.1 `P6.2` 读侧契约补强

检查结果：

- 已新增“五阶段状态输出契约”章节。
- 已明确：
  - 阶段侧负责生产正式状态事实
  - `P6.2` 负责固定平台消费的最小字段与映射规则
- 已新增 `StageNodeStatusPayload`，用于向门户节点提供统一显示负载。

### 4.2 `P6.1` 节点显示模型补强

检查结果：

- 已新增“节点显示输入”章节。
- 已新增：
  - 系统状态卡节点
  - 五阶段系统节点差异字段
  - 参与用户节点
- 已补齐系统状态卡节点的 5 层信息结构、尺寸范围和状态变化规则。
- 已补齐参与用户节点“显示什么 / 不显示什么 / 视觉权重如何控制”的规则。

### 4.3 `P6.3` 节点样式基线补强

检查结果：

- 已新增“节点样式基线输出”章节。
- 已新增 `NodeVisualBaseline` 对象。
- 已把系统状态卡节点区和参与用户节点区纳入共享展示基元基线和页面区块映射。

### 4.4 最小一致性校验

本轮通过以下方式完成最小校验：

- 使用 `rg` 检查三份文档的章节编号是否连续
- 使用 `rg` 检查 `SystemStageCardNode / ParticipantUserNode / StageNodeStatusPayload / NodeVisualBaseline` 等关键术语是否存在且互相引用

## 5. 结论

本轮 `P6` 节点模型与状态输出契约补强自测通过。

当前状态：

- 文档补强状态：已完成
- 代码实现状态：本轮未改动
- 人工验收状态：待用户确认
