# v6 对 v5 用户批注的处理记录

## 1. 批注来源

用户在 2026-05-13 对 v5 原型提出以下批注：

1. 6.1 工作区左上角三个按钮没有实际意义，应删除。
2. 全局右上角 `刷新需规 / 新建软设 / 打开当前软设` 中，只有刷新需规有意义。
3. 6.2 文档视图已经是草稿，`生成草稿` 没有意义；保存草稿可以保留。
4. `文档视图 / 结构化数据` 是整个可视化区域的切换，应放到软设工作区标题栏。
5. 6.3 结构化数据视图不需要 `同步正文` 和 `补全结构`，只保留 `生成投影候选`。
6. 6.4 P4 投影与工单的关系需要讲清楚，P4 投影本身就是工单组织。
7. 6.5 到 6.7 暂时先不要修改，可以先按通过处理。

## 2. 本版处理结论

- 已处理：6.1 工作区标题栏不再显示动作按钮。
- 已处理：全局右上只保留 `刷新需规`。
- 已处理：软设文档视图标题栏只保留 `保存草稿` 和 `提交复核`。
- 已处理：`文档视图 / 结构化数据` 切换上移到软设工作区标题栏。
- 已处理：结构化数据视图只保留 `生成投影候选`。
- 已处理：P4 视图标题和说明统一为 `P4 工单投影树`。
- 已处理：6.5 到 6.7 主体结构继承 v5，不做本轮修改。

## 3. 保留说明

- 6.1 右侧关联软设列表底部仍保留 `新建软设`，因为它是列表对象内的真实功能入口，不是顶部重复动作。
- 6.2 右侧章节交互仍保留 `扩写本节 / 补充小节 / 应用补丁`，因为本轮没有要求删除这些章节级动作。
