# v12 评审响应记录

## 1. 来源

用户对 v11 的进一步判断：

- 按当前形态链逻辑，`需规转软设`、`软设工作区`、`P4 投影` 三个 Tab 应合成一个。
- 合并后的 Tab 叫 `软设工作区`。

## 2. 处理结果

| 批注 | v12 处理 |
| --- | --- |
| 三个 Tab 应合成一个 | 左侧删除 `需规转软设` 和 `P4 投影` 独立入口，只保留 `软设工作区` |
| 合并后叫软设工作区 | 左侧主入口、工作区标题和说明统一改为 `软设工作区` |
| 需规转软设仍要存在 | 作为软设工作区形态链首段：`需规 -> 软设文档` |
| P4 投影仍要存在 | 作为软设工作区形态链末段：`展示形态 -> P4 投影` |
| 不改变滑窗逻辑 | 继承 v11 双元素滑窗排版和右侧 Inspector |

## 3. 仍待用户确认

- 左侧现在只保留五个入口是否合理：`需规输入`、`软设工作区`、`当前 Turn`、`检查评审`、`运行日志`。
- 是否将 v12 作为后续代码实现的导航和软设工作区基线。
