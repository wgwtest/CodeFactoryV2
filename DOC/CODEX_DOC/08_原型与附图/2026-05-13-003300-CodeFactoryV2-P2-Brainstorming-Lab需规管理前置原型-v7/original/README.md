# 原始材料说明

v7 继承 v6 的真实运行系统截图作为运行层事实源，并新增用户本轮批注作为对象管理层事实源。

## 1. 继承材料

`runtime-screens/` 中的截图来自 v6，说明当前真实 Lab 已具备复杂组织器配置、会话过程产物、Turn 审计和 Provider 调用日志。

这些截图继续作为 v7 中 `组织器配置`、`会话管理`、`当前 Turn`、`调用日志` 的事实源。

## 2. 新增事实源

用户在 2026-05-13 明确提出：

```text
先有需求规格说明管理。用户新建一个需求规格说明，然后进入或点编辑，再进入当前组织器配置这些概念。
```

v7 的新增 `需求规格说明管理` Tab 由该批注意图驱动。
