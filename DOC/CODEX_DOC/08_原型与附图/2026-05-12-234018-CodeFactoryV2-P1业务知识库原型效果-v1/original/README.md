# Original 材料说明

本版本包没有外部图片材料。

根目录下 `01`、`03`、`04`、`06`、`10`、`11`、`13` 这 7 张 PNG 均复制自仓库内历史 P1 原型包：

`DOC/CODEX_DOC/08_原型与附图/2026-05-05-213407-CodeFactoryV2-P1知识抽取闭环规则重算原型提示词-v1/`

使用目的：

1. 作为当前 `P1` 实现效果说明的结构参考图。
2. 辅助评审知识库管理、资料接入、抽取运行、策略规则、质量图谱、发布输出和知识成果等页面意图。
3. 在未完成真实运行态复截前，避免本版本包缺少图文证据链。

使用限制：

1. 这些图片不是当前工作树即时运行截图。
2. 这些图片不作为逐像素 UI 验收基线。
3. 当前真实事实以 `README.md` 中的路由、代码文件和 API 映射为准。

根目录下 `14` 到 `18` 这 5 张 PNG 是本版本新增消费者视图静态原型截图，源文件为：

`source/p1-consumer-views-prototype.html`
