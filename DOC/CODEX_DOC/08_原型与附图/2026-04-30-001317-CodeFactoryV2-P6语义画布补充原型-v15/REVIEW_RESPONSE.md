# v15 评审响应说明

## 1. 本轮输入

用户补充要求：

> 之前卡片的相关原型设计不要丢，不要复写。要充分参考当前的设计结论和之前的原型草图的设计结果，不要重复造轮子，不要把好的成果给丢下。

## 2. 响应结论

该要求成立，并作为 v15 的硬约束处理。

v15 没有重做卡片详情，而是：

1. 把 v12 的 P1 业务知识库详情卡作为 P1 同屏压缩卡的事实源。
2. 把 v14 的 P2-P5 总体状态详情卡作为 P2-P5 同屏压缩卡的事实源。
3. 把 v4 的星空画布和选中抽屉作为画布状态的事实源。
4. 把上述历史截图复制到 `original/`，并在 README 中逐项说明继承关系。

## 3. 本版修正点

| 关注点 | v15 处理 |
| --- | --- |
| 旧卡片原型不要丢 | `original/` 保存 v4/v12/v14 关键截图副本，README 写明完整历史包路径 |
| 不重复造轮子 | 卡片内部结构沿用顶部用户、左右端口、总体状态、实时状态、底部队列 |
| 五节点同屏 | 总览态固定五张系统卡，不保留独立行业用户卡 |
| P1 知识挂载 | P1 卡片底部保留更突出的知识挂载队列，左侧保留外部知识输入 |
| 连线语义 | 每条主链连线标注对象类型、方向和示例速率 |
| P6 不作为输出接口 | P5 终端输出为交付目录，不连接 P6 |

## 4. 保留风险

1. v15 是静态原型，粒子运动、缩放降级和队列前移在实现中仍需用真实事件链表达。
2. 同屏压缩卡不是 v12/v14 详情卡的替代版本，后续实现需要同时引用 v15 与 v12/v14。
3. 图例态为解释状态，不应被理解为门户总览中的常驻左侧面板。

