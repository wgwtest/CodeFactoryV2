# v17 评审响应说明

## 1. 用户批注

用户对 v16 概念方向基本认可，同时提出三项补充：

1. P1 业务知识库不应再画 `外部知识入库 23 条/分钟` 的外部箭头，因为底部挂载队列已经表达知识正在入库。
2. 阶段间连线应能看到数据对象从输出向输入流动或涌动，可以是小标签或小点。
3. 需要补充 `P3 -> P5` 的箭头。

## 2. 判断

三条批注均成立。

v16 中 P1 外部入库箭头与底部挂载队列表达重复，且会让 P1 的输入语义变成两个入口。对于 P1，底部挂钩队列才是更直观的“知识正在排队吸收 / 入库”表达。阶段间连线已有方向，但缺少更像数据对象的流动标签。P3 与 P5 之间确实需要单独显示一条设计基线同步关系。

## 3. v17 修正

| 批注点 | v17 处理 |
| --- | --- |
| P1 外部入库箭头重复 | 删除 `external-knowledge` 外部箭头和 `外部知识入库 · 23 条/分钟` 标签 |
| P1 输入表达 | P1 不再显示左侧外部输入端口；底部挂载队列承担“知识入库数量和顺序”表达 |
| 数据流动感 | 在每条阶段间流线上增加小对象标签：`知识`、`规格`、`工单`、`基线`、`工具`、`目录` |
| P3 到 P5 | 新增 `P3 -> P5` 的 `设计基线流`，用粉色虚线和 `基线` 标签表达 |

## 4. 保持不变

1. 五阶段主链仍是 `P1 -> P2 -> P3 -> P4 -> P5 -> 交付目录`。
2. P1 仍只向 P2 输出发布态知识。
3. P5 仍输出到交付目录，不输出到 P6。
4. v12 / v14 卡片详情原型不覆盖。
5. v16 不覆盖，v17 作为新版本独立评审。

## 5. 当前结论

v17 状态：用户已确认方向。

确认依据：用户在 2026-04-30 反馈“可以，我觉得当前的设计大差不差了”。该反馈按方向确认处理，不按逐像素定稿处理。
