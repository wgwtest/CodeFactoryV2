# 原始材料说明

v8 继承 v6 的真实运行系统截图作为运行层事实源，继承 v7 的“需规对象前置”设计方向，并新增用户对 v7 的简化批注作为本版 4.1 调整依据。

## 1. 继承材料

`runtime-screens/` 中的截图来自 v6，说明当前真实 Lab 已具备复杂组织器配置、会话过程产物、Turn 审计和 Provider 调用日志。

这些截图继续作为 v8 中 `组织器配置`、`会话管理`、`当前 Turn`、`调用日志` 的事实源。

## 2. 继承设计方向

v7 已确立：P2 Lab 前面需要 `需求规格说明管理` 首 Tab，用户先新建、打开或编辑一份需求规格说明，再进入组织器配置和运行层视图。

## 3. 新增事实源

用户在 2026-05-13 进一步提出：

```text
4.1 简单一点，就是需规的列表就行。右侧当前需规可以先移除。现在不想非得把 4.1 和 4.2 合并在一块。
```

v8 的新增变化由该批注意图驱动：4.1 只保留列表入口，移除右侧当前需规详情。
